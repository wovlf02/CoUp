# ✅ 화상 통화 스크롤 문제 최종 해결

> **날짜**: 2025-11-19  
> **문제**: 화상 회의 페이지에서 스크롤 발생  
> **원인**: `padding-bottom: 80px`로 인한 높이 초과  
> **상태**: ✅ 완전 해결

---

## 🐛 문제 원인

### Before (문제 상황)
```css
.mainContent {
  flex: 1;
  padding-bottom: 80px; /* ← 문제: 추가 높이 발생 */
}
```

**높이 계산**:
- 헤더: ~64px
- 메인콘텐츠: flex: 1 (남은 공간 전체)
- **+ padding-bottom: 80px** ← 초과!
- 컨트롤바: 80px (fixed)
- **총합: 100vh + 80px → 스크롤 발생!**

---

## ✅ 해결 방법 (최종)

### 1. Container를 position: fixed로 완전 고정

```css
.container {
  position: fixed; /* ← 핵심: 완전히 고정 */
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  width: 100vw;
  height: 100vh;
  background: var(--gray-50);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  z-index: 1;
}
```

**효과**: 
- ✅ body와 html의 스크롤에 영향받지 않음
- ✅ 완전히 화면에 고정되어 스크롤 불가능
- ✅ 다른 페이지 요소와 독립적

### 2. 헤더 고정 높이 지정

```css
.videoHeader {
  flex-shrink: 0; /* 크기 고정 */
  height: 64px;   /* 명시적 높이 */
  /* ...existing code... */
}
```

### 2. 헤더 고정 높이 지정

```css
.videoHeader {
  flex-shrink: 0; /* 크기 고정 */
  height: 64px;   /* 명시적 높이 */
  /* ...existing code... */
}
```

### 3. 메인콘텐츠 정확한 높이 계산

```css
.mainContent {
  height: calc(100vh - 64px - 80px); /* 전체 - 헤더 - 컨트롤바 */
  min-height: 0; /* flex 자식의 overflow 작동을 위해 필수 */
  overflow: hidden;
  /* padding-bottom 제거! */
}
```

### 3. 메인콘텐츠 정확한 높이 계산

```css
.mainContent {
  height: calc(100vh - 64px - 80px); /* 전체 - 헤더 - 컨트롤바 */
  min-height: 0; /* flex 자식의 overflow 작동을 위해 필수 */
  overflow: hidden;
  /* padding-bottom 제거! */
}
```

### 4. 컨트롤바 전체 너비 적용

```css
.controlBar {
  left: 0; /* 전체 너비 사용 */
  /* ...existing code... */
}
```

---

## 📊 높이 계산 구조

### Before ❌
```
100vh (전체 화면)
├─ 헤더: ~64px
├─ 메인콘텐츠: flex: 1 (~100vh - 64px)
│   └─ padding-bottom: 80px ← 초과!
└─ 컨트롤바: 80px (fixed)

총합: 100vh + 80px = 스크롤 발생 ❌
```

### After ✅
```
100vh (전체 화면)
├─ 헤더: 64px (고정)
├─ 메인콘텐츠: calc(100vh - 64px - 80px) ← 정확!
│   ├─ 참여자 목록
│   ├─ 비디오 영역
│   └─ 채팅
└─ 컨트롤바: 80px (fixed, 공간 차지 안 함)

총합: 정확히 100vh = 스크롤 없음 ✅
```

---

## 🔧 수정된 파일

### 1. `/coup/src/app/my-studies/[studyId]/video-call/page.module.css`

**변경사항**:
- ✅ `.container`: `position: fixed` + `top/left/right/bottom: 0` 추가 (완전 고정)
- ✅ `.container`: `width: 100vw` + `height: 100vh` + `z-index: 1` 추가
- ✅ `.videoHeader`: `flex-shrink: 0` + `height: 64px` 추가
- ✅ `.mainContent`: `padding-bottom: 80px` 제거
- ✅ `.mainContent`: `height: calc(100vh - 64px - 80px)` 추가
- ✅ `.mainContent`: `min-height: 0` 추가

### 2. `/coup/src/components/video-call/ControlBar.module.css`

**변경사항**:
- ✅ `.controlBar`: `left: clamp(200px, ...)` → `left: 0` 변경

---

## 🧪 테스트 확인사항

1. ✅ **페이지 스크롤 없음** - 브라우저 스크롤바 표시 안 됨
2. ✅ **참여자 목록** - 하단까지 정상 표시
3. ✅ **채팅** - 입력창이 컨트롤 바 위에 표시
4. ✅ **비디오** - 적절한 크기로 중앙 배치
5. ✅ **컨트롤 바** - 화면 하단에 전체 너비로 고정

---

## 💡 핵심 포인트

### CSS calc() 활용
```css
/* 정확한 높이 계산으로 스크롤 방지 */
height: calc(100vh - 64px - 80px);
/*            ↑      ↑      ↑
         전체높이  헤더  컨트롤바 */
```

### min-height: 0의 중요성
```css
/* flex 자식 요소의 overflow가 작동하려면 필수 */
.mainContent {
  flex: 1;
  min-height: 0; /* 이게 없으면 overflow: hidden이 작동 안 함 */
  overflow: hidden;
}
```

### fixed 요소는 공간을 차지하지 않음
```css
/* position: fixed는 문서 흐름에서 제외됨 */
.controlBar {
  position: fixed; /* 공간 차지 안 함 */
  bottom: 0;
  height: 80px;
}

/* 따라서 calc에서 빼줘야 함 */
.mainContent {
  height: calc(100vh - 헤더 - 컨트롤바);
}
```

---

## 🎉 완료

모든 높이가 정확하게 계산되어 **스크롤이 완전히 제거**되었습니다!

**구조**:
- Container: `100vh` (고정)
- Header: `64px` (고정)
- MainContent: `calc(100vh - 144px)` (자동)
- ControlBar: `80px` (fixed)

**결과**: 정확히 `100vh` = 스크롤 없음 ✅

---

**작성자**: AI Assistant (Claude)  
**작업 시간**: 5분  
**상태**: 스크롤 완전 제거 ✅

