# Slide 11: 시스템 아키텍처

---

## 🎯 슬라이드 목표
- 전체 시스템 구조를 다이어그램으로 전달

---

## 📐 실제 PPT 레이아웃 (16:9)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   🏗️ 시스템 아키텍처                                ← 44pt │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                                             │
│                    ┌───────────────────┐                    │
│                    │   🖥️ Client       │                    │
│                    │  React + Next.js  │                    │
│                    └─────────┬─────────┘                    │
│                              │                              │
│              ┌───────────────┴───────────────┐              │
│              │ HTTPS                WebSocket│              │
│              ▼                               ▼              │
│   ┌─────────────────┐             ┌─────────────────┐      │
│   │  🌐 Next.js App │             │  📡 Signaling   │      │
│   │    :3000        │             │     Server      │      │
│   │                 │             │     :4000       │      │
│   │  • API Routes   │             │  • Socket.IO    │      │
│   │  • NextAuth     │             │  • WebRTC       │      │
│   └────────┬────────┘             └────────┬────────┘      │
│            │                               │                │
│            ▼                               ▼                │
│   ┌─────────────────┐             ┌─────────────────┐      │
│   │  🐘 PostgreSQL  │             │  🔴 Redis       │      │
│   │      :5432      │             │     :6379       │      │
│   └─────────────────┘             └─────────────────┘      │
│                                                             │
│   ────────────────── 🐳 Docker Compose ──────────────────   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 들어가는 내용

**다이어그램 요소 (6개)**

| 요소 | 역할 | 포트 |
|------|------|:----:|
| Client | React + Next.js UI | - |
| Next.js App | API, 인증, DB 접근 | 3000 |
| Signaling Server | Socket.IO, WebRTC 시그널링 | 4000 |
| PostgreSQL | 주 데이터베이스 | 5432 |
| Redis | Pub/Sub, 메시지 동기화 | 6379 |
| Docker Compose | 전체 오케스트레이션 | - |

**핵심 포인트**
- 분리형 아키텍처: HTTP API와 WebSocket 서버 독립
- Redis로 다중 서버 동기화

---

⏱️ **발표 시간**: 30-35초

💬 **스크립트**:
> "시스템 아키텍처입니다.
> 클라이언트는 Next.js 앱과 HTTPS로 API 통신하고,
> 시그널링 서버와 WebSocket으로 실시간 통신합니다.
> 이렇게 분리해서 각각 독립적으로 확장할 수 있고,
> Redis Pub/Sub로 다중 서버 간 메시지를 동기화합니다.
> 전체는 Docker Compose로 관리합니다."

