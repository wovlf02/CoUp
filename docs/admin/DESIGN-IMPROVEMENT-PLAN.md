# 🎨 관리자 페이지 디자인 개선 설계 문서

**작성일**: 2025-11-29  
**목표**: 관리자 페이지 UI/UX 현대화 및 사용성 개선

---

## 📋 목차

1. [현황 분석](#현황-분석)
2. [디자인 개선 목표](#디자인-개선-목표)
3. [디자인 시스템](#디자인-시스템)
4. [페이지별 개선 계획](#페이지별-개선-계획)
5. [컴포넌트 개선 계획](#컴포넌트-개선-계획)
6. [구현 우선순위](#구현-우선순위)
7. [기술 스택](#기술-스택)

---

## 🔍 현황 분석

### 현재 관리자 페이지 구조

```
admin/
├── page.jsx                    // 대시보드
├── layout.jsx                  // 공통 레이아웃
├── users/                      // 사용자 관리
│   ├── page.jsx
│   └── [userId]/page.jsx
├── studies/                    // 스터디 관리
│   ├── page.jsx
│   └── [studyId]/page.jsx
├── reports/                    // 신고 관리
│   ├── page.jsx
│   └── [reportId]/page.jsx
├── analytics/                  // 분석
│   └── page.jsx
├── settings/                   // 설정
│   └── page.jsx
├── audit-logs/                 // 감사 로그
│   └── page.jsx
└── _components/                // 공통 컴포넌트
    ├── StatsCards.jsx
    ├── RecentActivity.jsx
    └── QuickActions.jsx
```

### 기존 컴포넌트

```
components/admin/
├── common/
│   ├── AdminNavbar.jsx        // 네비게이션 바
│   └── Breadcrumb.jsx         // 경로 표시
└── ui/
    ├── Badge.jsx              // 뱃지
    ├── Button.jsx             // 버튼
    └── Modal.jsx              // 모달
```

### 현재 문제점

#### 1. **일관성 부족**
- ❌ 페이지마다 다른 스타일 적용
- ❌ 버튼/입력 필드 스타일 불일치
- ❌ 여백/간격 불규칙

#### 2. **시각적 계층 부족**
- ❌ 중요 정보가 눈에 띄지 않음
- ❌ 카드/섹션 구분이 명확하지 않음
- ❌ 색상 사용이 산만함

#### 3. **사용성 문제**
- ❌ 버튼이 작고 클릭하기 어려움
- ❌ 로딩 상태 표시 불충분
- ❌ 에러 메시지가 명확하지 않음
- ❌ 모바일 반응형 미흡

#### 4. **접근성 문제**
- ❌ 대비 비율 낮음
- ❌ 키보드 네비게이션 불완전
- ❌ ARIA 속성 부족

---

## 🎯 디자인 개선 목표

### 1. **일관된 디자인 시스템 구축**
- ✅ 통일된 색상 팔레트
- ✅ 일관된 타이포그래피
- ✅ 표준화된 간격 시스템
- ✅ 재사용 가능한 컴포넌트

### 2. **시각적 계층 강화**
- ✅ 명확한 정보 구조
- ✅ 카드 기반 레이아웃
- ✅ 적절한 그림자/경계선
- ✅ 색상으로 중요도 표현

### 3. **사용성 개선**
- ✅ 큰 클릭 영역
- ✅ 즉각적인 피드백
- ✅ 로딩/에러 상태 명확히
- ✅ 모바일 최적화

### 4. **접근성 향상**
- ✅ WCAG 2.1 AA 준수
- ✅ 키보드 네비게이션 완전 지원
- ✅ 스크린 리더 지원
- ✅ 충분한 대비 비율

---

## 🎨 디자인 시스템

### 색상 팔레트

#### Primary Colors (브랜드 색상)
```css
--primary-50: #f0f9ff;    /* 배경 */
--primary-100: #e0f2fe;   /* hover 배경 */
--primary-500: #3b82f6;   /* 기본 */
--primary-600: #2563eb;   /* hover */
--primary-700: #1d4ed8;   /* active */
```

#### Semantic Colors (의미 색상)
```css
/* Success */
--success-50: #f0fdf4;
--success-500: #22c55e;
--success-700: #15803d;

/* Warning */
--warning-50: #fffbeb;
--warning-500: #f59e0b;
--warning-700: #b45309;

/* Danger */
--danger-50: #fef2f2;
--danger-500: #ef4444;
--danger-700: #b91c1c;

/* Info */
--info-50: #f0f9ff;
--info-500: #3b82f6;
--info-700: #1d4ed8;
```

#### Neutral Colors (회색조)
```css
--gray-50: #f9fafb;       /* 배경 */
--gray-100: #f3f4f6;      /* 카드 배경 */
--gray-200: #e5e7eb;      /* 경계선 */
--gray-300: #d1d5db;      /* 비활성 */
--gray-400: #9ca3af;      /* placeholder */
--gray-500: #6b7280;      /* 보조 텍스트 */
--gray-600: #4b5563;      /* 기본 텍스트 */
--gray-700: #374151;      /* 진한 텍스트 */
--gray-800: #1f2937;      /* 제목 */
--gray-900: #111827;      /* 강조 제목 */
```

### 타이포그래피

```css
/* Display - 대형 제목 */
--font-display: 48px/1.2 'Pretendard', sans-serif; font-weight: 700;

/* Heading - 제목 */
--font-h1: 36px/1.2 'Pretendard', sans-serif; font-weight: 700;
--font-h2: 30px/1.3 'Pretendard', sans-serif; font-weight: 700;
--font-h3: 24px/1.4 'Pretendard', sans-serif; font-weight: 600;
--font-h4: 20px/1.5 'Pretendard', sans-serif; font-weight: 600;
--font-h5: 18px/1.5 'Pretendard', sans-serif; font-weight: 600;

/* Body - 본문 */
--font-body-lg: 18px/1.6 'Pretendard', sans-serif; font-weight: 400;
--font-body: 16px/1.6 'Pretendard', sans-serif; font-weight: 400;
--font-body-sm: 14px/1.5 'Pretendard', sans-serif; font-weight: 400;
--font-body-xs: 12px/1.4 'Pretendard', sans-serif; font-weight: 400;

/* Label - 라벨 */
--font-label: 14px/1.4 'Pretendard', sans-serif; font-weight: 500;
--font-label-sm: 12px/1.3 'Pretendard', sans-serif; font-weight: 500;
```

### 간격 시스템

```css
/* Spacing Scale (4px base) */
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
--space-16: 64px;
--space-20: 80px;
--space-24: 96px;
```

### 그림자

```css
/* Shadows */
--shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
--shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
--shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
--shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
--shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
--shadow-2xl: 0 25px 50px -12px rgb(0 0 0 / 0.25);
```

### 경계선

```css
/* Border Radius */
--radius-sm: 4px;
--radius: 8px;
--radius-md: 12px;
--radius-lg: 16px;
--radius-xl: 24px;
--radius-full: 9999px;

/* Border Width */
--border-1: 1px;
--border-2: 2px;
--border-4: 4px;
```

### 애니메이션

```css
/* Transitions */
--transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
--transition-base: 250ms cubic-bezier(0.4, 0, 0.2, 1);
--transition-slow: 350ms cubic-bezier(0.4, 0, 0.2, 1);

/* Easing */
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

---

## 📄 페이지별 개선 계획

### 1. 대시보드 (`/admin`)

#### 현재 상태
- 기본적인 통계 카드
- 최근 활동 목록
- 빠른 작업 버튼

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  📊 대시보드                               │
│  플랫폼 현황을 한눈에 확인하세요            │
└─────────────────────────────────────────┘

┌────────┬────────┬────────┬────────┐
│ 사용자  │ 스터디  │ 신고    │ 활동   │
│ 1,234  │  567   │   12   │  89%  │
│ +5.2%  │ +8.1%  │  -3.2% │ +2.1% │
└────────┴────────┴────────┴────────┘

┌─────────────────────┬─────────────────┐
│ 📈 활동 추이          │ 🔔 최근 활동      │
│                     │                 │
│  [그래프]            │ • 신규 사용자     │
│                     │ • 신고 접수       │
│                     │ • 스터디 생성     │
└─────────────────────┴─────────────────┘

┌───────────────────────────────────────┐
│ ⚡ 빠른 작업                            │
│  [사용자 추가] [공지 작성] [설정]       │
└───────────────────────────────────────┘
```

**기능**
- ✅ 실시간 업데이트 (polling/websocket)
- ✅ 통계 카드 애니메이션
- ✅ 그래프 인터랙션
- ✅ 필터링 (날짜 범위)
- ✅ 다운로드/내보내기

**컴포넌트**
- `StatsCard` - 개선된 통계 카드
- `TrendChart` - 추이 그래프
- `ActivityFeed` - 활동 피드
- `QuickActionGrid` - 빠른 작업 그리드

---

### 2. 사용자 관리 (`/admin/users`)

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  👥 사용자 관리                            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 🔍 [검색]  [필터▼]  [상태▼]      [내보내기] │
└─────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ 📊 1,234명 (전체 2,000명 중)              │
└──────────────────────────────────────────┘

┌────┬────────┬─────────┬──────┬────────┐
│ ✓  │ 이름    │ 이메일   │ 상태  │ 작업    │
├────┼────────┼─────────┼──────┼────────┤
│ □  │ 홍길동  │ hong@   │ 활성  │ [•••]  │
│ □  │ 김철수  │ kim@    │ 활성  │ [•••]  │
└────┴────────┴─────────┴──────┴────────┘

┌─────────────────────────────────────────┐
│        [1] 2 3 4 5 ... 10 [다음]         │
└─────────────────────────────────────────┘
```

**기능**
- ✅ 실시간 검색 (debounce)
- ✅ 고급 필터 (다중 선택)
- ✅ 일괄 작업 (선택 → 액션)
- ✅ 정렬 (클릭)
- ✅ 페이지네이션
- ✅ 사용자 상세 모달
- ✅ 빠른 편집

**컴포넌트**
- `SearchBar` - 검색 바
- `FilterPanel` - 필터 패널
- `UserTable` - 사용자 테이블
- `UserRow` - 사용자 행
- `UserModal` - 사용자 상세 모달
- `BulkActions` - 일괄 작업 툴바

---

### 3. 스터디 관리 (`/admin/studies`)

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  📚 스터디 관리                            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ [카드뷰 ⬜] [리스트뷰 ☰]  [그리드▼] [필터▼]│
└─────────────────────────────────────────┘

┌──────────┬──────────┬──────────┬────────┐
│ [이미지]  │ [이미지]  │ [이미지]  │[이미지]│
│ React    │ Next.js  │ TypeScript│Node.js│
│ 스터디    │ 스터디    │ 스터디     │스터디  │
│ 5/10명   │ 8/12명   │ 3/8명     │10/10명│
│ 🟢 진행중 │ 🟢 진행중 │ 🟡 대기중 │🔴 완료 │
└──────────┴──────────┴──────────┴────────┘
```

**기능**
- ✅ 뷰 전환 (카드/리스트/그리드)
- ✅ 드래그 정렬
- ✅ 상태별 필터
- ✅ 빠른 승인/거부
- ✅ 스터디 카드 미리보기
- ✅ 통계 오버레이

**컴포넌트**
- `ViewToggle` - 뷰 전환 버튼
- `StudyCard` - 스터디 카드
- `StudyList` - 스터디 리스트
- `StudyGrid` - 스터디 그리드
- `StatusBadge` - 상태 뱃지

---

### 4. 신고 관리 (`/admin/reports`)

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  🚨 신고 관리                             │
│  ⚠️ 긴급: 2건  🔔 미처리: 12건            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ [전체▼] [긴급] [미처리] [처리중] [완료]   │
└─────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 🔴 [긴급] 부적절한 콘텐츠              ✕ │
│ 신고자: 홍길동  |  대상: 게시글 #123      │
│ 2025-11-29 14:30                        │
│ [상세보기] [빠른 처리]                   │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 🟡 [일반] 스팸 행위                    ✕ │
│ 신고자: 김철수  |  대상: 댓글 #456        │
│ 2025-11-29 12:15                        │
│ [상세보기] [빠른 처리]                   │
└────────────────────────────────────────┘
```

**기능**
- ✅ 우선순위 표시 (색상)
- ✅ 실시간 알림
- ✅ 빠른 처리 (드롭다운)
- ✅ 담당자 배정
- ✅ 신고 히스토리
- ✅ 자동 필터링

**컴포넌트**
- `ReportCard` - 신고 카드
- `PriorityBadge` - 우선순위 뱃지
- `QuickAction` - 빠른 처리 드롭다운
- `ReportDetail` - 신고 상세 모달
- `AssignModal` - 담당자 배정 모달

---

### 5. 분석 (`/admin/analytics`)

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  📊 분석                                  │
│  [오늘] [7일] [30일] [사용자 정의]        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 📈 사용자 증가 추이                       │
│  [라인 차트]                              │
└─────────────────────────────────────────┘

┌──────────────────┬──────────────────────┐
│ 🔵 활동 분포      │ 🟢 스터디 카테고리     │
│  [파이 차트]      │  [바 차트]            │
└──────────────────┴──────────────────────┘

┌─────────────────────────────────────────┐
│ 📋 상세 데이터                            │
│  [테이블]                                │
└─────────────────────────────────────────┘
```

**기능**
- ✅ 인터랙티브 차트 (recharts)
- ✅ 기간 선택
- ✅ 다운로드/내보내기
- ✅ 실시간 업데이트
- ✅ 비교 모드
- ✅ 커스텀 지표

**컴포넌트**
- `DateRangePicker` - 날짜 범위 선택
- `LineChart` - 라인 차트
- `PieChart` - 파이 차트
- `BarChart` - 바 차트
- `DataTable` - 데이터 테이블
- `ExportButton` - 내보내기 버튼

---

### 6. 설정 (`/admin/settings`)

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  ⚙️ 설정                                  │
└─────────────────────────────────────────┘

┌────────┬────────────────────────────────┐
│ 일반   │ ┌────────────────────────────┐ │
│ 보안   │ │ 사이트 이름                 │ │
│ 이메일 │ │ [CoUp]                     │ │
│ 알림   │ │                            │ │
│ 백업   │ │ 사이트 설명                 │ │
│        │ │ [협업 플랫폼...]            │ │
│        │ │                            │ │
│        │ │ [저장] [취소]               │ │
│        │ └────────────────────────────┘ │
└────────┴────────────────────────────────┘
```

**기능**
- ✅ 탭/사이드바 네비게이션
- ✅ 실시간 저장
- ✅ 변경 사항 추적
- ✅ 되돌리기
- ✅ 미리보기
- ✅ 설정 히스토리

**컴포넌트**
- `SettingsSidebar` - 설정 사이드바
- `SettingsPanel` - 설정 패널
- `SaveBar` - 저장 바 (sticky)
- `HistoryModal` - 히스토리 모달
- `PreviewModal` - 미리보기 모달

---

### 7. 감사 로그 (`/admin/audit-logs`)

#### 개선 사항

**레이아웃**
```
┌─────────────────────────────────────────┐
│  📋 감사 로그                             │
│  모든 관리자 활동을 추적합니다             │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ [날짜▼] [관리자▼] [액션▼] [리소스▼]       │
└─────────────────────────────────────────┘

┌────┬──────┬────────┬─────────┬─────────┐
│시간 │관리자 │액션     │리소스    │결과     │
├────┼──────┼────────┼─────────┼─────────┤
│14:30│홍길동│사용자   │user-123 │✓ 성공   │
│     │     │정지     │         │         │
├────┼──────┼────────┼─────────┼─────────┤
│14:25│김철수│신고     │report-45│✓ 성공   │
│     │     │승인     │         │         │
└────┴──────┴────────┴─────────┴─────────┘
```

**기능**
- ✅ 고급 필터
- ✅ 전체 검색
- ✅ 타임라인 뷰
- ✅ 상세 정보 모달
- ✅ 내보내기 (CSV/JSON)
- ✅ 실시간 업데이트

**컴포넌트**
- `LogTable` - 로그 테이블
- `LogRow` - 로그 행
- `LogDetail` - 로그 상세 모달
- `TimelineView` - 타임라인 뷰
- `FilterPanel` - 필터 패널

---

## 🧩 컴포넌트 개선 계획

### 공통 UI 컴포넌트

#### 1. Button
```jsx
<Button variant="primary" size="md" loading disabled>
  저장하기
</Button>

// Variants: primary, secondary, outline, ghost, danger
// Sizes: xs, sm, md, lg, xl
// States: loading, disabled, active
```

#### 2. Input
```jsx
<Input
  label="이메일"
  placeholder="email@example.com"
  error="유효하지 않은 이메일입니다"
  helperText="회사 이메일을 입력하세요"
  leftIcon={<EmailIcon />}
  rightIcon={<CheckIcon />}
/>
```

#### 3. Select
```jsx
<Select
  label="상태"
  options={[
    { value: 'active', label: '활성' },
    { value: 'inactive', label: '비활성' }
  ]}
  multiple
  searchable
/>
```

#### 4. Modal
```jsx
<Modal
  isOpen={isOpen}
  onClose={onClose}
  title="사용자 편집"
  size="lg"
  footer={
    <>
      <Button variant="outline">취소</Button>
      <Button variant="primary">저장</Button>
    </>
  }
>
  내용...
</Modal>
```

#### 5. Toast
```jsx
toast.success('저장되었습니다')
toast.error('오류가 발생했습니다')
toast.warning('경고 메시지')
toast.info('정보 메시지')

// Position: top-left, top-center, top-right, bottom-left, bottom-center, bottom-right
// Duration: auto, custom
// Action: 버튼 추가 가능
```

#### 6. Card
```jsx
<Card variant="elevated" hoverable>
  <CardHeader>
    <CardTitle>제목</CardTitle>
    <CardActions>
      <IconButton icon={<MoreIcon />} />
    </CardActions>
  </CardHeader>
  <CardContent>
    내용...
  </CardContent>
  <CardFooter>
    <Button>액션</Button>
  </CardFooter>
</Card>
```

#### 7. Table
```jsx
<Table
  columns={columns}
  data={data}
  sortable
  selectable
  pagination
  loading
  onSort={handleSort}
  onSelectRows={handleSelect}
/>
```

#### 8. Dropdown
```jsx
<Dropdown
  trigger={<Button>액션</Button>}
  items={[
    { label: '편집', icon: <EditIcon />, onClick: handleEdit },
    { label: '삭제', icon: <DeleteIcon />, onClick: handleDelete, danger: true }
  ]}
/>
```

#### 9. Tabs
```jsx
<Tabs defaultValue="general">
  <TabsList>
    <TabsTrigger value="general">일반</TabsTrigger>
    <TabsTrigger value="security">보안</TabsTrigger>
  </TabsList>
  <TabsContent value="general">일반 설정</TabsContent>
  <TabsContent value="security">보안 설정</TabsContent>
</Tabs>
```

#### 10. Badge
```jsx
<Badge variant="success" size="md" dot>
  활성
</Badge>

// Variants: default, primary, success, warning, danger, info
// Sizes: sm, md, lg
// Props: dot, removable
```

---

## 📊 구현 우선순위

### Phase 1: 디자인 시스템 구축 (1-2일)
**목표**: 재사용 가능한 기반 마련

1. ✅ **CSS 변수 정의** (`globals.css`)
   - 색상 팔레트
   - 타이포그래피
   - 간격 시스템
   - 그림자/경계선
   - 애니메이션

2. ✅ **기본 UI 컴포넌트**
   - Button
   - Input
   - Select
   - Checkbox/Radio
   - Badge

3. ✅ **레이아웃 컴포넌트**
   - Card
   - Container
   - Grid
   - Stack

---

### Phase 2: 공통 컴포넌트 개선 (1-2일)
**목표**: 관리자 전용 컴포넌트 업그레이드

1. ✅ **Navigation**
   - AdminNavbar 개선
   - Breadcrumb 개선
   - Sidebar (모바일)

2. ✅ **Data Display**
   - Table
   - Card
   - List
   - Stats

3. ✅ **Feedback**
   - Modal
   - Toast
   - Alert
   - Loading

---

### Phase 3: 주요 페이지 개선 (2-3일)
**목표**: 핵심 기능 페이지 현대화

1. ✅ **대시보드**
   - StatsCards 개선
   - 차트 추가
   - 활동 피드

2. ✅ **사용자 관리**
   - 테이블 개선
   - 필터 강화
   - 일괄 작업

3. ✅ **스터디 관리**
   - 카드 뷰
   - 리스트 뷰
   - 그리드 뷰

---

### Phase 4: 부가 기능 개선 (1-2일)
**목표**: 나머지 페이지 완성

1. ✅ **신고 관리**
   - 우선순위 표시
   - 빠른 처리

2. ✅ **분석**
   - 차트 개선
   - 필터 추가

3. ✅ **설정/감사로그**
   - 탭 구조
   - 히스토리

---

### Phase 5: 반응형 & 접근성 (1일)
**목표**: 모바일 최적화 및 접근성 향상

1. ✅ **반응형**
   - 모바일 레이아웃
   - 터치 최적화

2. ✅ **접근성**
   - ARIA 속성
   - 키보드 네비게이션
   - 대비 비율

---

### Phase 6: 최종 검수 & 문서화 (1일)
**목표**: 품질 보증 및 인수인계

1. ✅ **테스트**
   - 시각적 테스트
   - 기능 테스트
   - 성능 테스트

2. ✅ **문서화**
   - 컴포넌트 문서
   - 스타일 가이드
   - 사용 예시

---

## 🛠 기술 스택

### UI 라이브러리
- **Headless UI**: 접근성 기반 컴포넌트
- **Radix UI**: 프리미티브 컴포넌트
- **Framer Motion**: 애니메이션

### 차트
- **Recharts**: React 차트 라이브러리
- **Chart.js**: 유연한 차트

### 유틸리티
- **clsx**: 클래스 네임 유틸리티
- **date-fns**: 날짜 포맷팅
- **react-hot-toast**: 토스트 알림

### 아이콘
- **Heroicons**: Tailwind 아이콘
- **Lucide React**: 모던 아이콘

---

## 📏 개발 가이드라인

### 코드 스타일

#### 1. 컴포넌트 구조
```jsx
'use client' // if needed

import { useState } from 'react'
import styles from './Component.module.css'

/**
 * 컴포넌트 설명
 * 
 * @param {Object} props
 * @param {string} props.title - 제목
 */
export default function Component({ title }) {
  // 1. Hooks
  const [state, setState] = useState()

  // 2. Handlers
  const handleClick = () => {}

  // 3. Effects
  useEffect(() => {}, [])

  // 4. Render helpers
  const renderContent = () => {}

  // 5. Main render
  return (
    <div className={styles.container}>
      {/* Content */}
    </div>
  )
}
```

#### 2. CSS 모듈 네이밍
```css
/* BEM 스타일 */
.component { }
.component--variant { }
.component__element { }
.component__element--modifier { }

/* 상태 */
.component.is-active { }
.component.is-disabled { }
.component.has-error { }
```

#### 3. Props 타입
```jsx
Component.propTypes = {
  variant: PropTypes.oneOf(['primary', 'secondary']),
  size: PropTypes.oneOf(['sm', 'md', 'lg']),
  disabled: PropTypes.bool,
  onClick: PropTypes.func
}

Component.defaultProps = {
  variant: 'primary',
  size: 'md',
  disabled: false
}
```

---

## ✅ 완료 체크리스트

### Phase 1: 디자인 시스템
- [ ] CSS 변수 정의
- [ ] Button 컴포넌트
- [ ] Input 컴포넌트
- [ ] Select 컴포넌트
- [ ] Badge 컴포넌트
- [ ] Card 컴포넌트

### Phase 2: 공통 컴포넌트
- [ ] AdminNavbar 개선
- [ ] Breadcrumb 개선
- [ ] Table 컴포넌트
- [ ] Modal 컴포넌트
- [ ] Toast 시스템
- [ ] Dropdown 컴포넌트

### Phase 3: 주요 페이지
- [ ] 대시보드 개선
- [ ] 사용자 관리 개선
- [ ] 스터디 관리 개선

### Phase 4: 부가 기능
- [ ] 신고 관리 개선
- [ ] 분석 페이지 개선
- [ ] 설정 페이지 개선
- [ ] 감사 로그 개선

### Phase 5: 반응형 & 접근성
- [ ] 모바일 레이아웃
- [ ] 키보드 네비게이션
- [ ] ARIA 속성
- [ ] 대비 비율 검증

### Phase 6: 최종 검수
- [ ] 시각적 회귀 테스트
- [ ] 기능 테스트
- [ ] 성능 측정
- [ ] 문서화 완료

---

**작성일**: 2025-11-29  
**작성자**: GitHub Copilot  
**예상 소요 시간**: 8-10일

