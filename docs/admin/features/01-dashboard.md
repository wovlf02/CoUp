# 관리자 대시보드 기능 명세

> **작성일**: 2025-11-26  
> **영역**: 대시보드 및 통계  
> **목적**: 관리자가 플랫폼 현황을 한눈에 파악할 수 있는 대시보드

---

## 📋 목차

1. [개요](#1-개요)
2. [핵심 지표 카드](#2-핵심-지표-카드)
3. [최근 활동](#3-최근-활동)
4. [통계 그래프](#4-통계-그래프)
5. [긴급 알림](#5-긴급-알림)
6. [빠른 액션](#6-빠른-액션)

---

## 1. 개요

### 1.1 화면 구성
```
┌─────────────────────────────────────────────────────────┐
│ 관리자 대시보드                            [새로고침]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│ │ 신규     │ │ 활성     │ │ 미처리  │ │ 정지     │       │
│ │ 가입자   │ │ 사용자   │ │ 신고    │ │ 사용자   │       │
│ │   45     │ │  1,234   │ │    8    │ │    3     │       │
│ │ +12%     │ │  -3%     │ │  ⚠️     │ │  +1      │       │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
│                                                         │
│ ┌───────────────────────┐ ┌─────────────────────────┐ │
│ │ 📊 가입자 추이 (7일)   │ │ ⚠️ 긴급 알림            │ │
│ │                       │ │                         │ │
│ │    [그래프]           │ │ • 욕설 신고 3건 대기    │ │
│ │                       │ │ • 스팸 계정 탐지 5건    │ │
│ └───────────────────────┘ └─────────────────────────┘ │
│                                                         │
│ 최근 관리 활동                                          │
│ • [10분 전] admin1 - 사용자 user123 1일 정지           │
│ • [1시간 전] admin2 - 신고 #456 처리 완료               │
│ • [2시간 전] admin1 - 스터디 "스팸" 종료                │
└─────────────────────────────────────────────────────────┘
```

### 1.2 권한
- **ADMIN**: ✅ 조회 가능
- **SYSTEM_ADMIN**: ✅ 조회 + 상세 설정

### 1.3 자동 갱신
- 기본: 1분마다 자동 갱신
- 수동: [새로고침] 버튼 클릭

---

## 2. 핵심 지표 카드

### 2.1 신규 가입자 (오늘)
**표시 정보**:
- 오늘 가입한 사용자 수
- 전일 대비 증감률 (%, ↑↓)
- 색상: 파란색 (#3B82F6)

**데이터 소스**:
```sql
SELECT COUNT(*) 
FROM User 
WHERE DATE(createdAt) = CURDATE()
```

**클릭 시**: 사용자 관리 페이지 (필터: 오늘 가입)

### 2.2 활성 사용자 (DAU)
**표시 정보**:
- 오늘 활동한 사용자 수
- 전일 대비 증감률
- 색상: 초록색 (#10B981)

**데이터 소스**:
```sql
SELECT COUNT(DISTINCT userId) 
FROM UserActivity 
WHERE DATE(activityAt) = CURDATE()
```

**활동 기준**:
- 로그인
- 채팅 메시지 전송
- 공지/파일 작성
- 스터디 가입

**클릭 시**: 사용자 활동 통계

### 2.3 미처리 신고
**표시 정보**:
- 처리 대기 중인 신고 수
- 긴급 신고 수 (⚠️ 표시)
- 색상: 빨간색 (#EF4444) - 긴급 있을 때

**데이터 소스**:
```sql
SELECT COUNT(*) 
FROM Report 
WHERE status = 'PENDING'
```

**긴급 조건**:
- 우선순위: URGENT
- 접수 후 24시간 경과
- 동일 대상 중복 신고 (3건 이상)

**클릭 시**: 신고 관리 페이지 (필터: 미처리)

### 2.4 정지 사용자
**표시 정보**:
- 현재 정지 중인 사용자 수
- 오늘 정지된 사용자 수 ('+' 표시)
- 색상: 주황색 (#F59E0B)

**데이터 소스**:
```sql
SELECT COUNT(*) 
FROM User 
WHERE status = 'SUSPENDED' 
AND (suspendedUntil IS NULL OR suspendedUntil > NOW())
```

**클릭 시**: 사용자 관리 (필터: 정지 중)

---

## 3. 최근 활동

### 3.1 표시 내용
**최근 20개 활동 표시** (최신순):
- 사용자 제재 (경고/정지/해제)
- 신고 처리
- 스터디 제재 (숨김/종료)
- 콘텐츠 삭제
- 시스템 공지 발송

### 3.2 활동 항목 형식
```
[시간] 관리자명 - 작업 내용

예시:
• [10분 전] admin1 - 사용자 user123 1일 정지 (사유: 욕설)
• [1시간 전] admin2 - 신고 #456 처리 완료 (승인)
• [2시간 전] admin1 - 스터디 "스팸 스터디" 종료
• [3시간 전] admin2 - 메시지 #789 삭제 (욕설)
```

### 3.3 데이터 소스
```sql
SELECT * FROM AdminLog 
WHERE action IN (
  'USER_SUSPEND', 'USER_WARN', 'USER_UNSUSPEND',
  'REPORT_PROCESS', 'STUDY_CLOSE', 'CONTENT_DELETE',
  'SYSTEM_NOTICE'
)
ORDER BY createdAt DESC 
LIMIT 20
```

### 3.4 클릭 시
- 해당 대상 상세 페이지로 이동
- 예: 사용자 제재 → 사용자 상세

---

## 4. 통계 그래프

### 4.1 가입자 추이 (7일)
**그래프 유형**: 선 그래프 (Line Chart)

**데이터**:
- X축: 날짜 (최근 7일)
- Y축: 가입자 수
- 색상: 파란색 (#3B82F6)

**쿼리**:
```sql
SELECT 
  DATE(createdAt) as date,
  COUNT(*) as count
FROM User
WHERE createdAt >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY DATE(createdAt)
ORDER BY date ASC
```

### 4.2 활성 사용자 추이 (7일)
**그래프 유형**: 선 그래프

**데이터**:
- X축: 날짜
- Y축: DAU (일간 활성 사용자)
- 색상: 초록색 (#10B981)

### 4.3 신고 유형별 분포 (이번 달)
**그래프 유형**: 도넛 차트 (Doughnut Chart)

**데이터**:
- 사용자 신고 (노란색)
- 스터디 신고 (파란색)
- 콘텐츠 신고 (빨간색)
- 기타 (회색)

**쿼리**:
```sql
SELECT 
  reportType,
  COUNT(*) as count
FROM Report
WHERE MONTH(createdAt) = MONTH(NOW())
GROUP BY reportType
```

### 4.4 스터디 생성 추이 (30일)
**그래프 유형**: 막대 그래프 (Bar Chart)

**데이터**:
- X축: 날짜 (최근 30일)
- Y축: 생성된 스터디 수
- 색상: 보라색 (#8B5CF6)

---

## 5. 긴급 알림

### 5.1 알림 조건

#### 1) 미처리 신고 급증
- 조건: 미처리 신고 10건 이상
- 메시지: "미처리 신고 {count}건 대기 중"
- 색상: 빨간색

#### 2) 욕설/혐오 신고
- 조건: 욕설/혐오 신고 3건 이상 대기
- 메시지: "욕설 신고 {count}건 대기"
- 색상: 빨간색

#### 3) 스팸 계정 탐지
- 조건: AI 자동 탐지로 의심 계정 5개 이상
- 메시지: "스팸 계정 탐지 {count}건"
- 색상: 주황색

#### 4) 시스템 에러 급증
- 조건: 최근 1시간 에러 100건 이상
- 메시지: "시스템 에러 급증 ({count}건)"
- 색상: 빨간색

#### 5) 정지 사용자 급증
- 조건: 오늘 정지된 사용자 10명 이상
- 메시지: "오늘 정지 사용자 {count}명"
- 색상: 주황색

### 5.2 알림 표시
```
┌─────────────────────────────────┐
│ ⚠️ 긴급 알림                     │
├─────────────────────────────────┤
│ • 미처리 신고 15건 대기 중       │
│   [지금 확인하기]               │
│                                 │
│ • 욕설 신고 5건 대기             │
│   [지금 확인하기]               │
│                                 │
│ • 스팸 계정 탐지 7건             │
│   [지금 확인하기]               │
└─────────────────────────────────┘
```

### 5.3 알림 액션
- [지금 확인하기]: 해당 관리 페이지로 이동
- 알림 무시 (SYSTEM_ADMIN만)

---

## 6. 빠른 액션

### 6.1 버튼 목록
```
┌─────────────────────────────────┐
│ ⚡ 빠른 액션                      │
├─────────────────────────────────┤
│ [미처리 신고 확인]               │
│ [사용자 검색]                    │
│ [스터디 검색]                    │
│ [시스템 공지 발송]               │
└─────────────────────────────────┘
```

### 6.2 기능

#### 1) 미처리 신고 확인
- 이동: `/admin/reports?status=pending`
- 미처리 신고 목록 표시

#### 2) 사용자 검색
- 모달 팝업: 사용자 ID/이메일/이름 검색
- 결과: 사용자 상세 페이지 이동

#### 3) 스터디 검색
- 모달 팝업: 스터디 ID/이름 검색
- 결과: 스터디 상세 페이지 이동

#### 4) 시스템 공지 발송
- 모달 팝업: 공지 작성 폼
- 발송 대상 선택
- 즉시/예약 발송

---

## 7. 데이터 구조

### 7.1 DashboardStats
```typescript
interface DashboardStats {
  // 핵심 지표
  newUsers: {
    today: number
    changeRate: number  // 전일 대비 %
  }
  activeUsers: {
    today: number
    changeRate: number
  }
  pendingReports: {
    total: number
    urgent: number
  }
  suspendedUsers: {
    total: number
    todaySuspended: number
  }
  
  // 그래프 데이터
  userTrend: Array<{
    date: string
    count: number
  }>
  reportDistribution: Array<{
    type: string
    count: number
  }>
  studyTrend: Array<{
    date: string
    count: number
  }>
  
  // 긴급 알림
  alerts: Array<{
    type: string
    message: string
    count: number
    severity: 'CRITICAL' | 'WARNING'
    link: string
  }>
  
  // 최근 활동
  recentActivities: Array<{
    id: string
    adminId: string
    adminName: string
    action: string
    target: string
    description: string
    createdAt: Date
  }>
}
```

---

## 8. API 연동

### 8.1 대시보드 데이터 조회
```
GET /api/admin/dashboard
```

**응답**:
```json
{
  "success": true,
  "data": {
    "newUsers": {
      "today": 45,
      "changeRate": 12.5
    },
    "activeUsers": {
      "today": 1234,
      "changeRate": -3.2
    },
    "pendingReports": {
      "total": 8,
      "urgent": 3
    },
    "suspendedUsers": {
      "total": 3,
      "todaySuspended": 1
    },
    "userTrend": [
      { "date": "2025-11-20", "count": 38 },
      { "date": "2025-11-21", "count": 42 },
      // ...
    ],
    "alerts": [
      {
        "type": "PENDING_REPORTS",
        "message": "미처리 신고 15건 대기 중",
        "count": 15,
        "severity": "CRITICAL",
        "link": "/admin/reports?status=pending"
      }
    ],
    "recentActivities": [
      {
        "id": "log_123",
        "adminId": "admin1",
        "adminName": "관리자1",
        "action": "USER_SUSPEND",
        "target": "user123",
        "description": "사용자 user123 1일 정지 (사유: 욕설)",
        "createdAt": "2025-11-26T10:30:00Z"
      }
    ]
  }
}
```

### 8.2 갱신 주기
- 자동 갱신: 1분
- React Query:
```javascript
useQuery(['adminDashboard'], fetchDashboard, {
  refetchInterval: 60000,  // 1분
  refetchOnWindowFocus: true
})
```

---

## 9. 구현 체크리스트

### Phase 1: 기본 레이아웃
- [ ] 대시보드 페이지 생성 (`/admin/dashboard`)
- [ ] 4개 핵심 지표 카드
- [ ] 반응형 그리드 레이아웃

### Phase 2: 데이터 연동
- [ ] API 엔드포인트 구현
- [ ] 통계 쿼리 최적화
- [ ] React Query 캐싱

### Phase 3: 그래프
- [ ] Recharts 또는 Chart.js 설치
- [ ] 가입자 추이 그래프
- [ ] 신고 분포 차트
- [ ] 스터디 생성 추이

### Phase 4: 알림 및 액션
- [ ] 긴급 알림 로직
- [ ] 빠른 액션 버튼
- [ ] 최근 활동 타임라인

### Phase 5: 최적화
- [ ] 자동 갱신 (1분)
- [ ] 로딩 스켈레톤
- [ ] 에러 핸들링

---

**다음 문서**: `02-user-management.md`

