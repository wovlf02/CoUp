# 화상회의 구현 Todo List (분리형 아키텍처)

> **작성일**: 2025-11-19  
> **마지막 업데이트**: 2025-11-19  
> **아키텍처**: 분리형 (Next.js Standalone + 독립 시그널링 서버)  
> **전체 진행률**: 0/95 (0%)

---

## 📊 진행 상황 요약

| Phase | 완료/전체 | 진행률 | 상태 |
|-------|----------|--------|------|
| Phase 0: 인프라 구축 | 0/12 | 0% | 🔴 대기 중 |
| Phase 1: 핵심 기능 | 0/38 | 0% | 🔴 대기 중 |
| Phase 2: 고급 기능 | 0/35 | 0% | 🔴 대기 중 |
| Phase 3: 최적화 | 0/10 | 0% | 🔴 대기 중 |
| **전체** | **0/95** | **0%** | 🔴 **대기 중** |

---

## 🏗️ Phase 0: 인프라 구축 (Day 1-2)

### 0.1 프로젝트 구조 생성

- [ ] `/signaling-server` 폴더 생성
  - [ ] `package.json` 생성
  - [ ] `server.js` 생성
  - [ ] `/handlers` 폴더 생성
    - [ ] `video.js` 생성
    - [ ] `chat.js` 생성  
    - [ ] `presence.js` 생성
  - [ ] `/middleware` 폴더 생성
    - [ ] `auth.js` 생성

### 0.2 Docker Compose 설정

- [ ] `docker-compose.yml` 루트에 생성
  - [ ] Next.js 서비스 정의
  - [ ] Signaling 서비스 정의
  - [ ] PostgreSQL 서비스 정의
  - [ ] Redis 서비스 정의
  - [ ] 볼륨 설정
  - [ ] 네트워크 설정

### 0.3 Next.js 설정 변경

- [ ] `/coup/next.config.mjs` 수정
  - [ ] `output: 'standalone'` 설정
  - [ ] 환경 변수 설정

- [ ] `/coup/package.json` 수정
  - [ ] `dev` 스크립트 수정 (server.mjs 제거)
  - [ ] `start` 스크립트 수정

- [ ] `/coup/server.mjs` 삭제 또는 백업

### 0.4 환경 변수 설정

- [ ] `/coup/.env.local` 업데이트
  ```
  NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
  DATABASE_URL=postgresql://...
  REDIS_URL=redis://localhost:6379
  ```

- [ ] `/signaling-server/.env` 생성
  ```
  PORT=4000
  NEXTJS_URL=http://localhost:3000
  REDIS_URL=redis://localhost:6379
  ALLOWED_ORIGINS=http://localhost:3000
  ```

### 0.5 로컬 환경 테스트

- [ ] Docker Compose 빌드
  ```bash
  docker-compose build
  ```

- [ ] Docker Compose 실행
  ```bash
  docker-compose up
  ```

- [ ] 서비스 접근 확인
  - [ ] Next.js: http://localhost:3000
  - [ ] Signaling: http://localhost:4000/health
  - [ ] Redis: 연결 테스트

---

## 🎯 Phase 1: 핵심 기능 안정화 (2주)

### 1.1 Socket 이벤트 통일 (Day 1)

#### 백엔드 작업

- [ ] `/coup/src/lib/socket/server.js` 수정
  - [ ] `handleVideoCallEvents` 함수 재작성
  - [ ] `video:join-room` 핸들러 구현
    - [ ] 권한 검증 (studyMember 확인)
    - [ ] 방 입장 (`socket.join`)
    - [ ] 기존 참여자 목록 전송 (`video:room-state`)
    - [ ] 다른 참여자에게 알림 (`video:user-joined`)
  - [ ] `video:offer` 핸들러 구현 (relay)
  - [ ] `video:answer` 핸들러 구현 (relay)
  - [ ] `video:ice-candidate` 핸들러 구현 (relay)
  - [ ] `video:toggle-audio` 핸들러 구현
    - [ ] 다른 참여자에게 broadcast (`video:peer-audio-changed`)
  - [ ] `video:toggle-video` 핸들러 구현
    - [ ] 다른 참여자에게 broadcast (`video:peer-video-changed`)
  - [ ] `video:screen-share-start` 핸들러 구현
    - [ ] 다른 참여자에게 알림 (`video:peer-screen-share`)
  - [ ] `video:screen-share-stop` 핸들러 구현
  - [ ] `video:leave-room` 핸들러 구현
    - [ ] 방 퇴장
    - [ ] 다른 참여자에게 알림 (`video:user-left`)

#### 프론트엔드 작업

- [ ] `/coup/src/lib/hooks/useVideoCall.js` 수정
  - [ ] Socket 이벤트 리스너 확인 및 수정
    - [ ] `video:room-state` → 기존 참여자 처리
    - [ ] `video:user-joined` → 새 참여자 처리
    - [ ] `video:offer` → Offer 수신 처리
    - [ ] `video:answer` → Answer 수신 처리
    - [ ] `video:ice-candidate` → ICE Candidate 처리
    - [ ] `video:user-left` → 참여자 퇴장 처리
    - [ ] `video:peer-audio-changed` → 상대방 음소거 상태 반영
    - [ ] `video:peer-video-changed` → 상대방 비디오 상태 반영
    - [ ] `video:peer-screen-share` → 상대방 화면 공유 상태 반영

#### 테스트

- [ ] 브라우저 콘솔에서 이벤트 로그 확인
- [ ] 2명 참여 시 이벤트 전달 확인
- [ ] 에러 이벤트 처리 확인

---

### 1.2 WebRTC 연결 검증 및 디버깅 (Day 2)

#### 로깅 강화

- [ ] `/coup/src/lib/hooks/useVideoCall.js` 로깅 추가
  - [ ] `createPeerConnection` 함수에 로깅 추가
    - [ ] 연결 상태 변경 로그
    - [ ] ICE 연결 상태 로그
    - [ ] ICE Gathering 상태 로그
    - [ ] 시그널링 상태 로그
    - [ ] ICE Candidate 생성 로그
    - [ ] 트랙 수신 로그
  - [ ] `createOffer` 함수에 로깅 추가
  - [ ] Offer/Answer 처리에 로깅 추가

#### WebRTC 흐름 검증

- [ ] Offer/Answer 교환 검증
  - [ ] Offer 생성 확인
  - [ ] Local Description 설정 확인
  - [ ] Offer 전송 확인
  - [ ] Offer 수신 확인
  - [ ] Answer 생성 확인
  - [ ] Answer 전송 확인
  - [ ] Answer 수신 및 Remote Description 설정 확인

- [ ] ICE Candidate 교환 검증
  - [ ] Candidate 생성 확인
  - [ ] Candidate 전송 확인
  - [ ] Candidate 수신 및 추가 확인

- [ ] 연결 상태 검증
  - [ ] `connecting` → `connected` 전환 확인
  - [ ] `failed` 상태 처리 확인
  - [ ] `disconnected` 상태 처리 확인

#### 테스트 시나리오

- [ ] 2명 동시 참여 테스트
  - [ ] User A: Chrome
  - [ ] User B: Chrome Incognito (다른 계정)
  - [ ] 로컬 비디오 표시 확인
  - [ ] 원격 비디오 표시 확인
  - [ ] Console 에러 없음 확인

- [ ] 순차 참여 테스트
  - [ ] User A 먼저 참여
  - [ ] User B 나중에 참여
  - [ ] 양방향 비디오 확인

- [ ] 재참여 테스트
  - [ ] User A 나가기
  - [ ] User A 다시 참여
  - [ ] 정상 연결 확인

---

### 1.3 데이터베이스 스키마 추가 (Day 3)

#### Prisma 스키마 수정

- [ ] `/coup/prisma/schema.prisma` 수정
  - [ ] `VideoCallSession` 모델 추가
    - [ ] `id` (cuid)
    - [ ] `studyId` (String)
    - [ ] `roomId` (String, unique)
    - [ ] `startedBy` (String)
    - [ ] `startedAt` (DateTime)
    - [ ] `endedAt` (DateTime?)
    - [ ] `duration` (Int?)
    - [ ] Relations 추가
    - [ ] Indexes 추가
  - [ ] `VideoCallParticipant` 모델 추가
    - [ ] `id` (cuid)
    - [ ] `sessionId` (String)
    - [ ] `userId` (String)
    - [ ] `joinedAt` (DateTime)
    - [ ] `leftAt` (DateTime?)
    - [ ] `duration` (Int?)
    - [ ] `audioMutedTime` (Int)
    - [ ] `videoOffTime` (Int)
    - [ ] `screenSharedTime` (Int)
    - [ ] Relations 추가
    - [ ] Unique constraint 추가
    - [ ] Indexes 추가
  - [ ] `User` 모델에 relation 추가
  - [ ] `Study` 모델에 relation 추가

#### 마이그레이션

- [ ] 마이그레이션 파일 생성
  ```bash
  cd coup
  npx prisma migrate dev --name add_video_call_tables
  ```

- [ ] Prisma Client 재생성
  ```bash
  npx prisma generate
  ```

- [ ] 마이그레이션 적용 확인
  ```bash
  npx prisma studio
  ```

#### Seed 데이터 (선택)

- [ ] `/coup/prisma/seed.js`에 테스트 데이터 추가
- [ ] Seed 실행
  ```bash
  npx prisma db seed
  ```

---

### 1.4 기본 REST API 구현 (Day 4-5)

#### 세션 시작 API

- [ ] `/coup/src/app/api/my-studies/[studyId]/video-call/start/route.js` 생성
  - [ ] `POST` 메서드 구현
  - [ ] 인증 확인 (getServerSession)
  - [ ] 멤버 권한 확인
  - [ ] 기존 활성 세션 확인
  - [ ] 새 세션 생성 (VideoCallSession)
  - [ ] Socket.io로 알림 전송 (`video:started`)
  - [ ] 응답 반환

#### 현재 상태 조회 API

- [ ] `/coup/src/app/api/my-studies/[studyId]/video-call/status/route.js` 생성
  - [ ] `GET` 메서드 구현
  - [ ] 인증 확인
  - [ ] 활성 세션 조회
    - [ ] `endedAt` null인 세션
    - [ ] Include starter, participants
  - [ ] Socket.io에서 실시간 참여자 수 조회
  - [ ] 응답 반환

#### 참여 기록 API

- [ ] `/coup/src/app/api/my-studies/[studyId]/video-call/join/route.js` 생성
  - [ ] `POST` 메서드 구현
  - [ ] 인증 확인
  - [ ] 멤버 권한 확인
  - [ ] 세션 유효성 확인
  - [ ] VideoCallParticipant 생성 또는 업데이트 (upsert)
  - [ ] 응답 반환

#### 퇴장 기록 API

- [ ] `/coup/src/app/api/my-studies/[studyId]/video-call/leave/route.js` 생성
  - [ ] `POST` 메서드 구현
  - [ ] 인증 확인
  - [ ] VideoCallParticipant 업데이트
    - [ ] `leftAt` 설정
    - [ ] `duration` 계산 및 저장
  - [ ] 남은 참여자 확인
  - [ ] 모두 나갔으면 세션 종료
    - [ ] VideoCallSession 업데이트
    - [ ] `endedAt` 설정
    - [ ] `duration` 계산 및 저장
  - [ ] 응답 반환

#### 프론트엔드 연동

- [ ] `/coup/src/app/my-studies/[studyId]/video-call/page.jsx` 수정
  - [ ] 페이지 로드 시 `/api/.../status` 호출
  - [ ] 활성 세션 있으면 표시
  - [ ] [참여하기] 클릭 시 `/api/.../join` 호출
  - [ ] [나가기] 클릭 시 `/api/.../leave` 호출

#### API 테스트

- [ ] Postman/Thunder Client로 API 테스트
  - [ ] `/start` 테스트
  - [ ] `/status` 테스트
  - [ ] `/join` 테스트
  - [ ] `/leave` 테스트
- [ ] 에러 케이스 테스트
  - [ ] 인증 없이 요청
  - [ ] 권한 없이 요청
  - [ ] 잘못된 데이터로 요청

---

### 1.5 통합 테스트 (Day 6-7)

#### 테스트 환경 설정

- [ ] 테스트 계정 2개 생성
- [ ] 테스트 스터디 생성
- [ ] 두 계정을 스터디 멤버로 추가

#### 정상 플로우 테스트

- [ ] User A 로그인 및 화상 탭 접근
- [ ] User A 참여하기 클릭
- [ ] DB 확인: VideoCallSession 생성
- [ ] DB 확인: VideoCallParticipant (User A) 생성
- [ ] User B 로그인 및 화상 탭 접근
- [ ] User B 화면에 진행 중 표시 확인
- [ ] User B 참여하기 클릭
- [ ] DB 확인: VideoCallParticipant (User B) 생성
- [ ] User A 화면: User B 비디오 표시 확인
- [ ] User B 화면: User A 비디오 표시 확인
- [ ] User A 음소거 클릭
- [ ] User B 화면: User A 음소거 아이콘 확인
- [ ] User A 비디오 끄기 클릭
- [ ] User B 화면: User A 프로필 이미지 확인
- [ ] User A 나가기 클릭
- [ ] DB 확인: User A leftAt 업데이트
- [ ] User B 화면: User A 비디오 사라짐
- [ ] User B 나가기 클릭
- [ ] DB 확인: User B leftAt 업데이트
- [ ] DB 확인: VideoCallSession endedAt 업데이트

#### 동시 참여 테스트

- [ ] User A와 User B 동시에 참여하기 클릭
- [ ] 두 사용자 모두 정상 연결 확인
- [ ] 비디오 서로 보임 확인

#### 에러 처리 테스트

- [ ] 비멤버가 URL로 직접 접근
  - [ ] 403 에러 확인
  - [ ] 에러 메시지 표시 확인
- [ ] 카메라/마이크 권한 거부
  - [ ] 권한 에러 확인
  - [ ] 에러 메시지 표시 확인
- [ ] 네트워크 연결 끊김 시뮬레이션
  - [ ] 연결 종료 확인
  - [ ] 에러 처리 확인

#### 화면 공유 테스트

- [ ] User A 화면 공유 시작
- [ ] User B 화면에 User A 공유 화면 표시 확인
- [ ] User A 화면 공유 중지
- [ ] User B 화면에 원래 비디오 복구 확인

#### 메모리 누수 확인

- [ ] Chrome DevTools Performance 탭
- [ ] Heap Snapshot 비교
- [ ] 참여 → 나가기 반복 후 메모리 확인

---

## 🚀 Phase 2: 고급 기능 (1-2주)

### 2.1 참여자 제어 (호스트 기능)

#### 백엔드 작업

- [ ] `/coup/src/lib/socket/server.js`에 핸들러 추가
  - [ ] `video:mute-participant` 핸들러
    - [ ] 호스트 권한 확인 (OWNER/ADMIN)
    - [ ] 대상 참여자에게 명령 전송
  - [ ] `video:unmute-participant` 핸들러
  - [ ] `video:kick-participant` 핸들러
    - [ ] 호스트 권한 확인
    - [ ] 대상 참여자 연결 강제 종료
    - [ ] 모든 참여자에게 알림

#### 프론트엔드 작업

- [ ] `/coup/src/lib/hooks/useVideoCall.js`에 기능 추가
  - [ ] `muteParticipant(socketId)` 함수
  - [ ] `kickParticipant(socketId)` 함수
  - [ ] 강제 음소거 이벤트 리스너
  - [ ] 강제 퇴장 이벤트 리스너

- [ ] 참여자 제어 UI 컴포넌트 생성
  - [ ] `/coup/src/components/video-call/ParticipantControl.jsx`
  - [ ] 참여자 목록 표시
  - [ ] 각 참여자에 제어 버튼 (OWNER/ADMIN만)
  - [ ] 음소거/강퇴 버튼

- [ ] 페이지에 컴포넌트 통합
  - [ ] 우측 사이드바 또는 모달로 표시
  - [ ] 토글 버튼 추가

#### 테스트

- [ ] 호스트가 참여자 음소거
- [ ] 호스트가 참여자 강퇴
- [ ] 일반 멤버는 버튼 안 보임
- [ ] 에러 처리 확인

---

### 2.2 에러 처리 및 재연결

#### 연결 실패 감지

- [ ] `/coup/src/lib/hooks/useVideoCall.js` 수정
  - [ ] `onconnectionstatechange` 핸들러 강화
    - [ ] `failed` 상태 처리
    - [ ] `disconnected` 상태 처리
  - [ ] `oniceconnectionstatechange` 핸들러 추가
    - [ ] `failed` 상태 감지
    - [ ] `disconnected` 상태 감지

#### 자동 재연결 로직

- [ ] `retryConnection` 함수 구현
  - [ ] 최대 3회 재시도
  - [ ] 지수 백오프 (1초, 2초, 4초)
  - [ ] 재연결 시 Peer Connection 재생성
  - [ ] Offer/Answer 재교환

- [ ] 사용자에게 상태 알림
  - [ ] "연결이 끊어졌습니다. 재연결 시도 중..."
  - [ ] "재연결에 실패했습니다."
  - [ ] Toast 컴포넌트 사용

#### 네트워크 품질 저하 감지

- [ ] `checkNetworkQuality` 함수 구현
  - [ ] `getStats()` API 사용
  - [ ] Packet Loss 확인
  - [ ] Round-Trip Time 확인
  - [ ] 임계값 초과 시 경고 표시

#### 테스트

- [ ] 네트워크 끊김 시뮬레이션 (DevTools Network 탭)
- [ ] 재연결 확인
- [ ] 재연결 실패 시 에러 메시지 확인

---

### 2.3 통화 품질 모니터링

#### WebRTC 통계 수집

- [ ] `/coup/src/lib/hooks/useVideoCall.js`에 기능 추가
  - [ ] `getConnectionStats` 함수 구현
    - [ ] `peer.getStats()` 호출
    - [ ] 통계 데이터 파싱
  - [ ] 1초마다 통계 업데이트
  - [ ] State로 통계 저장

- [ ] 수집할 통계 항목
  - [ ] Latency (Round-Trip Time)
  - [ ] Bandwidth (송수신 속도)
  - [ ] Packet Loss
  - [ ] Frame Rate
  - [ ] Resolution
  - [ ] Jitter

#### 네트워크 품질 평가

- [ ] `calculateNetworkQuality` 함수
  - [ ] 통계 기반으로 품질 등급 계산
    - [ ] 양호 (Good): Latency < 100ms, Packet Loss < 1%
    - [ ] 보통 (Fair): Latency < 200ms, Packet Loss < 3%
    - [ ] 나쁨 (Poor): 그 외
  - [ ] 품질 등급 State로 저장

#### UI 표시

- [ ] 컨트롤 바에 네트워크 품질 표시
  - [ ] 양호: 🟢
  - [ ] 보통: 🟡
  - [ ] 나쁨: 🔴

- [ ] 상세 통계 모달
  - [ ] 통계 버튼 클릭 시 모달 표시
  - [ ] 모든 통계 항목 표시
  - [ ] 그래프로 시각화 (선택)

#### 테스트

- [ ] 정상 네트워크에서 통계 확인
- [ ] Network 탭에서 속도 제한 (3G)
- [ ] 품질 등급 변화 확인

---

### 2.4 말하는 중 표시

#### 음성 감지 구현

- [ ] `/coup/src/lib/hooks/useAudioDetection.js` 생성
  - [ ] `useAudioDetection` 훅 구현
  - [ ] Web Audio API 사용
    - [ ] AudioContext 생성
    - [ ] MediaStreamSource 생성
    - [ ] AnalyserNode 연결
  - [ ] 음량 레벨 측정
    - [ ] `getByteFrequencyData` 사용
    - [ ] 평균 음량 계산
  - [ ] 임계값 초과 시 speaking 상태
    - [ ] 임계값: 30 (조정 가능)
    - [ ] Debounce 적용 (500ms)

#### 훅 통합

- [ ] `/coup/src/lib/hooks/useVideoCall.js`에 통합
  - [ ] 로컬 스트림에 `useAudioDetection` 적용
  - [ ] `isSpeaking` State 추가
  - [ ] Socket으로 speaking 상태 전송
    - [ ] `video:speaking-changed` 이벤트

- [ ] 원격 참여자 speaking 상태 수신
  - [ ] `video:peer-speaking-changed` 이벤트 리스너
  - [ ] 참여자별 speaking 상태 저장

#### UI 반영

- [ ] `/coup/src/components/video-call/VideoTile.jsx` 수정
  - [ ] `isSpeaking` prop 사용
  - [ ] Speaking 상태일 때 테두리 애니메이션
    - [ ] 초록색 테두리
    - [ ] Pulse 애니메이션

- [ ] CSS 추가
  - [ ] `.speaking` 클래스
  - [ ] `@keyframes pulse` 정의

#### 테스트

- [ ] 마이크에 대고 말하기
- [ ] 비디오 타일 테두리 확인
- [ ] 다른 참여자도 확인

---

### 2.5 채팅 통합

#### 채팅 UI 컴포넌트

- [ ] `/coup/src/components/video-call/ChatPanel.jsx` 생성
  - [ ] 메시지 목록 표시
  - [ ] 메시지 입력 폼
  - [ ] 스크롤 자동 하단 이동
  - [ ] 읽음 표시

#### 메시지 처리

- [ ] 기존 채팅 Socket 이벤트 재사용
  - [ ] `chat:message` emit
  - [ ] `chat:new-message` listen

- [ ] 화상회의 전용 필터링
  - [ ] 현재 roomId 기반 메시지만 표시
  - [ ] 또는 별도 이벤트 사용 (`video:chat:message`)

#### UI 통합

- [ ] 페이지에 채팅 패널 추가
  - [ ] 우측 사이드바로 토글
  - [ ] 채팅 버튼 클릭 시 열림
  - [ ] 새 메시지 알림 뱃지

- [ ] 컨트롤 바에 채팅 버튼 추가
  - [ ] 아이콘: 💬
  - [ ] 새 메시지 시 빨간 점 표시

#### 파일 공유

- [ ] 파일 업로드 기능
  - [ ] Drag & Drop 지원
  - [ ] 파일 선택 버튼
  - [ ] 업로드 진행률 표시

- [ ] 파일 메시지 표시
  - [ ] 파일 아이콘 및 이름
  - [ ] 다운로드 버튼

#### 테스트

- [ ] 메시지 전송 확인
- [ ] 실시간 수신 확인
- [ ] 파일 공유 테스트

---

## 🎨 Phase 3: 최적화 및 테스트 (1주)

### 3.1 성능 최적화

#### 비디오 품질 자동 조정

- [ ] 네트워크 통계 기반 품질 조정
  - [ ] Packet Loss 높으면 해상도 낮춤
  - [ ] Bandwidth 충분하면 해상도 높임
  - [ ] `sender.setParameters()` 사용

- [ ] 참여자 수 기반 최적화
  - [ ] 3명 이하: 720p
  - [ ] 4-6명: 480p
  - [ ] 7명 이상: 360p

#### 레이아웃 최적화

- [ ] 가상화 (Virtualization)
  - [ ] 화면에 보이는 타일만 렌더링
  - [ ] `react-window` 또는 `react-virtualized` 사용

- [ ] Thumbnail 모드
  - [ ] 참여자 많을 때 썸네일 크기 축소
  - [ ] 클릭 시 확대

#### 메모리 최적화

- [ ] 스트림 정리 강화
  - [ ] `useEffect` cleanup 검증
  - [ ] 모든 Peer Connection 정리 확인
  - [ ] Event Listener 정리 확인

- [ ] Debounce/Throttle 적용
  - [ ] 통계 업데이트 throttle (1초)
  - [ ] 음성 감지 debounce (500ms)

#### 테스트

- [ ] 6명 이상 참여 테스트
- [ ] 메모리 프로파일링
- [ ] 성능 측정 (FPS, Memory)

---

### 3.2 UI/UX 개선

#### 우측 위젯 구현

- [ ] `/coup/src/components/video-call/VideoCallWidgets.jsx` 생성
  - [ ] 스터디 현황 위젯
  - [ ] 화상 통화 상태 위젯
  - [ ] 통화 중 참여자 위젯
  - [ ] 빠른 액션 위젯
  - [ ] 화상 통화 팁 위젯
  - [ ] 세션 기록 위젯

#### 레이아웃 모드 전환

- [ ] 그리드 뷰 (기본)
- [ ] 발표자 모드
  - [ ] 화면 공유 시 자동 전환
  - [ ] 공유 화면 큰 영역 표시
  - [ ] 참여자 썸네일 하단 표시

- [ ] 모드 전환 버튼
  - [ ] 수동 전환 가능

#### 키보드 단축키

- [ ] 단축키 핸들러 구현
  - [ ] `M`: 음소거 토글
  - [ ] `V`: 비디오 토글
  - [ ] `S`: 화면 공유 토글
  - [ ] `C`: 채팅 열기
  - [ ] `ESC`: 전체화면 나가기
  - [ ] `F`: 전체화면 토글

- [ ] 단축키 가이드 표시
  - [ ] `?` 키로 가이드 열기
  - [ ] 모든 단축키 목록

#### Toast 알림

- [ ] Toast 컴포넌트 생성 또는 라이브러리 사용
  - [ ] `react-hot-toast` 또는 `sonner`

- [ ] 알림 메시지 추가
  - [ ] "연결되었습니다"
  - [ ] "화면 공유를 시작했습니다"
  - [ ] "연결이 끊어졌습니다"
  - [ ] 등등

#### 로딩 상태

- [ ] Skeleton UI
  - [ ] 대기실 로딩
  - [ ] 비디오 타일 로딩

- [ ] 스피너
  - [ ] API 요청 중
  - [ ] 연결 중

#### 테스트

- [ ] 모든 단축키 테스트
- [ ] 레이아웃 전환 테스트
- [ ] Toast 알림 확인

---

### 3.3 테스트 코드 작성

#### 단위 테스트

- [ ] `useVideoCall` 훅 테스트
  - [ ] `joinRoom` 테스트
  - [ ] `leaveRoom` 테스트
  - [ ] `toggleMute` 테스트
  - [ ] `toggleVideo` 테스트
  - [ ] `shareScreen` 테스트

- [ ] `VideoTile` 컴포넌트 테스트
  - [ ] 렌더링 테스트
  - [ ] Props 전달 테스트
  - [ ] 상태별 표시 테스트

- [ ] `ControlBar` 컴포넌트 테스트
  - [ ] 버튼 클릭 테스트
  - [ ] 상태 변경 테스트

#### 통합 테스트

- [ ] Socket 이벤트 테스트
  - [ ] Mock Socket.io
  - [ ] 이벤트 전송/수신 테스트

- [ ] API 테스트
  - [ ] `/start` 테스트
  - [ ] `/status` 테스트
  - [ ] `/join` 테스트
  - [ ] `/leave` 테스트

#### E2E 테스트

- [ ] Playwright 설정
- [ ] 시나리오 테스트
  - [ ] 2명 참여 플로우
  - [ ] 화면 공유 플로우
  - [ ] 채팅 플로우

#### 테스트 커버리지

- [ ] 80% 이상 목표
- [ ] 커버리지 리포트 생성

---

### 3.4 문서화

#### API 문서

- [ ] `04-api-specification.md` 완성
  - [ ] 모든 엔드포인트 문서화
  - [ ] Request/Response 예제
  - [ ] 에러 코드 정리

#### 사용자 가이드

- [ ] 화상회의 사용법
- [ ] 트러블슈팅
- [ ] FAQ

#### 개발자 가이드

- [ ] 코드 구조 설명
- [ ] 주요 함수 설명
- [ ] 확장 방법

#### 릴리즈 노트

- [ ] 변경사항 정리
- [ ] 알려진 이슈
- [ ] 향후 계획

---

## 📌 추가 작업 (선택)

### 가상 배경

- [ ] Canvas API로 배경 합성
- [ ] TensorFlow.js로 인물 분리
- [ ] 배경 이미지 업로드
- [ ] 배경 흐리기

### 레코딩 기능

- [ ] MediaRecorder API 사용
- [ ] 서버 측 레코딩 구현
- [ ] 클라우드 저장 (S3)
- [ ] 다운로드 기능

### 모바일 최적화

- [ ] 반응형 레이아웃 강화
- [ ] 터치 제스처 지원
- [ ] 모바일 브라우저 테스트

### SFU 서버 (대규모 회의)

- [ ] Mediasoup 또는 Janus 통합
- [ ] 서버 배포
- [ ] 클라이언트 SDK 연동

---

## 🎯 마일스톤

### Milestone 1: MVP (1주)
**목표**: 2명이 정상적으로 화상통화 가능  
**체크리스트**:
- [x] Phase 1 완료
- [ ] 2명 테스트 통과
- [ ] DB 기록 확인
- [ ] 문서화 완료

### Milestone 2: 실용 버전 (3주)
**목표**: 실제 사용 가능한 화상회의 시스템  
**체크리스트**:
- [ ] Phase 2 완료
- [ ] 6명 테스트 통과
- [ ] 에러 처리 완료
- [ ] 채팅 통합 완료

### Milestone 3: 프로덕션 레디 (4주)
**목표**: 안정적이고 최적화된 시스템  
**체크리스트**:
- [ ] Phase 3 완료
- [ ] 테스트 커버리지 80%+
- [ ] 성능 최적화 완료
- [ ] 문서 완성

---

**마지막 업데이트**: 2025-11-19  
**다음 작업**: Phase 1 - Socket 이벤트 통일

