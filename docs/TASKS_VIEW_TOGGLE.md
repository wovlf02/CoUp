# 할 일 탭 리스트/달력 뷰 구현 (완성본)

## 📋 수정 일자
2025-01-21

## 🎯 목표
할 일 탭에서 리스트 뷰와 달력 뷰를 전환할 수 있는 기능 구현 및 달력 뷰 개선

## ✅ 구현 내용

### 1. 달력 뷰 컴포넌트 개선

#### 신규 파일:
- **`TaskCalendarView.jsx`** - 체크박스와 색상이 추가된 달력 뷰
- **`TaskCalendarView.module.css`** - 파스텔 톤 색상 스타일
- **`TaskDayModal.jsx`** - 특정 날짜 할 일 전체 보기 모달
- **`TaskDayModal.module.css`** - 모달 스타일

#### 주요 기능:

##### 1. 체크박스 추가
```javascript
<input
  type="checkbox"
  checked={task.completed}
  onChange={() => onToggle(task.id)}
  className={styles.checkbox}
/>
```
- 달력의 각 할 일에 체크박스 표시
- 클릭 시 즉시 완료/미완료 토글
- 완료된 항목은 취소선과 회색 처리

##### 2. 파스텔 톤 색상 적용
```css
/* 긴급 (빨강 파스텔) */
.taskItem.urgent {
  background: #fee2e2;
  border-color: #fca5a5;
  color: #991b1b;
}

/* 이번 주 (파랑 파스텔) */
.taskItem.thisWeek {
  background: #dbeafe;
  border-color: #93c5fd;
  color: #1e40af;
}

/* 나중에 (초록 파스텔) */
.taskItem.later {
  background: #d1fae5;
  border-color: #6ee7b7;
  color: #065f46;
}
```
- 리스트 뷰와 동일한 색상 시스템
- 긴급도별 파스텔 톤 구분
- 시각적으로 직관적인 우선순위 파악

##### 3. "+N개 더보기" 기능
```javascript
{dayTasks.length > 3 && (
  <div 
    className={styles.moreTask}
    onClick={(e) => handleMoreClick(date, dayTasks, e)}
  >
    +{dayTasks.length - 3}개 더보기
  </div>
)}
```
- 한 날짜당 최대 3개까지만 표시
- 3개 초과 시 "+N개 더보기" 버튼 표시
- 클릭 시 해당 날짜의 모든 할 일 모달로 표시

### 2. 날짜별 할 일 모달 (TaskDayModal)

#### 기능:
1. **모달 헤더**
   - 날짜 표시 (예: 2025년 1월 21일 화요일)
   - 총 할 일 개수 표시
   - 닫기 버튼 (✕)

2. **할 일 목록**
   - 모든 할 일을 스크롤 가능한 목록으로 표시
   - 각 할 일에 체크박스 포함
   - 파스텔 색상으로 긴급도 구분
   - D-Day 표시
   - 스터디 뱃지 표시
   - 설명 표시 (있는 경우)

3. **인터랙션**
   - 체크박스로 완료/미완료 토글
   - 모달 외부 클릭 시 닫기
   - 닫기 버튼 클릭 시 닫기

#### 코드:
```javascript
export default function TaskDayModal({ date, tasks, onToggle, onClose }) {
  const formatDate = (date) => {
    return new Date(date).toLocaleDateString('ko-KR', {
      year: 'numeric',
      month: 'long',
      day: 'numeric',
      weekday: 'long'
    });
  };

  const getTaskColor = (task) => {
    const urgency = getUrgencyLevel(task.dueDate);
    return urgency; // 'urgent', 'thisWeek', 'later'
  };

  // ... 렌더링
}
```

### 3. 색상 시스템 통일

#### 긴급도별 색상:
| 긴급도 | 배경색 | 테두리 | 텍스트 | 설명 |
|--------|---------|---------|---------|------|
| 🔥 긴급 | `#fee2e2` | `#fca5a5` | `#991b1b` | 빨강 파스텔 |
| 📅 이번 주 | `#dbeafe` | `#93c5fd` | `#1e40af` | 파랑 파스텔 |
| 📝 나중에 | `#d1fae5` | `#6ee7b7` | `#065f46` | 초록 파스텔 |
| ✓ 완료 | `#f3f4f6` | `#d1d5db` | `#6b7280` | 회색 |

#### 적용 위치:
- ✅ 리스트 뷰의 TaskGroup 컴포넌트
- ✅ 달력 뷰의 할 일 아이템
- ✅ 모달의 할 일 아이템
- ✅ 모든 뷰에서 일관된 색상

### 4. 체크박스 동작

#### 달력 뷰:
```javascript
<div
  className={styles.taskItem}
  onClick={(e) => {
    e.stopPropagation();
    onToggle(task.id);
  }}
>
  <input
    type="checkbox"
    checked={task.completed}
    onChange={() => onToggle(task.id)}
    onClick={(e) => e.stopPropagation()}
  />
  <span>{task.title}</span>
</div>
```

#### 모달:
```javascript
<div className={styles.taskItem}>
  <input
    type="checkbox"
    checked={task.completed}
    onChange={() => onToggle(task.id)}
  />
  <div className={styles.taskContent}>
    {/* 할 일 상세 정보 */}
  </div>
</div>
```

## 🎨 UI/UX 개선

### 달력 뷰 - Before vs After:

#### Before (체크박스 없음):
```
1일
  할일1
  할일2
  +1개 더보기
```

#### After (체크박스와 색상):
```
1일 [3]
  ☐ 할일1 (빨강 파스텔)
  ☐ 할일2 (파랑 파스텔)
  [+1개 더보기] (클릭 가능)
```

### 모달 - 할 일 상세:
```
┌─────────────────────────────────────┐
│ 📅 2025년 1월 21일 화요일      ✕  │
│ 총 5개의 할 일                      │
├─────────────────────────────────────┤
│ ☐ [빨강] 과제 제출하기    D-1      │
│    컴퓨터 과학 개론 과제            │
│    📚 알고리즘 스터디               │
│                                     │
│ ☐ [파랑] 회의 준비      D-3        │
│    발표 자료 만들기                 │
│    🚀 프로젝트 스터디               │
│                                     │
│ ☑ [회색] 완료된 할일              │
│    (취소선 처리)                    │
└─────────────────────────────────────┘
```

## 📊 기능 상세

### 1. 날짜별 할 일 표시 로직

```javascript
// 날짜별 그룹화
const tasksByDate = useMemo(() => {
  const grouped = {};
  tasks.forEach(task => {
    const dateKey = new Date(task.dueDate).toLocaleDateString('ko-KR');
    if (!grouped[dateKey]) {
      grouped[dateKey] = [];
    }
    grouped[dateKey].push(task);
  });
  return grouped;
}, [tasks]);

// 특정 날짜의 할 일 가져오기
const getDayTasks = (date) => {
  const dateKey = date.toLocaleDateString('ko-KR');
  return tasksByDate[dateKey] || [];
};
```

### 2. 색상 결정 로직

```javascript
const getTaskColor = (task) => {
  const urgency = getUrgencyLevel(task.dueDate);
  const colorMap = {
    urgent: 'urgent',      // D-2 이하
    thisWeek: 'thisWeek',  // D-7 이하
    later: 'later'         // D-8 이상
  };
  return colorMap[urgency] || 'later';
};
```

### 3. D-Day 계산

```javascript
const getDday = (dueDate) => {
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const due = new Date(dueDate);
  due.setHours(0, 0, 0, 0);
  const diff = Math.ceil((due - today) / (1000 * 60 * 60 * 24));
  
  if (diff < 0) return `D+${Math.abs(diff)}`;  // 지난 날짜
  if (diff === 0) return 'D-Day';              // 오늘
  return `D-${diff}`;                          // 남은 날짜
};
```

## 🎯 사용자 시나리오

### 시나리오 1: 달력에서 빠르게 완료 처리
```
1. 달력 뷰 선택
2. 특정 날짜의 할 일 확인
3. 체크박스 클릭으로 즉시 완료 처리
4. 취소선과 회색으로 변경됨
5. 필요시 다시 클릭하여 미완료로 복원
```

### 시나리오 2: 특정 날짜의 모든 할 일 확인
```
1. 달력에서 할 일이 4개 이상인 날짜 확인
2. "+N개 더보기" 클릭
3. 모달에서 전체 할 일 목록 확인
4. 각 할 일의 상세 정보 확인 (설명, 스터디, D-Day)
5. 모달에서 직접 체크박스로 완료 처리
6. 외부 클릭 또는 ✕ 버튼으로 닫기
```

### 시나리오 3: 색상으로 긴급도 파악
```
1. 달력 전체 보기
2. 빨간색(긴급) 할 일 확인 → 우선순위 높음
3. 파란색(이번 주) 할 일 확인 → 중간 우선순위
4. 초록색(나중에) 할 일 확인 → 여유 있음
5. 회색 할 일 → 이미 완료됨
```

## 🔧 기술적 세부사항

### 이벤트 버블링 처리
```javascript
// 체크박스 클릭 시 부모 이벤트 전파 방지
<input
  type="checkbox"
  onChange={() => onToggle(task.id)}
  onClick={(e) => e.stopPropagation()}
/>

// 더보기 버튼 클릭 시 부모 이벤트 전파 방지
<div onClick={(e) => handleMoreClick(date, tasks, e)}>
  +{dayTasks.length - 3}개 더보기
</div>
```

### 모달 닫기 처리
```javascript
// 오버레이 클릭 시 닫기
<div className={styles.modalOverlay} onClick={onClose}>
  {/* 모달 콘텐츠 클릭 시 이벤트 전파 중단 */}
  <div onClick={(e) => e.stopPropagation()}>
    {/* 모달 내용 */}
  </div>
</div>
```

### 성능 최적화
```javascript
// useMemo로 달력 날짜 캐싱
const calendarDays = useMemo(() => {
  // 날짜 계산 로직
}, [currentDate]);

// useMemo로 날짜별 할 일 그룹화 캐싱
const tasksByDate = useMemo(() => {
  // 그룹화 로직
}, [tasks]);
```

## 📱 반응형 디자인

### 체크박스 크기:
- 데스크톱: `14px`
- 태블릿: `12px`
- 모바일: `11px`

### 할 일 아이템:
- 데스크톱: `padding: 6px 8px`, `font-size: 0.75rem`
- 태블릿: `padding: 5px 6px`, `font-size: 0.7rem`
- 모바일: `padding: 4px`, `font-size: 0.65rem`

### 모달:
- 데스크톱: `max-width: 600px`, `max-height: 80vh`
- 모바일: `max-height: 90vh`, 전체 화면 활용

## 🎉 최종 결과

이제 할 일 탭 달력 뷰에서:
- ✅ **체크박스로 즉시 완료 처리** - 달력과 모달 모두에서 가능
- ✅ **파스텔 톤 색상** - 긴급도별 직관적 구분 (빨강/파랑/초록)
- ✅ **리스트 뷰와 색상 통일** - 모든 뷰에서 일관된 색상 시스템
- ✅ **최대 3개 표시** - 깔끔한 달력 레이아웃 유지
- ✅ **+N개 더보기 모달** - 4개 이상일 때 전체 목록 확인
- ✅ **모달에서 상세 정보** - D-Day, 스터디, 설명 모두 표시
- ✅ **모달에서도 체크박스** - 즉시 완료 처리 가능
- ✅ **반응형 디자인** - 모든 기기에서 최적화

## 🎨 색상 참조표

### Tailwind CSS 색상 사용:
```css
/* 긴급 - Red 200~900 */
background: #fee2e2;  /* red-100 */
border: #fca5a5;      /* red-300 */
color: #991b1b;       /* red-800 */

/* 이번 주 - Blue 200~900 */
background: #dbeafe;  /* blue-100 */
border: #93c5fd;      /* blue-300 */
color: #1e40af;       /* blue-800 */

/* 나중에 - Green 200~900 */
background: #d1fae5;  /* green-100 */
border: #6ee7b7;      /* green-300 */
color: #065f46;       /* green-800 */
```

## 🔄 향후 개선 가능

### 1. 드래그 앤 드롭
```javascript
// 할 일을 다른 날짜로 드래그하여 마감일 변경
const handleDragEnd = (taskId, newDate) => {
  updateTaskDueDate(taskId, newDate);
};
```

### 2. 달력에서 직접 추가
```javascript
// 날짜 클릭 시 해당 날짜로 할 일 추가 모달 열기
const handleDateClick = (date) => {
  setNewTaskDate(date);
  setShowCreateModal(true);
};
```

### 3. 월간 통계
```javascript
// 모달에 월간 통계 추가
const monthStats = {
  totalTasks: 30,
  completed: 20,
  pending: 10,
  completionRate: 66.7
};
```

브라우저를 새로고침하면 개선된 달력 뷰를 확인할 수 있습니다! 🎉
