# 페이지네이션 개수 조정

## 📋 수정 일자
2025-01-21

## 🎯 목표
스터디 탐색과 내 스터디 페이지의 페이지네이션 개수를 각각 조정하여 사용성 향상

## ❌ 문제점

### Before:
- **스터디 탐색**: 6개씩 표시 (너무 적음)
- **내 스터디**: 5개씩 표시 (적절)
- 내 스터디 페이지의 필터링과 페이지네이션이 서버 측 데이터에 의존하여 정확하지 않음

## ✅ 해결 방법

### 1. 스터디 탐색 페이지

#### 수정된 파일:
**파일**: `coup/src/app/studies/page.jsx`

#### 변경사항:
```javascript
// Before
const itemsPerPage = 6;

// After
const itemsPerPage = 10;
```

### 2. 내 스터디 페이지

#### 수정된 파일:
**파일**: `coup/src/app/my-studies/page.jsx`

#### 변경사항:

##### Before: 서버 측 페이지네이션 (필터링 문제)
```javascript
const { data, isLoading, error } = useMyStudies({
  page: currentPage,
  limit: itemsPerPage,
});

const allStudies = data?.data || [];
const pagination = data?.pagination || { total: 0, totalPages: 1 };

const myStudies = getFilteredStudies();

// 페이지네이션
{pagination.totalPages > 1 && (
  // ...
)}
```

##### After: 클라이언트 측 페이지네이션 (필터링 정확)
```javascript
// 전체 데이터 가져오기
const { data, isLoading, error } = useMyStudies({
  limit: 1000, // 충분히 큰 값
});

const allStudies = data?.data || [];

// 클라이언트 측 필터링
const filteredStudies = getFilteredStudies();

// 클라이언트 측 페이지네이션
const totalPages = Math.ceil(filteredStudies.length / itemsPerPage);
const startIndex = (currentPage - 1) * itemsPerPage;
const endIndex = startIndex + itemsPerPage;
const myStudies = filteredStudies.slice(startIndex, endIndex);

// 페이지네이션
{totalPages > 1 && (
  // ...
  {Array.from({ length: totalPages }, (_, i) => i + 1).map((page) => (
    // ...
  ))}
)}
```

## 📊 결과

### After:

#### 스터디 탐색 페이지:
- ✅ 한 페이지당 **10개** 표시
- ✅ 11개부터 2페이지 생성
- ✅ 더 많은 스터디를 한 번에 볼 수 있음
- ✅ 스크롤 횟수 감소

#### 내 스터디 페이지:
- ✅ 한 페이지당 **5개** 표시 (유지)
- ✅ 6개부터 2페이지 생성
- ✅ 필터링과 페이지네이션이 정확하게 작동
- ✅ "참여중", "관리중", "대기중" 탭별로 올바른 페이지 수

## 🔍 페이지네이션 예시

### 스터디 탐색 (10개씩):
```
총 스터디 수: 25개
페이지 1: 1~10번 스터디 (10개)
페이지 2: 11~20번 스터디 (10개)
페이지 3: 21~25번 스터디 (5개)
→ 총 3페이지
```

### 내 스터디 - 전체 (5개씩):
```
총 스터디 수: 12개
페이지 1: 1~5번 스터디 (5개)
페이지 2: 6~10번 스터디 (5개)
페이지 3: 11~12번 스터디 (2개)
→ 총 3페이지
```

### 내 스터디 - 참여중 필터 (5개씩):
```
전체: 12개
필터링 후: 4개 (MEMBER 역할만)
페이지 1: 1~4번 스터디 (4개)
→ 총 1페이지 (5개 미만이므로)
```

### 내 스터디 - 관리중 필터 (5개씩):
```
전체: 12개
필터링 후: 7개 (OWNER, ADMIN 역할)
페이지 1: 1~5번 스터디 (5개)
페이지 2: 6~7번 스터디 (2개)
→ 총 2페이지
```

## 🎨 클라이언트 측 페이지네이션의 장점

### 1. 정확한 필터링
- 서버에서 전체 데이터를 가져온 후 클라이언트에서 필터링
- 탭 변경 시 즉각적인 반응
- 추가 API 호출 불필요

### 2. 일관된 동작
```javascript
// 필터링 → 페이지네이션 순서
1. allStudies에서 역할별 필터링 (filteredStudies)
2. filteredStudies의 개수로 총 페이지 수 계산
3. 현재 페이지에 해당하는 항목만 slice
4. 올바른 페이지네이션 버튼 표시
```

### 3. 탭 변경 시 자동 페이지 리셋
```javascript
onClick={() => {
  setActiveTab(tab.label);
  setCurrentPage(1); // 첫 페이지로
}}
```

## 📱 사용자 경험

### 스터디 탐색:
- 한 페이지에 더 많은 스터디 표시
- 스크롤 감소, 탐색 효율 증가
- 10개 단위로 깔끔한 그리드 레이아웃

### 내 스터디:
- 컴팩트한 5개 단위로 관리 용이
- 필터링 시 정확한 페이지네이션
- 탭 전환 시 자동으로 첫 페이지

## 🔧 기술적 개선

### 성능 최적화:
```javascript
// 한 번만 API 호출
const { data } = useMyStudies({ limit: 1000 });

// 클라이언트에서 필터링 + 페이지네이션
const filteredStudies = getFilteredStudies();
const myStudies = filteredStudies.slice(startIndex, endIndex);
```

### 메모리 효율:
- 일반적으로 사용자당 스터디 수가 많지 않음 (< 100개)
- 1000개 limit로 충분히 커버
- 클라이언트 측 처리가 네트워크 비용보다 효율적

## 🧪 테스트 시나리오

### 스터디 탐색:
- [ ] 10개 이하: 페이지네이션 없음
- [ ] 11~20개: 2페이지
- [ ] 21~30개: 3페이지
- [ ] 검색/필터 후에도 10개씩 표시

### 내 스터디 - 전체:
- [ ] 5개 이하: 페이지네이션 없음
- [ ] 6~10개: 2페이지
- [ ] 11~15개: 3페이지

### 내 스터디 - 필터링:
- [ ] "참여중" 탭: MEMBER만 표시, 5개씩
- [ ] "관리중" 탭: OWNER/ADMIN만 표시, 5개씩
- [ ] "대기중" 탭: PENDING만 표시, 5개씩
- [ ] 탭 변경 시 첫 페이지로 자동 이동

### 페이지 전환:
- [ ] 이전/다음 버튼 정상 작동
- [ ] 첫 페이지에서 이전 버튼 비활성화
- [ ] 마지막 페이지에서 다음 버튼 비활성화
- [ ] 페이지 번호 클릭 시 해당 페이지로 이동

## 🎉 결과

이제 두 페이지에서:
- ✅ **스터디 탐색**: 10개씩 표시
- ✅ **내 스터디**: 5개씩 표시
- ✅ 필터링과 페이지네이션이 정확하게 작동
- ✅ 탭 전환 시 자동으로 첫 페이지로
- ✅ 스터디 개수에 따라 페이지 자동 증가
- ✅ 깔끔하고 일관된 사용자 경험

## 📊 페이지네이션 수식

```javascript
// 총 페이지 수
totalPages = Math.ceil(총_항목_수 / 페이지당_항목_수)

// 현재 페이지의 항목
startIndex = (currentPage - 1) * itemsPerPage
endIndex = startIndex + itemsPerPage
items = allItems.slice(startIndex, endIndex)

// 예시 1: 스터디 탐색 (총 25개, 10개씩)
totalPages = Math.ceil(25 / 10) = 3페이지
페이지 1: slice(0, 10) → 0~9번 인덱스
페이지 2: slice(10, 20) → 10~19번 인덱스
페이지 3: slice(20, 30) → 20~24번 인덱스

// 예시 2: 내 스터디 (총 12개, 5개씩)
totalPages = Math.ceil(12 / 5) = 3페이지
페이지 1: slice(0, 5) → 0~4번 인덱스
페이지 2: slice(5, 10) → 5~9번 인덱스
페이지 3: slice(10, 15) → 10~11번 인덱스
```

브라우저를 새로고침하면 올바른 개수와 정확한 페이지네이션을 확인할 수 있습니다! 🎉

