# 스터디 컴포넌트 가이드

## 개요

스터디 탐색 도메인의 UI 컴포넌트에 대한 상세 문서입니다.

---

## 화면 레이아웃

### 스터디 탐색 페이지 전체 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [사이드바]  │                        [헤더]                                │
│             │───────────────────────────────────────────────────────────────│
│  📊 대시보드 │                                                               │
│  📚 스터디  │   🔍 스터디 탐색                      [+ 스터디 만들기]        │
│  ✅ 할일    │   관심있는 스터디를 찾아 함께 성장하세요                      │
│  🔔 알림    │                                                               │
│  👤 마이페이지│  ┌─────────────────────────────────┬───────────────────────┐ │
│             │  │                                   │                       │ │
│             │  │       Main Content (75%)          │   Sidebar (25%)       │ │
│             │  │                                   │                       │ │
│             │  │  ┌─────────────────────────────┐  │  ┌─────────────────┐ │ │
│             │  │  │     검색 & 필터 섹션        │  │  │  🔥 인기 카테고리│ │ │
│             │  │  │  [검색어 입력]  [🔍 검색]   │  │  │  프로그래밍     │ │ │
│             │  │  │                             │  │  │  어학           │ │ │
│             │  │  │  [전체][프로그래밍][어학]...│  │  │  자격증         │ │ │
│             │  │  │                             │  │  └─────────────────┘ │ │
│             │  │  │  모집 상태: [전체][모집중]  │  │                       │ │
│             │  │  │             [모집마감]      │  │  ┌─────────────────┐ │ │
│             │  │  └─────────────────────────────┘  │  │  💡 스터디 팁   │ │ │
│             │  │                                   │  │  명확한 목표    │ │ │
│             │  │  총 150개의 스터디               │  │  규칙적인 일정  │ │ │
│             │  │                                   │  └─────────────────┘ │ │
│             │  │  ┌───┐ ┌───┐ ┌───┐ ┌───┐        │                       │ │
│             │  │  │스터│ │디 │ │카 │ │드 │        │                       │ │
│             │  │  │   │ │   │ │   │ │   │        │                       │ │
│             │  │  └───┘ └───┘ └───┘ └───┘        │                       │ │
│             │  │  ┌───┐ ┌───┐ ┌───┐ ┌───┐        │                       │ │
│             │  │  │   │ │   │ │   │ │   │        │                       │ │
│             │  │  └───┘ └───┘ └───┘ └───┘        │                       │ │
│             │  │                                   │                       │ │
│             │  │  [ 더 불러오는 중... ]           │                       │ │
│             │  │                                   │                       │ │
│             │  └───────────────────────────────────┴───────────────────────┘ │
│             │                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 스터디 카드 레이아웃

```
┌──────────────────────────────────┐
│  💻         [모집중] 또는 [마감] │
│                                  │
│  알고리즘 마스터 스터디          │
│                                  │
│  매주 알고리즘 문제를 함께       │
│  풀어봅니다...                   │
│                                  │
│  프로그래밍 · 알고리즘   ⭐ 4.5  │
│                                  │
│  #알고리즘 #코딩테스트 #Python   │
│                                  │
│  ─────────────────────────────── │
│  👥 12/20명          👤 홍길동  │
└──────────────────────────────────┘
```

### 스터디 프리뷰 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ← 스터디 탐색으로                                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────┐  ┌─────────────────────────┐│
│  │                                           │  │                         ││
│  │   Left Section (70%)                      │  │  Right Section (30%)    ││
│  │                                           │  │                         ││
│  │  ┌─────────────────────────────────────┐  │  │  ┌───────────────────┐  ││
│  │  │         스터디 카드                 │  │  │  │  🚀 빠른 가입     │  ││
│  │  │                                     │  │  │  │                   │  ││
│  │  │  💻               [모집중]          │  │  │  │  지금 가입하고    │  ││
│  │  │                                     │  │  │  │  함께 성장해보세요│  ││
│  │  │  알고리즘 스터디                    │  │  │  │                   │  ││
│  │  │                                     │  │  │  │  [가입하기]       │  ││
│  │  │  프로그래밍 · 알고리즘    ⭐ 4.5    │  │  │  └───────────────────┘  ││
│  │  │                                     │  │  │                         ││
│  │  │  #알고리즘 #코딩테스트              │  │  │  ┌───────────────────┐  ││
│  │  │                                     │  │  │  │  ℹ️ 스터디 정보   │  ││
│  │  │  매주 알고리즘 문제를...            │  │  │  │                   │  ││
│  │  │                                     │  │  │  │  그룹장: 홍길동   │  ││
│  │  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │  │  │  │  멤버: 12/20명   │  ││
│  │  │  │멤버 │ │가입 │ │공개 │ │개설일│   │  │  │  │  평점: ⭐ 4.5    │  ││
│  │  │  │12/20│ │자동 │ │전체 │ │01.01│   │  │  │  │  공개: 전체공개   │  ││
│  │  │  └─────┘ └─────┘ └─────┘ └─────┘   │  │  │  └───────────────────┘  ││
│  │  │                                     │  │  │                         ││
│  │  │      [🚀 스터디 가입하기]           │  │  │  ┌───────────────────┐  ││
│  │  └─────────────────────────────────────┘  │  │  │  ✨ 가입 후 혜택  │  ││
│  │                                           │  │  │                   │  ││
│  │  ┌─────────────────────────────────────┐  │  │  │  💬 실시간 채팅   │  ││
│  │  │  📝 상세 소개                       │  │  │  │  📢 전체 공지    │  ││
│  │  │  ...                                │  │  │  │  📁 학습 자료    │  ││
│  │  └─────────────────────────────────────┘  │  │  │  📅 일정 관리    │  ││
│  │                                           │  │  │  ✅ 할일 관리    │  ││
│  │  ┌─────────────────────────────────────┐  │  │  │  📹 화상 스터디  │  ││
│  │  │  📢 최근 공지   🔒 가입 후 전체 확인│  │  │  └───────────────────┘  ││
│  │  │  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │  │  │                         ││
│  │  │  가입 후 모든 공지를 확인할 수 있습니다│  │                         ││
│  │  └─────────────────────────────────────┘  │  │                         ││
│  │                                           │  │                         ││
│  │  ┌─────────────────────────────────────┐  │  │                         ││
│  │  │  👥 멤버 (12명)  🔒 가입 후 확인    │  │  │                         ││
│  │  │  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │  │  │                         ││
│  │  └─────────────────────────────────────┘  │  │                         ││
│  │                                           │  │                         ││
│  └───────────────────────────────────────────┘  └─────────────────────────┘│
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 스터디 생성 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ← 뒤로가기                                                                 │
│                                                                             │
│  ✨ 새 스터디 만들기                                                        │
│  함께 성장할 멤버들을 모집해보세요                                          │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │     ●───────────────────●───────────────────○                       │   │
│  │   Step 1             Step 2             Step 3                      │   │
│  │  기본 정보           상세 설정          모집 설정                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  Step 1: 기본 정보                                                  │   │
│  │                                                                     │   │
│  │  스터디 이모지 *                                                    │   │
│  │  [💻] [📚] [🎨] [🌏] [🏃] [💼] [🎯] [🚀]                            │   │
│  │                                                                     │   │
│  │  스터디 이름 *                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │ 예: 알고리즘 마스터 스터디                                  │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │  2-50자 사이로 입력해주세요                                        │   │
│  │                                                                     │   │
│  │  카테고리 *                                                         │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │ 카테고리 선택 ▼                                             │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                                                     │   │
│  │  세부 카테고리 *                                                    │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │ 세부 카테고리 선택 ▼                                        │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                                                     │   │
│  │                                          [다음 단계 →]             │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 스터디 가입 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ← 프리뷰로 돌아가기                                                        │
│                                                                             │
│  💻 알고리즘 스터디 가입하기                                                │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │       ●──────────────○──────────────○                               │   │
│  │     Step 1        Step 2        Step 3                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  📋 Step 1/3: 스터디 규칙 확인                                      │   │
│  │  우리 스터디의 규칙을 확인해주세요                                  │   │
│  │                                                                     │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  ✓ 정기 모임에 성실히 참여합니다                            │   │   │
│  │  │  ✓ 과제 및 할일을 기한 내 완료합니다                        │   │   │
│  │  │  ✓ 멤버들과 존중하며 소통합니다                             │   │   │
│  │  │  ✓ 불참 시 최소 1일 전 공지합니다                           │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  │                                                                     │   │
│  │  ☐ 위 규칙을 모두 확인했으며 동의합니다                            │   │
│  │                                                                     │   │
│  │  ⚠️ 규칙을 지키지 않을 경우 강퇴될 수 있습니다                     │   │
│  │                                                                     │   │
│  │  [취소]                               [다음 단계 →]                │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 페이지 컴포넌트

### StudiesExplorePage (Client Component)

**경로:** `/studies`

**파일 위치:** `src/app/studies/page.jsx`

#### 상태 관리

```javascript
// 필터 상태
const [searchKeyword, setSearchKeyword] = useState('')
const [selectedCategory, setSelectedCategory] = useState('전체')
const [recruitingFilter, setRecruitingFilter] = useState('all')

// 무한 스크롤 상태
const [studies, setStudies] = useState([])
const [page, setPage] = useState(1)
const [hasMore, setHasMore] = useState(true)
const [isLoading, setIsLoading] = useState(false)
const [isInitialLoading, setIsInitialLoading] = useState(true)
const [totalCount, setTotalCount] = useState(0)

// 내 스터디 제외용
const [myStudyIds, setMyStudyIds] = useState([])

// 맨 위로 버튼
const [showScrollTop, setShowScrollTop] = useState(false)

// Refs
const observerRef = useRef(null)
const loadMoreRef = useRef(null)
const isLoadingRef = useRef(false)
```

#### 무한 스크롤 구현

```javascript
// Intersection Observer 설정
useEffect(() => {
  if (!hasMore) {
    if (observerRef.current) {
      observerRef.current.disconnect()
    }
    return
  }

  const observer = new IntersectionObserver(
    (entries) => {
      if (entries[0].isIntersecting && hasMore && !isLoading && !isLoadingRef.current) {
        setPage(prev => prev + 1)
      }
    },
    { threshold: 0.1, rootMargin: '100px' }
  )
  
  observerRef.current = observer
  
  if (loadMoreRef.current) {
    observer.observe(loadMoreRef.current)
  }
  
  return () => {
    if (observerRef.current) {
      observerRef.current.disconnect()
    }
  }
}, [hasMore, isLoading])
```

#### 필터링 로직

```javascript
// 스터디 목록 가져오기
const fetchStudies = useCallback(async (pageNum, reset = false) => {
  if (isLoading || isLoadingRef.current) return

  isLoadingRef.current = true
  setIsLoading(true)
  if (reset) setIsInitialLoading(true)
  
  const params = new URLSearchParams({
    page: pageNum.toString(),
    limit: ITEMS_PER_LOAD.toString(),
  })
  
  if (selectedCategory && selectedCategory !== '전체') {
    params.append('category', selectedCategory)
  }
  
  if (searchKeyword && searchKeyword.trim()) {
    params.append('search', searchKeyword.trim())
  }
  
  if (recruitingFilter && recruitingFilter !== 'all') {
    params.append('recruiting', recruitingFilter)
  }
  
  const response = await fetch(`/api/studies?${params}`)
  const data = await response.json()
  
  // 내 스터디 제외
  const filteredStudies = data.data.filter(
    study => !myStudyIds.includes(study.id)
  )
  
  if (reset) {
    setStudies(filteredStudies)
  } else {
    setStudies(prev => [...prev, ...filteredStudies])
  }
  
  setHasMore(data.data.length >= ITEMS_PER_LOAD)
  
}, [selectedCategory, searchKeyword, recruitingFilter, myStudyIds, isLoading])
```

---

### StudyCreatePage (Client Component)

**경로:** `/studies/create`

**파일 위치:** `src/app/studies/create/page.jsx`

#### 상태 관리

```javascript
const router = useRouter()
const createStudy = useCreateStudy()
const [step, setStep] = useState(1)
const [errors, setErrors] = useState({})
const [isSubmitting, setIsSubmitting] = useState(false)
const [formData, setFormData] = useState({
  name: '',
  emoji: '💻',
  category: '',
  subCategory: '',
  description: '',
  tags: [],
  maxMembers: 20,
  isPublic: true,
  autoApprove: true,
  activityFrequency: '',
  location: 'online',
})
```

#### 유효성 검증

```javascript
const validateField = (fieldName, value) => {
  const newErrors = { ...errors }

  switch (fieldName) {
    case 'name':
      if (!value) {
        newErrors.name = '스터디 이름을 입력해주세요'
      } else if (value.length < 2) {
        newErrors.name = '스터디 이름은 최소 2자 이상이어야 합니다'
      } else if (value.length > 50) {
        newErrors.name = '스터디 이름은 최대 50자까지 가능합니다'
      } else {
        delete newErrors.name
      }
      break

    case 'description':
      if (!value) {
        newErrors.description = '스터디 설명을 입력해주세요'
      } else if (value.length < 10) {
        newErrors.description = '스터디 설명은 최소 10자 이상 입력해주세요'
      } else if (value.length > 2000) {
        newErrors.description = '스터디 설명은 최대 2000자까지 가능합니다'
      } else {
        delete newErrors.description
      }
      break

    case 'maxMembers':
      if (value < 2) {
        newErrors.maxMembers = '최소 2명 이상으로 설정해주세요'
      } else if (value > 100) {
        newErrors.maxMembers = '최대 100명까지 설정할 수 있습니다'
      } else {
        delete newErrors.maxMembers
      }
      break

    case 'tags':
      if (value.length > 10) {
        newErrors.tags = '태그는 최대 10개까지 추가할 수 있습니다'
      } else {
        delete newErrors.tags
      }
      break
  }

  setErrors(newErrors)
}
```

#### 스터디 생성 제출

```javascript
const handleSubmit = async (e) => {
  e.preventDefault()
  setErrors({})

  if (!formData.name || !formData.category || !formData.subCategory || !formData.description) {
    showErrorToast('필수 항목을 모두 입력해주세요')
    return
  }

  setIsSubmitting(true)

  try {
    const studyData = {
      name: formData.name,
      emoji: formData.emoji,
      category: formData.category,
      subCategory: formData.subCategory,
      description: formData.description,
      tags: formData.tags,
      maxMembers: formData.maxMembers,
      isPublic: formData.isPublic,
      autoApprove: formData.autoApprove,
    }

    const result = await createStudy.mutateAsync(studyData)
    showSuccessToast('🎉 스터디가 생성되었습니다!')
    router.push(`/my-studies/${result.data.id}`)
  } catch (error) {
    const { message, field, type } = handleStudyError(error)
    
    if (field) {
      setErrors({ [field]: message })
    }
    showStudyErrorToast(error)
  } finally {
    setIsSubmitting(false)
  }
}
```

---

### StudyPreviewPage (Client Component)

**경로:** `/studies/[studyId]`

**파일 위치:** `src/app/studies/[studyId]/page.jsx`

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `params.studyId` | `string` | 스터디 ID |

#### 상태 관리

```javascript
const router = useRouter()
const { studyId } = use(params)
const { data: studyData, isLoading } = useStudy(studyId)
const study = studyData?.data
```

#### 가입 가능 여부 확인

```javascript
// 인원 마감 또는 모집완료 여부 확인
const isFull = study?.currentMembers >= study?.maxMembers
const isNotRecruiting = study?.isRecruiting === false
const cannotJoin = isFull || isNotRecruiting

// 가입 불가 사유 메시지
const getJoinDisabledMessage = () => {
  if (isFull) return '인원이 다 찼습니다'
  if (isNotRecruiting) return '모집이 마감되었습니다'
  return ''
}
```

---

### StudyJoinPage (Client Component)

**경로:** `/studies/[studyId]/join`

**파일 위치:** `src/app/studies/[studyId]/join/page.jsx`

#### 상태 관리

```javascript
const router = useRouter()
const { studyId } = use(params)
const [currentStep, setCurrentStep] = useState(1)
const [isSubmitting, setIsSubmitting] = useState(false)
const [formData, setFormData] = useState({
  agreeToRules: false,
  introduction: '',
  purpose: '',
  level: '',
})

const { data: studyData, isLoading } = useStudy(studyId)
const joinStudy = useJoinStudy()
const study = studyData?.data
```

#### 가입 제출

```javascript
const handleSubmit = async () => {
  setIsSubmitting(true)

  try {
    await joinStudy.mutateAsync({
      id: studyId,
      data: {
        introduction: formData.introduction,
        purpose: formData.purpose,
        level: formData.level,
      }
    })

    if (study.autoApprove) {
      showSuccessToast('🎉 가입이 완료되었습니다!')
      router.push(`/my-studies/${studyId}`)
    } else {
      showSuccessToast('가입 신청이 완료되었습니다. 승인을 기다려주세요.')
      router.push('/studies')
    }
  } catch (error) {
    const { message, type } = handleStudyError(error)

    if (type === 'ALREADY_MEMBER') {
      showErrorToast(message)
      setTimeout(() => router.push(`/my-studies/${studyId}`), 2000)
    } else if (type === 'STUDY_FULL') {
      showErrorToast(message)
      setTimeout(() => router.push(`/studies/${studyId}`), 2000)
    } else {
      showStudyErrorToast(error)
    }
  } finally {
    setIsSubmitting(false)
  }
}
```

---

## 공통 컴포넌트

### StudiesEmptyState

**파일 위치:** `src/components/studies/StudiesEmptyState.jsx`

검색 결과가 없을 때 표시되는 UI입니다.

```jsx
export default function StudiesEmptyState() {
  return (
    <div className={styles.emptyState}>
      <div className={styles.icon}>🔍</div>
      <h3 className={styles.title}>검색 결과가 없습니다</h3>
      <p className={styles.description}>다른 키워드로 검색해보세요</p>
      <button className={styles.resetButton}>
        필터 초기화
      </button>
    </div>
  )
}
```

---

### StudiesSkeleton

**파일 위치:** `src/components/studies/StudiesSkeleton.jsx`

로딩 상태에서 표시되는 스켈레톤 UI입니다.

---

### StudyHeader

**파일 위치:** `src/components/studies/StudyHeader.jsx`

스터디 상세 페이지 헤더 컴포넌트입니다.

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `studyId` | `string` | 스터디 ID |
| `study` | `object` | 스터디 데이터 |

#### 탭 구성

```javascript
const tabs = [
  { label: '개요', path: `/my-studies/${studyId}` },
  { label: '채팅', path: `/my-studies/${studyId}/chat` },
  { label: '공지', path: `/my-studies/${studyId}/notices` },
  { label: '파일', path: `/my-studies/${studyId}/files` },
  { label: '캘린더', path: `/my-studies/${studyId}/calendar` },
  { label: '할일', path: `/my-studies/${studyId}/tasks` },
  { label: '화상 스터디', path: `/my-studies/${studyId}/video-call` },
]

// 스터디장만 설정 탭 표시
if (study?.role === 'OWNER') {
  tabs.push({ label: '설정', path: `/my-studies/${studyId}/settings` })
}
```

---

### StudyTabs

**파일 위치:** `src/components/study/StudyTabs.jsx`

스터디 탭 네비게이션 컴포넌트입니다.

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `studyId` | `string` | 스터디 ID |
| `activeTab` | `string` | 활성 탭 라벨 |
| `userRole` | `string` | 사용자 역할 (OWNER, ADMIN, MEMBER) |

#### 탭 구성

```javascript
const tabs = [
  { label: '개요', href: `/my-studies/${studyId}`, icon: '📊' },
  { label: '채팅', href: `/my-studies/${studyId}/chat`, icon: '💬' },
  { label: '공지', href: `/my-studies/${studyId}/notices`, icon: '📢' },
  { label: '파일', href: `/my-studies/${studyId}/files`, icon: '📁' },
  { label: '일정', href: `/my-studies/${studyId}/calendar`, icon: '📅' },
  { label: '할일', href: `/my-studies/${studyId}/tasks`, icon: '✅' },
  { label: '화상', href: `/my-studies/${studyId}/video-call`, icon: '📹' },
  { label: '멤버', href: `/my-studies/${studyId}/members`, icon: '👥', adminOnly: true },
  { label: '설정', href: `/my-studies/${studyId}/settings`, icon: '⚙️' },
]
```

---

### StudySidebar

**파일 위치:** `src/components/studies/StudySidebar.jsx`

스터디 사이드바 위젯 모음입니다.

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `studyId` | `string` | 스터디 ID |

#### 위젯 구성

```jsx
export default function StudySidebar({ studyId }) {
  return (
    <div>
      <StatsWidget studyId={studyId} />
      <OnlineMembersWidget studyId={studyId} />
      <QuickActionsWidget studyId={studyId} />
      <PinnedNoticeWidget studyId={studyId} />
      <UrgentTasksWidget studyId={studyId} />
      <UpcomingEventsWidget studyId={studyId} />
      <MyActivityWidget studyId={studyId} />
    </div>
  )
}
```

---

### LeaveStudyModal

**파일 위치:** `src/components/study/LeaveStudyModal.jsx`

스터디 탈퇴 확인 모달입니다.

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `isOpen` | `boolean` | 모달 열림 상태 |
| `onClose` | `function` | 닫기 핸들러 |
| `onConfirm` | `function` | 확인 핸들러 |
| `studyName` | `string` | 스터디 이름 |
| `userRole` | `string` | 사용자 역할 |
| `isLoading` | `boolean` | 로딩 상태 |

#### OWNER 탈퇴 불가 처리

```jsx
// OWNER인 경우 탈퇴 불가 안내
{isOwner && (
  <>
    <div className={styles.ownerWarning}>
      <p>
        <strong>스터디장</strong>은 스터디를 탈퇴할 수 없습니다.
      </p>
      <p>
        탈퇴를 원하시면 다른 멤버에게 스터디장 권한을 위임하거나,
        스터디를 삭제해주세요.
      </p>
    </div>

    <div className={styles.optionCards}>
      <div className={styles.optionCard}>
        <span>👥</span>
        <div>
          <h4>권한 위임</h4>
          <p>다른 멤버에게 스터디장 권한을 넘기고 탈퇴할 수 있습니다.</p>
        </div>
      </div>
      <div className={styles.optionCard}>
        <span>🗑️</span>
        <div>
          <h4>스터디 삭제</h4>
          <p>설정 → 위험 구역에서 스터디를 완전히 삭제할 수 있습니다.</p>
        </div>
      </div>
    </div>
  </>
)}
```

---

### NoticeCreateEditModal

**파일 위치:** `src/components/studies/NoticeCreateEditModal.jsx`

공지사항 생성/수정 모달입니다.

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `studyId` | `string` | 스터디 ID |
| `notice` | `object \| null` | 수정할 공지 (생성 시 null) |
| `onClose` | `function` | 닫기 핸들러 |
| `onSuccess` | `function` | 성공 콜백 |

---

### MarkdownRenderer

**파일 위치:** `src/components/studies/MarkdownRenderer.jsx`

마크다운 텍스트를 HTML로 렌더링합니다.

#### Props

| Prop | 타입 | 설명 |
|------|------|------|
| `content` | `string` | 마크다운 콘텐츠 |
| `className` | `string` | 추가 CSS 클래스 |

#### 지원하는 마크다운 문법

- 제목: `# H1`, `## H2`, `### H3`
- 굵게: `**bold**` 또는 `__bold__`
- 기울임: `*italic*` 또는 `_italic_`
- 링크: `[텍스트](URL)`
- 이미지: `![alt](URL)`
- 코드: `` `inline code` `` 또는 ` ```code block``` `
- 리스트: `-` 또는 `*` (unordered), `1.` (ordered)
- 인용: `> quote`
- 수평선: `---` 또는 `***`

---

## 스타일 클래스

### 스터디 탐색 페이지

```css
.container        /* 메인 컨테이너 */
.mainContent      /* 메인 콘텐츠 영역 */
.sidebar          /* 우측 사이드바 */

.header           /* 페이지 헤더 */
.headerContent    /* 헤더 내용 */
.title            /* 페이지 제목 */
.subtitle         /* 페이지 부제목 */
.createButton     /* 스터디 만들기 버튼 */

.filterSection    /* 필터 섹션 */
.searchBox        /* 검색 박스 */
.searchInput      /* 검색 입력 */
.searchButton     /* 검색 버튼 */
.categoryTabs     /* 카테고리 탭 */
.categoryTab      /* 카테고리 탭 아이템 */
.categoryTab.active /* 활성 탭 */
.recruitingFilter /* 모집 상태 필터 */
.filterBtn        /* 필터 버튼 */
.filterBtn.active /* 활성 필터 */

.resultInfo       /* 결과 정보 */
.loadedCount      /* 로드된 수 */

.studiesGrid      /* 스터디 그리드 */
.studyCard        /* 스터디 카드 */
.fullStudyCard    /* 마감된 스터디 카드 */
.cardHeader       /* 카드 헤더 */
.emoji            /* 스터디 이모지 */
.recruitingBadge  /* 모집중 배지 */
.fullBadge        /* 인원마감 배지 */
.closedBadge      /* 모집완료 배지 */
.studyName        /* 스터디 이름 */
.studyDescription /* 스터디 설명 */
.studyMeta        /* 스터디 메타 정보 */
.category         /* 카테고리 */
.rating           /* 평점 */
.tags             /* 태그 영역 */
.tag              /* 태그 */
.cardFooter       /* 카드 푸터 */
.members          /* 멤버 수 */
.membersFull      /* 마감된 멤버 수 */
.owner            /* 스터디장 */

.loadMore         /* 더 불러오기 영역 */
.loadingMore      /* 로딩 중 */
.endMessage       /* 끝 메시지 */

.widget           /* 사이드바 위젯 */
.widgetTitle      /* 위젯 제목 */
.widgetContent    /* 위젯 내용 */
```

### 스터디 생성 페이지

```css
.container        /* 컨테이너 */
.header           /* 헤더 */
.backButton       /* 뒤로가기 버튼 */
.title            /* 제목 */
.subtitle         /* 부제목 */

.steps            /* 단계 표시 */
.stepItem         /* 단계 아이템 */
.stepItem.active  /* 활성 단계 */
.stepNumber       /* 단계 번호 */
.stepLabel        /* 단계 라벨 */
.stepLine         /* 단계 연결선 */

.form             /* 폼 */
.formSection      /* 폼 섹션 */
.sectionTitle     /* 섹션 제목 */
.formGroup        /* 폼 그룹 */
.label            /* 라벨 */
.required         /* 필수 표시 */
.input            /* 입력 필드 */
.inputError       /* 에러 상태 입력 */
.select           /* 셀렉트 */
.textarea         /* 텍스트에리어 */
.hint             /* 힌트 텍스트 */
.errorText        /* 에러 텍스트 */

.emojiPicker      /* 이모지 선택 */
.emojiButton      /* 이모지 버튼 */
.emojiButton.selected /* 선택된 이모지 */

.buttonGroup      /* 버튼 그룹 */
.nextButton       /* 다음 버튼 */
.prevButton       /* 이전 버튼 */
.submitButton     /* 제출 버튼 */
```

### 스터디 가입 페이지

```css
.container        /* 컨테이너 */
.header           /* 헤더 */
.backButton       /* 뒤로가기 버튼 */
.title            /* 제목 */

.progress         /* 진행 표시 */
.progressSteps    /* 진행 단계 */
.step             /* 단계 */
.step.active      /* 활성 단계 */
.stepDivider      /* 단계 구분선 */
.progressLabel    /* 진행 라벨 */

.mainContent      /* 메인 콘텐츠 */
.formSection      /* 폼 섹션 */
.stepCard         /* 스텝 카드 */
.stepTitle        /* 스텝 제목 */
.stepDescription  /* 스텝 설명 */

.rulesList        /* 규칙 목록 */
.ruleItem         /* 규칙 아이템 */
.ruleIcon         /* 규칙 아이콘 */
.ruleText         /* 규칙 텍스트 */
.agreeBox         /* 동의 박스 */
.agreeLabel       /* 동의 라벨 */
.warning          /* 경고 */

.buttonGroup      /* 버튼 그룹 */
.backBtn          /* 뒤로 버튼 */
.nextBtn          /* 다음 버튼 */
```

---

## 반응형 디자인

### 브레이크포인트

```css
/* 모바일 (< 768px) */
@media (max-width: 768px) {
  .container {
    flex-direction: column;
  }
  
  .sidebar {
    order: -1;
    width: 100%;
  }
  
  .studiesGrid {
    grid-template-columns: 1fr;
  }
  
  .categoryTabs {
    overflow-x: auto;
    white-space: nowrap;
  }
}

/* 태블릿 (768px - 1024px) */
@media (min-width: 768px) and (max-width: 1024px) {
  .studiesGrid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* 데스크톱 (> 1024px) */
@media (min-width: 1024px) {
  .studiesGrid {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .container {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 24px;
  }
}
```

