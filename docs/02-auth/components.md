# 인증 컴포넌트 가이드

## 개요

인증 관련 UI 컴포넌트와 페이지에 대한 상세 문서입니다.

---

## 화면 레이아웃

### 로그인 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  ← (뒤로가기)                                                   │
│                                                                 │
│         ┌─────────────────────────────────────────┐             │
│         │                                         │             │
│         │           [CoUp 로고]                   │             │
│         │                                         │             │
│         │      로그인하고 시작하기                │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │ ⚠️ 에러 메시지 (조건부)          │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  이메일                                 │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │ example@email.com               │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  비밀번호                               │             │
│         │  ┌──────────────────────────────┬──┐   │             │
│         │  │ ********                     │👁│   │             │
│         │  └──────────────────────────────┴──┘   │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │           로그인                │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  ──────────── 또는 ────────────        │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │  [G] Google로 계속하기          │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │  [GH] GitHub로 계속하기         │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  아직 계정이 없으신가요? [회원가입]    │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │ 🧹 세션 초기화 (에러 시만 표시) │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │       이용약관 | 개인정보처리방침       │             │
│         │                                         │             │
│         └─────────────────────────────────────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 회원가입 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  ← (뒤로가기)                                                   │
│                                                                 │
│         ┌─────────────────────────────────────────┐             │
│         │                                         │             │
│         │           [CoUp 로고]                   │             │
│         │                                         │             │
│         │      새로운 여정을 시작하세요           │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │ ⚠️ 에러 메시지 (조건부)          │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  프로필 사진 (선택)                     │             │
│         │  ┌──────┐                               │             │
│         │  │  📷  │  [사진 선택]                  │             │
│         │  │      │  JPG, PNG (최대 5MB)          │             │
│         │  └──────┘                               │             │
│         │                                         │             │
│         │  이름 *                                 │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │ 홍길동                          │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  이메일 *                               │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │ example@email.com               │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  비밀번호                               │             │
│         │  ┌──────────────────────────────┬──┐   │             │
│         │  │ ********                     │👁│   │             │
│         │  └──────────────────────────────┴──┘   │             │
│         │  ████████░░ 비밀번호 강도: 보통        │             │
│         │                                         │             │
│         │  비밀번호 확인                          │             │
│         │  ┌──────────────────────────────┬──┐   │             │
│         │  │ ********                     │👁│   │             │
│         │  └──────────────────────────────┴──┘   │             │
│         │                                         │             │
│         │  ┌─────────────────────────────────┐   │             │
│         │  │          회원가입               │   │             │
│         │  └─────────────────────────────────┘   │             │
│         │                                         │             │
│         │  회원가입 시 이용약관 및                │             │
│         │  개인정보처리방침에 동의하는 것으로     │             │
│         │  간주됩니다.                            │             │
│         │                                         │             │
│         │  이미 계정이 있으신가요? [로그인]       │             │
│         │                                         │             │
│         └─────────────────────────────────────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 컴포넌트 계층 구조

### 로그인 페이지 컴포넌트 트리

```
SignInPage
│
├── BackButton (← 뒤로가기)
│
└── Card (로그인 카드)
    │
    ├── LogoContainer
    │   └── Image (CoUp 로고)
    │
    ├── Title ("로그인하고 시작하기")
    │
    ├── ErrorMessage (조건부)
    │   ├── Icon (⚠️)
    │   └── Text
    │
    ├── Form (로그인 폼)
    │   │
    │   ├── FormGroup (이메일)
    │   │   ├── Label
    │   │   ├── Input (type="email")
    │   │   └── FormError (조건부)
    │   │
    │   ├── FormGroup (비밀번호)
    │   │   ├── Label
    │   │   ├── InputWrapper
    │   │   │   ├── Input (type="password/text")
    │   │   │   └── PasswordToggle (👁 버튼)
    │   │   └── FormError (조건부)
    │   │
    │   └── LoginButton
    │       ├── Spinner (로딩 시)
    │       └── Text
    │
    ├── Divider ("또는")
    │
    ├── SocialButtons
    │   ├── GoogleButton
    │   │   ├── GoogleIcon
    │   │   └── Text
    │   │
    │   └── GitHubButton
    │       ├── GitHubIcon
    │       └── Text
    │
    ├── SignupLink
    │   └── Link ("/sign-up")
    │
    ├── TroubleshootSection (에러 시만)
    │   ├── ClearSessionButton
    │   └── HintText
    │
    └── Footer
        └── FooterLinks
            ├── Link ("/terms")
            └── Link ("/privacy")
```

### 회원가입 페이지 컴포넌트 트리

```
SignUpPage
│
├── BackButton (← 뒤로가기)
│
└── Card (회원가입 카드)
    │
    ├── LogoContainer
    │   └── Image (CoUp 로고)
    │
    ├── Title ("새로운 여정을 시작하세요")
    │
    ├── ErrorMessage (조건부)
    │   ├── Icon (⚠️)
    │   └── Text
    │
    └── Form (회원가입 폼)
        │
        ├── FormGroup (프로필 사진)
        │   ├── Label
        │   ├── AvatarUploadContainer
        │   │   ├── AvatarPreview
        │   │   │   ├── AvatarImage (업로드 시)
        │   │   │   └── AvatarPlaceholder (📷)
        │   │   └── AvatarUploadInfo
        │   │       ├── FileInput (hidden)
        │   │       ├── FileInputLabel ("사진 선택")
        │   │       └── FileInputHint
        │   └── FormError (조건부)
        │
        ├── FormGroup (이름)
        │   ├── Label
        │   ├── Input (type="text")
        │   └── FormError (조건부)
        │
        ├── FormGroup (이메일)
        │   ├── Label
        │   ├── Input (type="email")
        │   └── FormError (조건부)
        │
        ├── FormGroup (비밀번호)
        │   ├── Label
        │   ├── InputWrapper
        │   │   ├── Input (type="password/text")
        │   │   └── PasswordToggle
        │   ├── PasswordStrength
        │   │   ├── StrengthBar
        │   │   │   └── StrengthFill
        │   │   └── StrengthLabel
        │   └── FormError (조건부)
        │
        ├── FormGroup (비밀번호 확인)
        │   ├── Label
        │   ├── InputWrapper
        │   │   ├── Input (type="password/text")
        │   │   └── PasswordToggle
        │   └── FormError (조건부)
        │
        ├── SignupButton
        │   ├── Spinner (로딩 시)
        │   └── Text
        │
        ├── TermsText
        │   ├── Link ("/terms")
        │   └── Link ("/privacy")
        │
        └── SigninLink
            └── Link ("/sign-in")
```

---

## 페이지 컴포넌트

### 로그인 페이지 (`sign-in/page.jsx`)

**경로:** `/sign-in`

**파일 위치:** `src/app/(auth)/sign-in/page.jsx`

#### 컴포넌트 구조

```
SignInPage
├── 뒤로가기 버튼
├── 로고
├── 제목
├── 에러 메시지 (조건부)
├── 로그인 폼
│   ├── 이메일 입력
│   ├── 비밀번호 입력 (토글 포함)
│   └── 로그인 버튼
├── 구분선 ("또는")
├── 소셜 로그인 버튼
│   ├── Google 버튼
│   └── GitHub 버튼
├── 회원가입 링크
├── 세션 초기화 버튼 (에러 시)
└── 푸터 링크
```

#### 상태 (State)

```typescript
interface SignInState {
  // 폼 데이터
  email: string
  password: string
  showPassword: boolean
  
  // UI 상태
  loading: 'credentials' | 'google' | 'github' | null
  error: string | null
  formErrors: {
    email?: string
    password?: string
  }
}
```

#### Props

없음 (페이지 컴포넌트)

#### 주요 기능

1. **이메일/비밀번호 로그인**
   - NextAuth `signIn('credentials')` 사용
   - 실시간 폼 유효성 검증

2. **세션 자동 검증**
   - 이미 로그인된 경우 `/api/auth/validate-session` 호출
   - 유효한 세션이면 자동 리다이렉트

3. **관리자 분기 처리**
   - 로그인 성공 후 `/api/auth/me` 호출
   - adminRole 존재 시 `/admin`으로 이동

4. **에러 처리**
   - URL 파라미터 에러 표시 (`?error=account-deleted`)
   - 세션 초기화 버튼 제공

#### 유효성 검증

```javascript
const validateEmail = (email) => {
  const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  return re.test(email)
}

const validateForm = () => {
  const errors = {}
  
  if (!email) {
    errors.email = '이메일을 입력해주세요'
  } else if (!validateEmail(email)) {
    errors.email = '올바른 이메일 형식이 아닙니다'
  }
  
  if (!password) {
    errors.password = '비밀번호를 입력해주세요'
  } else if (password.length < 8) {
    errors.password = '비밀번호는 8자 이상이어야 합니다'
  }
  
  return Object.keys(errors).length === 0
}
```

#### 사용 예시

```jsx
// 내부적으로 사용되는 로그인 처리
const handleCredentialsLogin = async (e) => {
  e.preventDefault()
  
  if (!validateForm()) return
  
  setLoading('credentials')
  
  const result = await signIn('credentials', {
    email,
    password,
    redirect: false,
  })
  
  if (result?.ok) {
    // 관리자 권한 확인 후 리다이렉트
    const userData = await api.get('/api/auth/me')
    if (userData.adminRole) {
      router.push('/admin')
    } else {
      router.push('/dashboard')
    }
  }
}
```

---

### 회원가입 페이지 (`sign-up/page.jsx`)

**경로:** `/sign-up`

**파일 위치:** `src/app/(auth)/sign-up/page.jsx`

#### 컴포넌트 구조

```
SignUpPage
├── 뒤로가기 버튼
├── 로고
├── 제목
├── 에러 메시지 (조건부)
├── 회원가입 폼
│   ├── 프로필 사진 업로드 (선택)
│   ├── 이름 입력
│   ├── 이메일 입력
│   ├── 비밀번호 입력
│   │   └── 비밀번호 강도 표시
│   ├── 비밀번호 확인 입력
│   └── 회원가입 버튼
├── 이용약관 동의 안내
└── 로그인 링크
```

#### 상태 (State)

```typescript
interface SignUpState {
  // 폼 데이터
  name: string
  email: string
  password: string
  confirmPassword: string
  avatar: File | null
  avatarPreview: string | null
  
  // 비밀번호 표시
  showPassword: boolean
  showConfirmPassword: boolean
  
  // UI 상태
  loading: boolean
  uploadingAvatar: boolean
  error: string | null
  formErrors: {
    name?: string
    email?: string
    password?: string
    confirmPassword?: string
    avatar?: string
  }
  passwordStrength: 'weak' | 'medium' | 'strong' | null
}
```

#### 주요 기능

1. **프로필 사진 업로드**
   - 이미지 파일만 허용
   - 5MB 제한
   - 미리보기 기능

2. **비밀번호 강도 표시**
   - 실시간 강도 계산
   - 시각적 표시 (약함/보통/강함)

3. **유효성 검증**
   - 이름: 2자 이상
   - 이메일: 형식 검증
   - 비밀번호: 8자 이상, 복합 문자 조합
   - 비밀번호 확인: 일치 여부

4. **자동 로그인**
   - 회원가입 성공 후 자동 로그인
   - 대시보드로 리다이렉트

#### 비밀번호 강도 계산

```javascript
const calculatePasswordStrength = (pwd) => {
  if (pwd.length < 8) return 'weak'
  
  let strength = 0
  if (/[a-z]/.test(pwd)) strength++  // 소문자
  if (/[A-Z]/.test(pwd)) strength++  // 대문자
  if (/[0-9]/.test(pwd)) strength++  // 숫자
  if (/[^a-zA-Z0-9]/.test(pwd)) strength++  // 특수문자
  
  if (strength >= 3 && pwd.length >= 12) return 'strong'
  if (strength >= 2 && pwd.length >= 8) return 'medium'
  return 'weak'
}
```

#### 유효성 검증

```javascript
const validateForm = () => {
  const errors = {}
  
  // 이름 검증
  if (!name || name.trim().length < 2) {
    errors.name = '이름은 2자 이상이어야 합니다'
  }

  // 이메일 검증
  if (!email) {
    errors.email = '이메일을 입력해주세요'
  } else if (!validateEmail(email)) {
    errors.email = '올바른 이메일 형식이 아닙니다'
  }
  
  // 비밀번호 검증
  if (!password) {
    errors.password = '비밀번호를 입력해주세요'
  } else if (password.length < 8) {
    errors.password = '비밀번호는 8자 이상이어야 합니다'
  } else {
    const hasLetter = /[a-zA-Z]/.test(password)
    const hasNumber = /[0-9]/.test(password)
    const hasSpecial = /[^a-zA-Z0-9]/.test(password)
    const validTypes = [hasLetter, hasNumber, hasSpecial].filter(Boolean).length
    
    if (validTypes < 2) {
      errors.password = '영문, 숫자, 특수문자 중 2가지 이상 포함해야 합니다'
    }
  }
  
  // 비밀번호 확인 검증
  if (!confirmPassword) {
    errors.confirmPassword = '비밀번호 확인을 입력해주세요'
  } else if (password !== confirmPassword) {
    errors.confirmPassword = '비밀번호가 일치하지 않습니다'
  }
  
  return Object.keys(errors).length === 0
}
```

---

### 인증 레이아웃 (`layout.jsx`)

**파일 위치:** `src/app/(auth)/layout.jsx`

```jsx
export const metadata = {
  title: '로그인 - CoUp',
  description: 'CoUp에 로그인하고 스터디를 시작하세요',
}

export default function AuthLayout({ children }) {
  return children
}
```

- 헤더/사이드바 없는 깔끔한 레이아웃
- SEO 메타데이터 설정

---

## 스타일

### 로그인 스타일 (`sign-in.module.css`)

**파일 위치:** `src/styles/auth/sign-in.module.css`

#### 주요 클래스

```css
.container { /* 전체 컨테이너, 중앙 정렬 */ }
.card { /* 로그인 카드 */ }
.logoContainer { /* 로고 영역 */ }
.title { /* 제목 */ }

/* 폼 */
.form { /* 폼 컨테이너 */ }
.formGroup { /* 입력 그룹 */ }
.formLabel { /* 라벨 */ }
.formInput { /* 입력 필드 */ }
.formInput.error { /* 에러 상태 */ }
.formError { /* 에러 메시지 */ }

/* 비밀번호 */
.inputWrapper { /* 비밀번호 입력 래퍼 */ }
.passwordToggle { /* 비밀번호 표시 토글 */ }

/* 버튼 */
.loginButton { /* 로그인 버튼 */ }
.loadingButton { /* 로딩 상태 버튼 */ }
.spinner { /* 로딩 스피너 */ }

/* 에러 */
.errorMessage { /* 에러 메시지 박스 */ }

/* 소셜 로그인 */
.divider { /* 구분선 */ }
.socialButtons { /* 소셜 버튼 컨테이너 */ }
.socialButton { /* 소셜 버튼 기본 */ }
.googleButton { /* Google 버튼 */ }
.githubButton { /* GitHub 버튼 */ }

/* 링크 */
.signupLink { /* 회원가입 링크 */ }
.footer { /* 푸터 */ }
.footerLinks { /* 푸터 링크 */ }

/* 세션 초기화 */
.troubleshootSection { /* 문제 해결 섹션 */ }
.clearSessionButton { /* 세션 초기화 버튼 */ }

/* 뒤로가기 */
.backButton { /* 뒤로가기 버튼 */ }
```

### 회원가입 스타일 (`sign-up.module.css`)

**파일 위치:** `src/styles/auth/sign-up.module.css`

#### 추가 클래스

```css
/* 프로필 사진 */
.avatarUploadContainer { /* 업로드 컨테이너 */ }
.avatarPreview { /* 미리보기 */ }
.avatarPlaceholder { /* 플레이스홀더 */ }
.avatarImage { /* 업로드된 이미지 */ }
.fileInput { /* 숨겨진 파일 입력 */ }
.fileInputLabel { /* 파일 선택 버튼 */ }
.fileInputHint { /* 힌트 텍스트 */ }

/* 비밀번호 강도 */
.passwordStrength { /* 강도 컨테이너 */ }
.strengthBar { /* 강도 바 */ }
.strengthFill { /* 강도 채우기 */ }
.strengthFill.weak { /* 약함 - 빨강 */ }
.strengthFill.medium { /* 보통 - 노랑 */ }
.strengthFill.strong { /* 강함 - 초록 */ }
.strengthLabel { /* 강도 텍스트 */ }

/* 회원가입 버튼 */
.signupButton { /* 회원가입 버튼 */ }

/* 약관 */
.termsText { /* 이용약관 동의 안내 */ }

/* 로그인 링크 */
.signinLink { /* 로그인 링크 */ }
```

---

## Hooks 및 유틸리티

### useSession (next-auth/react)

```jsx
import { useSession } from 'next-auth/react'

function Component() {
  const { data: session, status, update } = useSession()
  
  // status: 'loading' | 'authenticated' | 'unauthenticated'
  
  if (status === 'loading') {
    return <Loading />
  }
  
  if (status === 'unauthenticated') {
    return <Redirect to="/sign-in" />
  }
  
  return <div>Welcome, {session.user.name}</div>
}
```

### signIn (next-auth/react)

```jsx
import { signIn } from 'next-auth/react'

// Credentials 로그인
const result = await signIn('credentials', {
  email: 'user@example.com',
  password: 'password123',
  redirect: false,  // 수동 리다이렉트
})

if (result?.error) {
  console.error('로그인 실패:', result.error)
} else if (result?.ok) {
  console.log('로그인 성공')
  router.push('/dashboard')
}

// 소셜 로그인 (미구현)
await signIn('google', { callbackUrl: '/dashboard' })
await signIn('github', { callbackUrl: '/dashboard' })
```

### signOut (next-auth/react)

```jsx
import { signOut } from 'next-auth/react'

// 로그아웃
await signOut({ redirect: false })

// 또는 리다이렉트와 함께
await signOut({ callbackUrl: '/sign-in' })
```

### useQueryClient (캐시 초기화)

```jsx
import { useQueryClient } from '@tanstack/react-query'

const queryClient = useQueryClient()

// 로그아웃 시 캐시 초기화
await signOut({ redirect: false })
queryClient.clear()
```

---

## 접근성

### 폼 접근성

```jsx
// 라벨과 입력 연결
<label htmlFor="email">이메일</label>
<input id="email" type="email" />

// 버튼 aria-label
<button aria-label="뒤로가기">
  <svg>...</svg>
</button>

<button aria-label={showPassword ? '비밀번호 숨기기' : '비밀번호 보기'}>
  <svg>...</svg>
</button>
```

### 에러 상태 표시

```jsx
<input
  className={`${styles.formInput} ${formErrors.email ? styles.error : ''}`}
/>
{formErrors.email && (
  <div className={styles.formError}>{formErrors.email}</div>
)}
```

### 로딩 상태

```jsx
<button disabled={loading !== null}>
  {loading === 'credentials' ? (
    <div className={styles.loadingButton}>
      <div className={styles.spinner}></div>
      <span>로그인 중...</span>
    </div>
  ) : (
    '로그인'
  )}
</button>
```

---

## 에러 처리

### URL 파라미터 에러

```jsx
const searchParams = useSearchParams()
const errorParam = searchParams.get('error')

const [error, setError] = useState(
  errorParam === 'account-deleted' ? '삭제된 계정입니다.' :
  errorParam === 'account-suspended' ? '정지된 계정입니다.' :
  errorParam === 'CredentialsSignin' ? '이메일 또는 비밀번호가 일치하지 않습니다.' :
  null
)
```

### API 에러 처리

```jsx
try {
  const result = await signIn('credentials', { ... })
  if (result?.error) {
    setError(result.error)
  }
} catch (err) {
  console.error('로그인 실패:', err)
  setError('로그인 중 오류가 발생했습니다.')
}
```

### 세션 초기화 (문제 해결)

```jsx
const handleClearSession = () => {
  // 쿠키 삭제
  document.cookie.split(";").forEach(cookie => {
    const name = cookie.split("=")[0].trim()
    document.cookie = name + "=;expires=Thu, 01 Jan 1970 00:00:00 GMT;path=/"
  })

  // 스토리지 삭제
  localStorage.clear()
  sessionStorage.clear()

  // 페이지 새로고침
  window.location.reload()
}
```
