# 알림 도메인 (Notifications)

## 개요

CoUp 프로젝트의 알림 도메인은 사용자에게 스터디 활동에 대한 실시간 알림을 제공합니다.
가입 승인, 공지, 파일, 일정, 할일, 멤버 변경, 강퇴, 채팅 등 다양한 알림 유형을 지원합니다.

### 주요 특징

- **다양한 알림 유형**: 11개 이상의 알림 타입 지원
- **읽음/읽지 않음 필터링**: 상태별 필터링 및 전체 읽음 처리
- **날짜별 그룹화**: 오늘, 어제, 이번 주, 이전 자동 분류
- **타입별 필터링**: 특정 알림 유형만 조회
- **대량 삭제**: 읽은 알림 일괄 삭제 지원

---

## 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              알림 도메인                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                     /notifications (알림 페이지)                 │    │
│  │                                                                  │    │
│  │   ┌─────────────────────────────────────────────────────────┐   │    │
│  │   │ NotificationHeader                                       │   │    │
│  │   │ • 통계 (전체/읽지않음/읽음)                               │   │    │
│  │   │ • [모두 읽음 처리] [읽은 알림 삭제]                       │   │    │
│  │   └─────────────────────────────────────────────────────────┘   │    │
│  │                                                                  │    │
│  │   ┌─────────────────────────────────────────────────────────┐   │    │
│  │   │ NotificationFilters                                      │   │    │
│  │   │ [전체] [읽지않음] [읽음]  [타입 필터 드롭다운]            │   │    │
│  │   └─────────────────────────────────────────────────────────┘   │    │
│  │                                                                  │    │
│  │   ┌─────────────────────────────────────────────────────────┐   │    │
│  │   │ NotificationList (날짜별 그룹)                           │   │    │
│  │   │                                                          │   │    │
│  │   │  📅 오늘                                                 │   │    │
│  │   │  ┌─────────────────────────────────────────────────┐    │   │    │
│  │   │  │ ● [가입승인] 💻 알고리즘 스터디                  │    │   │    │
│  │   │  │   가입 신청이 승인되었습니다             5분 전  │    │   │    │
│  │   │  └─────────────────────────────────────────────────┘    │   │    │
│  │   │  ┌─────────────────────────────────────────────────┐    │   │    │
│  │   │  │ ○ [공지] 📚 독서 모임                            │    │   │    │
│  │   │  │   새로운 공지가 등록되었습니다          1시간 전  │    │   │    │
│  │   │  └─────────────────────────────────────────────────┘    │   │    │
│  │   │                                                          │   │    │
│  │   │  📅 어제                                                 │   │    │
│  │   │  ┌─────────────────────────────────────────────────┐    │   │    │
│  │   │  │ ○ [파일] 🌍 영어 스터디                          │    │   │    │
│  │   │  │   새 파일이 업로드되었습니다               어제  │    │   │    │
│  │   │  └─────────────────────────────────────────────────┘    │   │    │
│  │   │                                                          │   │    │
│  │   └─────────────────────────────────────────────────────────┘   │    │
│  │                                                                  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 페이지 구조

```
/notifications
│
├── page.jsx                  # 알림 메인 페이지
├── page.module.css           # 스타일
├── constants.js              # 상수 정의 (타입, 필터)
├── utils.js                  # 유틸리티 함수
│
├── components/
│   ├── Toast.jsx             # 토스트 알림
│   ├── NotificationSkeleton.jsx  # 로딩 스켈레톤
│   ├── NotificationHeader.jsx    # 헤더 (통계, 액션 버튼)
│   ├── NotificationFilters.jsx   # 필터 컴포넌트
│   ├── NotificationList.jsx      # 알림 목록
│   └── EmptyState.jsx            # 빈 상태
│
└── hooks/
    └── useNotifications.js   # 알림 커스텀 훅
```

---

## 데이터 모델

### Notification

```javascript
{
  id: String,
  userId: String,             // 수신자 ID
  type: NotificationType,     // 알림 타입 (enum)
  message: String,            // 알림 메시지
  link: String | null,        // 클릭 시 이동할 링크
  isRead: Boolean,            // 읽음 여부
  studyId: String | null,     // 관련 스터디 ID
  studyName: String | null,   // 스터디 이름 (캐시)
  studyEmoji: String | null,  // 스터디 이모지 (캐시)
  data: Json | null,          // 추가 데이터
  createdAt: DateTime,
  readAt: DateTime | null     // 읽은 시간
}
```

---

## 알림 타입 (NotificationType)

### Prisma Enum 정의 타입

| 타입 | 아이콘 | 라벨 | 색상 | 설명 |
|------|--------|------|------|------|
| `JOIN_APPROVED` | ✅ | 가입 승인 | #10b981 | 스터디 가입 승인됨 |
| `NOTICE` | 📢 | 공지 | #ef4444 | 새 공지사항 |
| `FILE` | 📁 | 파일 | #8b5cf6 | 새 파일 업로드 |
| `EVENT` | 📅 | 일정 | #f59e0b | 새 일정 생성 |
| `TASK` | ✏️ | 할일 | #3b82f6 | 새 할일 생성 |
| `MEMBER` | 👤 | 멤버 | #6366f1 | 멤버 변경 |
| `KICK` | 🚫 | 강퇴 | #dc2626 | 스터디에서 강퇴됨 |
| `CHAT` | 💬 | 채팅 | #06b6d4 | 채팅 멘션 |

### 확장 타입 (코드 레벨)

| 타입 | 아이콘 | 라벨 | 설명 |
|------|--------|------|------|
| `TASK_ASSIGNED` | 📋 | 할일 배정 | 할일이 나에게 배정됨 |
| `FILE_UPLOADED` | 📤 | 파일 업로드 | 파일이 업로드됨 |
| `EVENT_CREATED` | 🗓️ | 일정 생성 | 새 일정이 생성됨 |
| `MENTION` | @ | 멘션 | 채팅에서 멘션됨 |
| `DEFAULT` | 🔔 | 알림 | 기본 알림 |

---

## 필터 상태

### 읽음 상태 필터

| 필터 | 값 | 설명 |
|------|-----|------|
| 전체 | `all` | 모든 알림 |
| 읽지 않음 | `unread` | 읽지 않은 알림만 |
| 읽음 | `read` | 읽은 알림만 |

### 날짜 그룹

| 그룹 | 키 | 라벨 |
|------|-----|------|
| 오늘 | `today` | 📅 오늘 |
| 어제 | `yesterday` | 📅 어제 |
| 이번 주 | `thisWeek` | 📅 이번 주 |
| 이전 | `older` | 📅 이전 |

---

## API 엔드포인트

| 메서드 | 경로 | 설명 | 인증 |
|--------|------|------|------|
| GET | `/api/notifications` | 알림 목록 조회 | ✅ |
| POST | `/api/notifications` | 알림 생성 (시스템용) | ✅ |
| GET | `/api/notifications/[id]` | 알림 상세 조회 | ✅ |
| DELETE | `/api/notifications/[id]` | 알림 삭제 | ✅ |
| POST | `/api/notifications/[id]/read` | 읽음 처리 | ✅ |
| POST | `/api/notifications/mark-all-read` | 전체 읽음 처리 | ✅ |
| DELETE | `/api/notifications/bulk` | 대량 삭제 | ✅ |
| GET | `/api/notifications/count` | 읽지 않은 개수 | ✅ |

---

## 데이터 플로우

### 알림 목록 조회 및 필터링

```
┌──────────┐     ┌────────────────────┐     ┌──────────────┐     ┌──────────┐
│  Client  │     │  useNotifications  │     │   API Route  │     │   DB     │
└────┬─────┘     └─────────┬──────────┘     └──────┬───────┘     └────┬─────┘
     │                     │                       │                  │
     │  페이지 로드        │                       │                  │
     │────────────────────>│                       │                  │
     │                     │                       │                  │
     │                     │ GET /api/notifications│                  │
     │                     │──────────────────────>│                  │
     │                     │                       │                  │
     │                     │                       │  세션 확인       │
     │                     │                       │─────────────────>│
     │                     │                       │                  │
     │                     │                       │  Notification    │
     │                     │                       │  .findMany       │
     │                     │                       │  (userId)        │
     │                     │                       │─────────────────>│
     │                     │                       │                  │
     │                     │  알림 목록            │                  │
     │                     │<──────────────────────│                  │
     │                     │                       │                  │
     │                     │  클라이언트 필터링    │                  │
     │                     │  (status, type)       │                  │
     │                     │                       │                  │
     │                     │  날짜별 그룹화        │                  │
     │                     │  (today, yesterday,   │                  │
     │                     │   thisWeek, older)    │                  │
     │                     │                       │                  │
     │  그룹화된 알림 렌더링│                       │                  │
     │<────────────────────│                       │                  │
     │                     │                       │                  │
```

### 알림 읽음 처리

```
┌──────────┐     ┌────────────────────┐     ┌──────────────┐     ┌──────────┐
│  Client  │     │  useNotifications  │     │   API Route  │     │   DB     │
└────┬─────┘     └─────────┬──────────┘     └──────┬───────┘     └────┬─────┘
     │                     │                       │                  │
     │  알림 클릭          │                       │                  │
     │────────────────────>│                       │                  │
     │                     │                       │                  │
     │                     │ POST /api/notif/      │                  │
     │                     │      [id]/read        │                  │
     │                     │──────────────────────>│                  │
     │                     │                       │                  │
     │                     │                       │ Notification     │
     │                     │                       │ .update          │
     │                     │                       │ (isRead: true)   │
     │                     │                       │─────────────────>│
     │                     │                       │                  │
     │                     │  성공                 │                  │
     │                     │<──────────────────────│                  │
     │                     │                       │                  │
     │                     │  Optimistic Update    │                  │
     │                     │  (로컬 상태 업데이트) │                  │
     │                     │                       │                  │
     │  링크로 이동        │                       │                  │
     │  (notification.link)│                       │                  │
     │<────────────────────│                       │                  │
     │                     │                       │                  │
```

---

## 통계 정보

### Stats 객체 구조

```javascript
{
  total: number,           // 전체 알림 수
  unreadCount: number,     // 읽지 않은 알림 수
  readCount: number,       // 읽은 알림 수
  typeCounts: {            // 타입별 개수
    JOIN_APPROVED: number,
    NOTICE: number,
    FILE: number,
    EVENT: number,
    TASK: number,
    // ...
  }
}
```

### 통계 계산 함수

```javascript
export const calculateStats = (notifications) => {
  const total = notifications.length
  const unreadCount = notifications.filter(n => !n.isRead).length
  const readCount = notifications.filter(n => n.isRead).length
  
  const typeCounts = {}
  notifications.forEach(n => {
    typeCounts[n.type] = (typeCounts[n.type] || 0) + 1
  })
  
  return { total, unreadCount, readCount, typeCounts }
}
```

---

## 관련 문서

- [API 레퍼런스](./api-reference.md) - 상세 API 명세
- [컴포넌트 가이드](./components.md) - UI 컴포넌트 문서
- [Hooks](./hooks.md) - React Hooks 문서

