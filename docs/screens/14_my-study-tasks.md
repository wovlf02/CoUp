# 12. 스터디 할 일 관리 (Study Tasks)

> **화면 ID**: `STUDY-06`  
> **라우트**: `/studies/[studyId]/tasks`  
> **레이아웃**: 좌측 네비게이션(15%) + 우측 콘텐츠(85%)  
> **렌더링**: CSR

---

## 📋 탭 콘텐츠 개요

**이 문서는 [할일] 탭의 콘텐츠 영역만 정의합니다.**

- **공통 레이아웃** (뒤로가기 버튼, 스터디 헤더, 탭 바): `07_study-detail.md` 참조
- **이 탭의 콘텐츠**: 할 일 목록 및 관리

---

## 📐 [할일] 탭 콘텐츠 영역

```
┌──────────────────────────────────────────────────────────┐
│  할 일 관리               [전체] [미완료] [완료]          │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │ [+ 할 일 추가...]                                  │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  미완료 (5개)                                            │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │ ☐ 백준 1234번 문제 풀이                            │  │
│  │   담당: 김철수 · 마감: 11/7 (D-2)      [✓] [×]    │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │ ☐ 코드 리뷰 준비                                   │  │
│  │   담당: 이영희 · 마감: 11/10 (D-5)                │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  완료 (3개)                                [모두 보기]   │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │ ☑ 알고리즘 문제 3개 풀기                           │  │
│  │   완료: 김철수 · 완료일: 11/4                      │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 🎨 섹션별 상세 설계

### 1. 페이지 헤더
```
할 일 관리                        [전체] [미완료] [완료]
```

**필터 버튼**:
- 전체: 모든 할 일
- 미완료: 완료되지 않은 할 일 (기본)
- 완료: 완료된 할 일

---

### 2. 할 일 추가 입력
```
┌─────────────────────────────────────────────────────────┐
│ [+ 할 일 추가...]                                        │
└─────────────────────────────────────────────────────────┘
```

**클릭 시 → 인라인 확장**:
```
┌─────────────────────────────────────────────────────────┐
│ 할 일 내용                                               │
│ ┌───────────────────────────────────────────────────────┐│
│ │ 백준 1234번 문제 풀이                                 ││
│ └───────────────────────────────────────────────────────┘│
│                                                          │
│ 담당자: [김철수 ▼]  마감일: [2025-11-07]                │
│ 우선순위: ( ) 높음 (•) 보통 ( ) 낮음                    │
│                                                          │
│                                    [취소]  [추가하기]    │
└─────────────────────────────────────────────────────────┘
```

---

### 3. 할 일 목록

#### 미완료 할 일
```
미완료 (5개)

┌─────────────────────────────────────────────────────────┐
│ ☐ 백준 1234번 문제 풀이                                  │
│   담당: 김철수 · 마감: 11/7 (D-2)          [✓] [수정] [×]│
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ ☐ 코드 리뷰 준비                                         │
│   담당: 이영희 · 마감: 11/10 (D-5) · 🔴 높음            │
└─────────────────────────────────────────────────────────┘
```

**할 일 카드**:
- 체크박스 (좌측)
- 할 일 내용 (text-base, gray-900)
- 메타 정보 (text-sm, gray-600)
  - 담당자 (프로필 이미지 + 이름)
  - 마감일 (상대 날짜, D-day)
  - 우선순위 (높음: 🔴, 보통: 생략, 낮음: 🔵)
- 액션 버튼 (우측, Hover 시 표시)
  - [✓] 완료
  - [수정] 수정
  - [×] 삭제

**마감일 색상**:
- D-day 또는 지남: 빨간색
- D-1 ~ D-3: 주황색
- D-4 이상: 회색

#### 완료된 할 일
```
완료 (3개)                                      [모두 보기]

┌─────────────────────────────────────────────────────────┐
│ ☑ 알고리즘 문제 3개 풀기                                 │
│   완료: 김철수 · 완료일: 11/4                            │
└─────────────────────────────────────────────────────────┘
```

**스타일**:
- 체크박스: 체크됨
- 텍스트: 취소선 (line-through), gray-500
- 최대 3개만 표시 → [모두 보기] 클릭 시 전체 표시

---

## 🎬 인터랙션

### 할 일 추가
```javascript
const handleCreate = async (data) => {
  await api.post(`/api/v1/studies/${studyId}/tasks`, {
    content: data.content,
    assigneeId: data.assigneeId,
    dueDate: data.dueDate,
    priority: data.priority
  })
  
  toast.success('할 일이 추가되었습니다!')
  setIsAdding(false)
  refetch()
}
```

### 할 일 완료 토글 (Optimistic Update)
```javascript
const toggleMutation = useMutation({
  mutationFn: ({ taskId, isCompleted }) =>
    api.patch(`/api/v1/studies/${studyId}/tasks/${taskId}`, {
      isCompleted
    }),
  
  onMutate: async ({ taskId, isCompleted }) => {
    // 즉시 UI 업데이트
    await queryClient.cancelQueries(['tasks', studyId])
    
    const previous = queryClient.getQueryData(['tasks', studyId])
    
    queryClient.setQueryData(['tasks', studyId], (old) => ({
      ...old,
      data: old.data.map(task =>
        task.id === taskId ? { ...task, isCompleted } : task
      )
    }))
    
    return { previous }
  },
  
  onError: (err, variables, context) => {
    // 에러 시 롤백
    queryClient.setQueryData(['tasks', studyId], context.previous)
  }
})
```

### 할 일 수정
- [수정] 클릭 → 인라인 편집 또는 모달

### 할 일 삭제
```javascript
const handleDelete = async (taskId) => {
  if (!confirm('할 일을 삭제하시겠습니까?')) return
  
  await api.delete(`/api/v1/studies/${studyId}/tasks/${taskId}`)
  
  toast.success('할 일이 삭제되었습니다!')
  refetch()
}
```

---

## 📱 반응형 설계

### Desktop
- 할 일 카드: 전체 너비
- 액션 버튼: Hover 시 표시

### Mobile
- 할 일 카드: 전체 너비
- 액션 버튼: 항상 표시 (아이콘만)
- 마감일: 줄임 (11/7)

---

## 🎨 스타일 코드

```css
/* 할 일 카드 */
.task-card {
  background: white;
  border: 1px solid var(--gray-200);
  border-radius: 8px;
  padding: 16px;
  display: flex;
  align-items: center;
  gap: 12px;
  transition: all 0.2s ease;
}

.task-card:hover {
  box-shadow: var(--shadow-sm);
}

.task-card.completed {
  background: var(--gray-50);
}

/* 체크박스 */
.task-checkbox {
  width: 20px;
  height: 20px;
  border: 2px solid var(--gray-300);
  border-radius: 4px;
  cursor: pointer;
}

.task-checkbox.checked {
  background: var(--primary-500);
  border-color: var(--primary-500);
}

/* 할 일 내용 */
.task-content {
  flex: 1;
}

.task-title {
  font-size: 16px;
  color: var(--gray-900);
  margin-bottom: 4px;
}

.task-title.completed {
  text-decoration: line-through;
  color: var(--gray-500);
}

.task-meta {
  font-size: 14px;
  color: var(--gray-600);
  display: flex;
  align-items: center;
  gap: 8px;
}

/* 마감일 */
.task-deadline {
  font-weight: 500;
}

.task-deadline.overdue {
  color: var(--danger-500);
}

.task-deadline.urgent {
  color: var(--warning-500);
}

/* 우선순위 */
.task-priority.high {
  color: var(--danger-500);
}

.task-priority.low {
  color: var(--info-500);
}
```

---

## ✅ 완료 체크리스트

- [ ] 할 일 목록 (필터링)
- [ ] 할 일 추가 (인라인)
- [ ] 완료 토글 (Optimistic)
- [ ] 할 일 수정
- [ ] 할 일 삭제
- [ ] 담당자 지정
- [ ] 마감일 설정
- [ ] 우선순위 설정
- [ ] D-day 계산 및 색상 표시

---

**이전 화면**: `11_study-calendar.md` (캘린더)  
**다음 화면**: `13_study-video-call.md` (화상 스터디)
