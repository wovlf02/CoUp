# 파일 관리 (Study Files)

> **화면 ID**: `STUDY-FILES-01`  
> **라우트**: `/my-studies/[studyId]/files`  
> **목적**: 스터디 파일 업로드, 관리, 공유  
> **접근 권한**: MEMBER+ (읽기/다운로드), MEMBER+ (업로드), ADMIN+ (삭제)  
> **렌더링**: CSR (실시간 업데이트)

---

## 🎯 화면 목적

**"스터디 자료를 한곳에 모아서"**
- 파일 업로드/다운로드
- 폴더 구조로 체계적 관리
- 빠른 검색 및 필터링
- 공유 링크 생성

---

## 📐 레이아웃 구조 (FHD 최적화)

```
┌─────┬──────────────────────────────────────────────────────┬──────────────────┐
│     │ [개요] [채팅] [공지] [파일] [캘린더] [할일] [화상] [설정]│                  │
│ Nav ├──────────────────────────────────────────────────────┤  파일 정보       │
│ 12% │ 📁 파일 관리                      [⬆️ 파일 업로드]    │  (280px)         │
│     │                                                      │                  │
│     │ [전체] [문서] [이미지] [압축] [기타]  🔍 [검색]      │  💾 저장 공간    │
│     ├──────────────────────────────────────────────────────┤  128MB / 2GB     │
│     │ 📂 경로: / > 과제 > 알고리즘                         │  ██████░░ 6%     │
│ 🏠 │                                                      │                  │
│ 🔍 │ ┌─ 폴더 (3) ───────────────────────────────────────┐ │  ⚡ 빠른 액션    │
│ 👥 │ │                                                  │ │  [업로드]        │
│ 📋 │ │ 📁 과제                    12개  2.5MB   [열기]  │ │  [새 폴더]       │
│ 🔔 │ │ 📁 참고자료                 8개   5.2MB   [열기]  │ │  [공유 링크]     │
│ 👤 │ │ 📁 회의록                   5개   1.8MB   [열기]  │ │                  │
│     │ │                                                  │ │  📊 통계         │
│     │ └──────────────────────────────────────────────────┘ │  전체 파일: 48개 │
│     │                                                      │  폴더: 5개       │
│     │ ┌─ 파일 (25) ──────────────────────────────────────┐ │  이번 주: 8개    │
│     │ │ [🔍] [이름▼] [크기] [업로더] [날짜▼]             │ │                  │
│     │ │                                                  │ │  📁 최근 파일    │
│     │ ├──────────────────────────────────────────────────┤ │  • 백준1234.py   │
│     │ │ [✓] 📄 알고리즘_문제집.pdf         2.5MB        │ │    1시간 전      │
│     │ │     김철수(OWNER) · 2시간 전                     │ │  • 코드리뷰.zip  │
│     │ │     ⬇ 15  [다운] [공유] [이동] [삭제]            │ │    3시간 전      │
│     │ ├──────────────────────────────────────────────────┤ │                  │
│     │ │ [✓] 🖼️ 회의_사진.jpg                 1.2MB        │ │  🔥 인기 파일    │
│     │ │     이영희(ADMIN) · 1일 전                       │ │  1. 스터디규칙   │
│     │ │     ⬇ 8   [다운] [미리보기] [공유]               │ │     (45회)       │
│     │ ├──────────────────────────────────────────────────┤ │  2. 알고리즘집   │
│     │ │ [ ] 📊 면접_준비.xlsx                 0.8MB        │ │     (32회)       │
│     │ │     박민수(MEMBER) · 3일 전                      │ │                  │
│     │ │     ⬇ 12  [다운] [공유] [이동]                   │ │  📎 파일 형식    │
│     │ ├──────────────────────────────────────────────────┤ │  PDF: 18개       │
│     │ │ [ ] 📦 코드리뷰_자료.zip             3.1MB        │ │  이미지: 12개    │
│     │ │     최지은(MEMBER) · 1주 전                      │ │  문서: 8개       │
│     │ │     ⬇ 5   [다운] [공유] [이동]                   │ │  압축: 6개       │
│     │ └──────────────────────────────────────────────────┘ │  기타: 4개       │
│     │                                                      │                  │
│     │ [더 보기] (페이지네이션 또는 무한 스크롤)            │  💡 팁           │
│     │                                                      │  드래그&드롭으로 │
│     │ 선택된 파일 (2개): [다운로드] [이동] [삭제]          │  빠른 업로드     │
│     │                                                      │                  │
└─────┴──────────────────────────────────────────────────────┴──────────────────┘
```

**레이아웃 비율**:
- 좌측 네비게이션: 12% (240px)
- 파일 목록: 58% (최대 900px)
- 우측 정보: 30% (280px)

---

## 📋 파일 업로드 영역

### 1. 드래그 앤 드롭 존

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                    ⬆️ 파일 업로드                        │
│                                                          │
│         파일을 여기에 드래그하거나 클릭하세요            │
│                                                          │
│         지원 형식: 모든 파일 (최대 50MB)                 │
│         한 번에 최대 10개까지 업로드 가능                │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**드래그 중 상태**:
```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                    📂 파일을 놓으세요                    │
│                                                          │
│                  (파란색 테두리 강조)                    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 2. 업로드 진행 상태

```
┌──────────────────────────────────────────────────────────┐
│ 📤 업로드 중... (2/5)                            [최소화]│
├──────────────────────────────────────────────────────────┤
│                                                          │
│ ✅ 알고리즘_문제집.pdf (2.5MB)                          │
│ ✅ 회의_사진.jpg (1.2MB)                                 │
│                                                          │
│ ⏳ 코드리뷰.zip (3.1MB)                                  │
│ ████████████░░░░░░░░ 65%                                │
│                                                          │
│ ⏳ 참고자료.docx (0.8MB)                                 │
│ ░░░░░░░░░░░░░░░░░░░░ 대기 중                            │
│                                                          │
│ ⏳ 과제.pdf (1.5MB)                                      │
│ ░░░░░░░░░░░░░░░░░░░░ 대기 중                            │
│                                                          │
│                                    [취소]                │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**완료 알림**:
```
┌──────────────────────────────────────────────────────────┐
│ ✅ 업로드 완료!                                   [닫기]│
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 5개 파일이 성공적으로 업로드되었습니다.                  │
│ 총 크기: 9.1MB                                           │
│                                                          │
│ [파일 목록 보기]                                          │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 📂 폴더 관리

### 1. 새 폴더 생성 모달

```
┌──────────────────────────────────────────────────────────┐
│ 📁 새 폴더 만들기                                  [닫기]│
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 현재 위치: / > 과제                                       │
│                                                          │
│ 폴더 이름 *                                               │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 알고리즘                                             │ │
│ └──────────────────────────────────────────────────────┘ │
│ 2-50자 (특수문자 제한)                                    │
│                                                          │
│ 설명 (선택)                                               │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 알고리즘 문제 풀이 및 해설 자료                      │ │
│ └──────────────────────────────────────────────────────┘ │
│                                                          │
│                              [취소]  [만들기]            │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 2. 폴더 구조 네비게이션

```
┌──────────────────────────────────────────────────────────┐
│ 📂 경로: / > 과제 > 알고리즘 > 백준                      │
│          ↑   ↑      ↑           ↑                       │
│       (클릭하여 해당 폴더로 이동)                         │
└──────────────────────────────────────────────────────────┘
```

**브레드크럼 네비게이션**:
- 각 단계 클릭 시 해당 폴더로 이동
- `/` 클릭 시 루트로 이동

---

## 🗂️ 파일 목록 테이블

### 1. 테이블 헤더 (정렬 가능)

```
┌──────────────────────────────────────────────────────────┐
│ [🔍] [이름 ▼] [크기] [업로더] [날짜 ▼]  [액션]           │
└──────────────────────────────────────────────────────────┘
```

**정렬 옵션**:
- **이름**: 가나다순/역순
- **크기**: 큰 순/작은 순
- **날짜**: 최신순/오래된순
- **다운로드**: 많은 순

### 2. 파일 행

```
┌──────────────────────────────────────────────────────────┐
│ [✓] 📄 알고리즘_문제집.pdf                      2.5MB   │
│     김철수(OWNER) · 2025.11.05 14:23                    │
│     ⬇ 15회 다운로드                                     │
│     [다운로드] [공유] [이동] [이름 변경] [삭제]          │
└──────────────────────────────────────────────────────────┘
```

**파일 타입별 아이콘**:
- 📄 PDF
- 📊 스프레드시트 (xlsx, csv)
- 📝 문서 (docx, txt)
- 🖼️ 이미지 (jpg, png, gif)
- 📦 압축 (zip, rar)
- 🎬 비디오 (mp4, avi)
- 🎵 오디오 (mp3, wav)
- 💻 코드 (py, js, java)

### 3. 다중 선택 액션

```
┌──────────────────────────────────────────────────────────┐
│ 선택된 파일 (3개): [일괄 다운로드] [이동] [삭제]         │
└──────────────────────────────────────────────────────────┘
```

---

## 🔍 검색 및 필터

### 1. 검색

```javascript
const handleSearch = (keyword) => {
  const results = files.filter(file => 
    file.name.toLowerCase().includes(keyword.toLowerCase()) ||
    file.uploader.name.toLowerCase().includes(keyword.toLowerCase())
  )
  setFilteredFiles(results)
}
```

### 2. 파일 타입 필터

```
┌──────────────────────────────────────────────────────────┐
│ [전체 48] [문서 18] [이미지 12] [압축 6] [기타 12]       │
└──────────────────────────────────────────────────────────┘
```

### 3. 고급 필터 모달

```
┌──────────────────────────────────────────────────────────┐
│ 🔍 고급 필터                                       [닫기]│
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 파일 형식                                                 │
│ [✓] 문서  [✓] 이미지  [ ] 압축  [ ] 기타                │
│                                                          │
│ 업로더                                                    │
│ [ ] 전체  [✓] OWNER  [✓] ADMIN  [ ] MEMBER              │
│                                                          │
│ 크기                                                      │
│ [ ] 1MB 이하  [✓] 1-10MB  [ ] 10MB 이상                 │
│                                                          │
│ 업로드 날짜                                               │
│ ( ) 전체  ( ) 오늘  (?) 이번 주  ( ) 이번 달            │
│                                                          │
│                          [초기화]  [적용]                │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 📥 파일 다운로드

### 1. 단일 파일 다운로드

```javascript
const handleDownload = async (fileId, fileName) => {
  try {
    setDownloading(true)
    
    const response = await api.get(
      `/api/v1/my-studies/${studyId}/files/${fileId}/download`,
      { responseType: 'blob' }
    )
    
    // 파일 다운로드
    const url = window.URL.createObjectURL(new Blob([response.data]))
    const link = document.createElement('a')
    link.href = url
    link.setAttribute('download', fileName)
    document.body.appendChild(link)
    link.click()
    link.remove()
    
    // 다운로드 수 증가
    await api.post(`/api/v1/my-studies/${studyId}/files/${fileId}/increment-download`)
    
    toast.success('다운로드가 시작되었습니다')
  } catch (error) {
    toast.error('다운로드 중 오류가 발생했습니다')
  } finally {
    setDownloading(false)
  }
}
```

### 2. 일괄 다운로드 (ZIP)

```javascript
const handleBulkDownload = async (fileIds) => {
  if (fileIds.length === 0) {
    toast.error('다운로드할 파일을 선택해주세요')
    return
  }
  
  try {
    setDownloading(true)
    
    toast.info('파일을 압축하고 있습니다...')
    
    // 서버에서 ZIP 생성
    const response = await api.post(
      `/api/v1/my-studies/${studyId}/files/download-bulk`,
      { fileIds },
      { responseType: 'blob' }
    )
    
    // ZIP 다운로드
    const url = window.URL.createObjectURL(new Blob([response.data]))
    const link = document.createElement('a')
    link.href = url
    link.setAttribute('download', `files_${Date.now()}.zip`)
    document.body.appendChild(link)
    link.click()
    link.remove()
    
    toast.success(`${fileIds.length}개 파일이 다운로드되었습니다`)
  } catch (error) {
    toast.error('일괄 다운로드 중 오류가 발생했습니다')
  } finally {
    setDownloading(false)
  }
}
```

---

## 🔗 공유 링크 생성

```
┌──────────────────────────────────────────────────────────┐
│ 🔗 공유 링크 생성                                  [닫기]│
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 파일: 알고리즘_문제집.pdf (2.5MB)                       │
│                                                          │
│ 공유 링크가 생성되었습니다                                │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ https://coup.com/files/share/abc123xyz              │ │
│ │                                          [복사]      │ │
│ └──────────────────────────────────────────────────────┘ │
│                                                          │
│ 옵션                                                      │
│ [ ] 비밀번호 설정                                         │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ ••••••••                                            │ │
│ └──────────────────────────────────────────────────────┘ │
│                                                          │
│ 만료 기간                                                 │
│ (?) 7일  ( ) 30일  ( ) 무제한                            │
│                                                          │
│ 다운로드 제한                                             │
│ (?) 무제한  ( ) 10회  ( ) 50회  ( ) 100회               │
│                                                          │
│                [링크 복사]  [카카오톡 공유]  [닫기]      │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 🖼️ 파일 미리보기

### 이미지 미리보기

```
┌──────────────────────────────────────────────────────────┐
│ ← 목록   회의_사진.jpg (1.2MB)              [다운로드]   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                                                          │
│                  [이미지 표시 영역]                      │
│                                                          │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ 업로더: 이영희(ADMIN) · 2025.11.04 10:30                │
│ 크기: 1920x1080 · 1.2MB                                  │
│ 다운로드: 8회                                            │
│                                                          │
│ [← 이전] [확대] [축소] [원본 크기] [다음 →]              │
└──────────────────────────────────────────────────────────┘
```

### PDF 미리보기

```
┌──────────────────────────────────────────────────────────┐
│ ← 목록   알고리즘_문제집.pdf (2.5MB)        [다운로드]   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│               [PDF 뷰어 - 1/15 페이지]                   │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ [← 이전] [1 / 15] [다음 →] [확대] [축소] [전체화면]     │
└──────────────────────────────────────────────────────────┘
```

---

## 🎨 우측 정보 위젯

### 1️⃣ 저장 공간

```
┌─────────────────────────────────────┐
│ 💾 저장 공간                         │
│                                     │
│ 128MB / 2GB 사용 중                  │
│ ██████░░░░░░░░░░░░░░ 6%             │
│                                     │
│ 남은 공간: 1.87GB                    │
│                                     │
│ [용량 관리 →]                        │
└─────────────────────────────────────┘
```

### 2️⃣ 빠른 액션

```
┌─────────────────────────────────────┐
│ ⚡ 빠른 액션                         │
│                                     │
│ [⬆️ 파일 업로드]                    │
│ [📁 새 폴더 만들기]                  │
│ [🔗 공유 링크 관리]                  │
│ [🗑️ 휴지통 보기]                     │
└─────────────────────────────────────┘
```

### 3️⃣ 파일 통계

```
┌─────────────────────────────────────┐
│ 📊 파일 통계                         │
│                                     │
│ 전체 파일: 48개                      │
│ 폴더: 5개                           │
│                                     │
│ 이번 주 업로드: 8개                  │
│ 총 다운로드: 342회                   │
│                                     │
│ 파일 형식 분포:                      │
│ • PDF: 18개 (37.5%)                 │
│ • 이미지: 12개 (25%)                │
│ • 문서: 8개 (16.7%)                 │
│ • 압축: 6개 (12.5%)                 │
│ • 기타: 4개 (8.3%)                  │
└─────────────────────────────────────┘
```

### 4️⃣ 최근 파일

```
┌─────────────────────────────────────┐
│ 📁 최근 업로드 (5)                   │
│                                     │
│ 📄 백준1234.py                       │
│ 김철수 · 1시간 전                    │
│                                     │
│ 📦 코드리뷰.zip                      │
│ 이영희 · 3시간 전                    │
│                                     │
│ 📊 분석결과.xlsx                     │
│ 박민수 · 1일 전                      │
│                                     │
│ [전체 보기 →]                        │
└─────────────────────────────────────┘
```

### 5️⃣ 인기 파일

```
┌─────────────────────────────────────┐
│ 🔥 가장 많이 다운로드                │
│                                     │
│ 1. 📄 스터디_규칙.pdf                │
│    45회 다운로드                     │
│                                     │
│ 2. 📄 알고리즘_문제집.pdf            │
│    32회 다운로드                     │
│                                     │
│ 3. 📊 면접_준비.xlsx                 │
│    28회 다운로드                     │
└─────────────────────────────────────┘
```

### 6️⃣ 사용 팁

```
┌─────────────────────────────────────┐
│ 💡 파일 관리 팁                      │
│                                     │
│ • 드래그&드롭으로 빠른 업로드        │
│ • 폴더로 체계적 관리                 │
│ • 공유 링크로 외부 공유              │
│ • 검색으로 빠른 찾기                 │
│                                     │
│ [사용 가이드 보기 →]                 │
└─────────────────────────────────────┘
```

---

## 🔧 인터랙션 플로우

### 1. 파일 업로드

```javascript
const handleUpload = async (files) => {
  // 검증
  if (files.length > 10) {
    toast.error('한 번에 최대 10개까지 업로드 가능합니다')
    return
  }
  
  const oversizedFiles = files.filter(f => f.size > 50 * 1024 * 1024)
  if (oversizedFiles.length > 0) {
    toast.error('50MB 이하 파일만 업로드 가능합니다')
    return
  }
  
  // 저장 공간 확인
  const totalSize = files.reduce((sum, f) => sum + f.size, 0)
  if (storageUsed + totalSize > storageLimit) {
    toast.error('저장 공간이 부족합니다')
    return
  }
  
  try {
    setUploading(true)
    
    const formData = new FormData()
    files.forEach(file => formData.append('files', file))
    formData.append('folderId', currentFolderId || '')
    
    // 업로드 진행률 추적
    const response = await api.post(
      `/api/v1/my-studies/${studyId}/files/upload`,
      formData,
      {
        headers: { 'Content-Type': 'multipart/form-data' },
        onUploadProgress: (progressEvent) => {
          const percentCompleted = Math.round(
            (progressEvent.loaded * 100) / progressEvent.total
          )
          setUploadProgress(percentCompleted)
        }
      }
    )
    
    toast.success(`${files.length}개 파일이 업로드되었습니다`)
    
    // 목록 새로고침
    queryClient.invalidateQueries(['files', studyId])
  } catch (error) {
    toast.error('업로드 중 오류가 발생했습니다')
  } finally {
    setUploading(false)
    setUploadProgress(0)
  }
}
```

### 2. 폴더 생성

```javascript
const handleCreateFolder = async (folderName, description) => {
  if (!folderName || folderName.length < 2) {
    toast.error('폴더 이름은 2자 이상 입력해주세요')
    return
  }
  
  // 특수문자 검증
  if (!/^[가-힣a-zA-Z0-9\s_-]+$/.test(folderName)) {
    toast.error('폴더 이름에 특수문자는 사용할 수 없습니다')
    return
  }
  
  try {
    await api.post(`/api/v1/my-studies/${studyId}/folders`, {
      name: folderName,
      description,
      parentId: currentFolderId || null
    })
    
    toast.success('폴더가 생성되었습니다')
    
    queryClient.invalidateQueries(['files', studyId])
  } catch (error) {
    toast.error('폴더 생성 중 오류가 발생했습니다')
  }
}
```

### 3. 파일 이동

```javascript
const handleMoveFiles = async (fileIds, targetFolderId) => {
  try {
    await api.patch(`/api/v1/my-studies/${studyId}/files/move`, {
      fileIds,
      targetFolderId
    })
    
    toast.success(`${fileIds.length}개 파일이 이동되었습니다`)
    
    queryClient.invalidateQueries(['files', studyId])
  } catch (error) {
    toast.error('파일 이동 중 오류가 발생했습니다')
  }
}
```

### 4. 파일 삭제

```javascript
const handleDeleteFiles = async (fileIds) => {
  const confirmed = confirm(
    `${fileIds.length}개 파일을 삭제하시겠습니까?\n` +
    '삭제된 파일은 휴지통으로 이동됩니다.'
  )
  
  if (!confirmed) return
  
  try {
    await api.delete(`/api/v1/my-studies/${studyId}/files`, {
      data: { fileIds }
    })
    
    toast.success(`${fileIds.length}개 파일이 삭제되었습니다`)
    
    queryClient.invalidateQueries(['files', studyId])
  } catch (error) {
    toast.error('파일 삭제 중 오류가 발생했습니다')
  }
}
```

---

## 📱 반응형 설계

### Desktop (1920px)
```css
.files-layout {
  display: grid;
  grid-template-columns: 240px 1fr 280px;
  gap: 20px;
}

.file-list {
  max-width: 900px;
}
```

### Tablet (1024px)
```css
.files-layout {
  grid-template-columns: 60px 1fr 240px;
}

/* 파일 행을 카드 형태로 */
.file-item {
  display: flex;
  flex-direction: column;
}
```

### Mobile (<768px)
```css
.files-layout {
  display: flex;
  flex-direction: column;
}

.file-info-widgets {
  display: none; /* 또는 하단 배치 */
}

/* 그리드 뷰로 변경 */
.file-list {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}

.file-card {
  text-align: center;
  padding: 16px;
}
```

---

## ✅ 구현 체크리스트

### Phase 1: 기본 구조
- [ ] 파일 목록 레이아웃
- [ ] 폴더 구조 네비게이션
- [ ] 파일 타입 필터
- [ ] 검색 기능

### Phase 2: 업로드
- [ ] 드래그 앤 드롭 업로드
- [ ] 파일 선택 업로드
- [ ] 업로드 진행률 표시
- [ ] 다중 파일 업로드

### Phase 3: 다운로드
- [ ] 단일 파일 다운로드
- [ ] 일괄 다운로드 (ZIP)
- [ ] 다운로드 수 카운트
- [ ] 공유 링크 생성

### Phase 4: 폴더 관리
- [ ] 폴더 생성/삭제
- [ ] 폴더 이동
- [ ] 브레드크럼 네비게이션
- [ ] 폴더 구조 트리

### Phase 5: 파일 작업
- [ ] 파일 이름 변경
- [ ] 파일 이동
- [ ] 파일 삭제 (휴지통)
- [ ] 다중 선택

### Phase 6: 미리보기
- [ ] 이미지 미리보기
- [ ] PDF 뷰어
- [ ] 텍스트 파일 뷰어
- [ ] 비디오/오디오 플레이어

---

## 🎯 UX 최적화 포인트

1. **드래그 앤 드롭**
   - 직관적인 업로드 경험
   - 시각적 피드백 (테두리 강조)

2. **진행률 표시**
   - 업로드 진행 상황 실시간 표시
   - 취소 옵션 제공

3. **폴더 구조**
   - 브레드크럼으로 현재 위치 명확히
   - 빠른 이동

4. **미리보기**
   - 다운로드 전 내용 확인
   - 이미지/PDF 바로 보기

5. **저장 공간 관리**
   - 사용량 시각화
   - 용량 초과 방지

6. **공유 기능**
   - 링크로 외부 공유
   - 만료 기간/다운로드 제한

---

**이전 화면**: `08_study-notices.md` (공지사항)  
**다음 화면**: `10_study-calendar.md` (캘린더)
