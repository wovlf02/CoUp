# 스터디 분석 대시보드 (Study Analytics Dashboard)

> **화면 ID**: `STUDY-ANALYTICS-01`  
> **라우트**: `/my-studies/[studyId]/analytics`  
> **목적**: 스터디 운영 및 멤버 활동 데이터 분석  
> **접근 권한**: OWNER, ADMIN  
> **렌더링**: CSR

---

## 🎯 화면 목적

**"데이터 기반 스터디 운영"**
- 스터디 전체 활동 추이 분석
- 멤버별 참여도 및 기여도 파악
- 목표 달성률 모니터링
- 인사이트 기반 개선점 도출
- 데이터 기반 의사결정 지원

---

## 📐 레이아웃 구조 (FHD 최적화)

```
┌─────┬──────────────────────────────────────────────────────────┬──────────────────┐
│     │ [개요] [채팅] [공지] [파일] [캘린더] [할일] [화상] [분석]│                  │
│ Nav ├──────────────────────────────────────────────────────────┤  빠른 인사이트   │
│ 12% │ 📊 스터디 분석                          [📥 리포트 다운로드]│  (30%)           │
│     │                                                          │                  │
│     │ [전체 기간] [이번 달] [이번 주] [사용자 정의]             │ 📈 핵심 지표     │
│     ├──────────────────────────────────────────────────────────┤ 평균 출석률      │
│     │                                                          │ 🟢 85%          │
│     │ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────┐ │                  │
│ 👥 │ │ 📊 총 활동  │ │ 👥 활성    │ │ ✅ 할일    │ │ 📁 파일│ │ 할일 완료율      │
│ 🔍 │ │            │ │   멤버     │ │   완료율   │ │  공유  │ │ 🟡 78%          │
│ 👥←│ │   2,847    │ │  10/12명  │ │    78%    │ │  45개 │ │                  │
│ 📋 │ │ ▲ 12%      │ │ ▲ 2명     │ │ ▼ 5%      │ │ ▲ 8개 │ │ 채팅 활동        │
│ 🔔 │ └────────────┘ └────────────┘ └────────────┘ └────────┘ │ 🟢 매우 활발     │
│ 👤 │                                                          │                  │
│     │ ┌──────────────────────────────────────────────────────┐ │ ⚠️ 주의 필요    │
│     │ │ 📈 일별 활동 추이                     [꺾은선] [막대]│ │ • 3명 출석률 ↓  │
│     │ │                                                      │ │ • 할일 지연 5건 │
│     │ │  100 ┤         ●─────●                              │ │                  │
│     │ │      │     ●──●       ●──●                          │ │ 💡 추천 액션    │
│     │ │   50 │  ●─●               ●                         │ │ 출석 독려 메시지 │
│     │ │      │●●                                            │ │ [보내기]         │
│     │ │    0 └┴─┴─┴─┴─┴─┴─┴─                               │ │                  │
│     │ │      월 화 수 목 금 토 일                             │ │ 📅 다음 목표    │
│     │ │                                                      │ │ 출석률 90% 달성  │
│     │ │ 📊 채팅  📢 공지  📁 파일  ✅ 할일  🎥 화상         │ │ (현재: 85%)      │
│     │ └──────────────────────────────────────────────────────┘ │                  │
│     │                                                          │                  │
│     │ ┌───────────────────┐ ┌────────────────────────────────┐ │                  │
│     │ │ 👥 멤버별 활동 순위│ │ 📊 활동 유형별 분포            │ │                  │
│     │ │                   │ │                                │ │                  │
│     │ │ 🥇 김철수         │ │ [도넛 차트]                    │ │                  │
│     │ │ 92/100 ⭐⭐⭐    │ │                                │ │                  │
│     │ │                   │ │ 채팅 40%                       │ │                  │
│     │ │ 🥈 이영희         │ │ 할일 25%                       │ │                  │
│     │ │ 88/100 ⭐⭐☆    │ │ 파일 20%                       │ │                  │
│     │ │                   │ │ 공지 10%                       │ │                  │
│     │ │ 🥉 박민수         │ │ 기타 5%                        │ │                  │
│     │ │ 85/100 ⭐⭐☆    │ │                                │ │                  │
│     │ │                   │ │                                │ │                  │
│     │ │ [전체 순위 →]    │ │                                │ │                  │
│     │ └───────────────────┘ └────────────────────────────────┘ │                  │
│     │                                                          │                  │
└─────┴──────────────────────────────────────────────────────────┴──────────────────┘
```

**레이아웃 비율** (해상도별 자동 조정):

### 🖥️ FHD (1920px) - 기본 기준
- 좌측 네비게이션: **12%** (min: 200px, max: 240px)
- 메인 분석 영역: **58%** (min: 900px, max: 1200px)
- 우측 인사이트: **30%** (min: 260px, max: 320px)
- 갭(여백): **2%**

### 🖥️ QHD (2560px) - 고해상도
- 좌측 네비게이션: **10%** (min: 240px, max: 280px)
- 메인 분석 영역: **60%** (min: 1200px, max: 1600px)
- 우측 인사이트: **28%** (min: 320px, max: 400px)
- 갭(여백): **2%**

### 🖥️ 4K (3840px) - 초고해상도
- 좌측 네비게이션: **8%** (min: 280px, max: 320px)
- 메인 분석 영역: **62%** (min: 1600px, max: 2200px)
- 우측 인사이트: **28%** (min: 400px, max: 500px)
- 갭(여백): **2%**

### 📱 반응형 브레이크포인트
- **Desktop Small (1440px)**: 15% / 55% / 28%
- **Tablet (1024px)**: 5% / 65% / 28%
- **Mobile (<768px)**: 100% 단일 컬럼

---

## 📐 CSS Grid 구현 예시

```css
/* 글로벌 CSS 변수 정의 */
:root {
  /* FHD 기본값 */
  --nav-width: 12%;
  --content-width: 58%;
  --widget-width: 30%;
  --gap-size: 2%;
  
  /* 최소/최대 제약 */
  --nav-min: 200px;
  --nav-max: 240px;
  --content-min: 900px;
  --content-max: 1200px;
  --widget-min: 260px;
  --widget-max: 320px;
}

/* QHD 해상도 (2560px 이상) */
@media (min-width: 2560px) {
  :root {
    --nav-width: 10%;
    --content-width: 60%;
    --widget-width: 28%;
    --nav-min: 240px;
    --nav-max: 280px;
    --content-min: 1200px;
    --content-max: 1600px;
    --widget-min: 320px;
    --widget-max: 400px;
  }
}

/* 4K 해상도 (3840px 이상) */
@media (min-width: 3840px) {
  :root {
    --nav-width: 8%;
    --content-width: 62%;
    --widget-width: 28%;
    --nav-min: 280px;
    --nav-max: 320px;
    --content-min: 1600px;
    --content-max: 2200px;
    --widget-min: 400px;
    --widget-max: 500px;
  }
}

/* Desktop Small */
@media (max-width: 1680px) {
  :root {
    --nav-width: 15%;
    --content-width: 55%;
    --widget-width: 28%;
  }
}

/* Tablet */
@media (max-width: 1279px) {
  :root {
    --nav-width: 5%;
    --content-width: 65%;
    --widget-width: 28%;
    --nav-min: 60px;
    --nav-max: 80px;
  }
}

/* 메인 레이아웃 */
.analytics-layout {
  display: grid;
  grid-template-columns: 
    minmax(var(--nav-min), var(--nav-width))
    minmax(var(--content-min), var(--content-width))
    minmax(var(--widget-min), var(--widget-width));
  gap: var(--gap-size);
  width: 100%;
  max-width: 100vw;
  padding: 0 1%;
}

/* 차트 그리드 */
.charts-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 2%;
}

@media (max-width: 1024px) {
  .charts-grid {
    grid-template-columns: 1fr;
  }
}

/* Mobile */
@media (max-width: 767px) {
  .analytics-layout {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  
  .insights-widgets {
    order: 3;
    margin-top: 24px;
    width: 100%;
  }
}
```

---

## 🎯 우측 빠른 인사이트 위젯 (30% - 해상도별 자동 조정)

### 1️⃣ 핵심 지표 요약

```
┌─────────────────────────────────┐
│ 📈 핵심 지표                    │
│                                 │
│ 평균 출석률                     │
│ 🟢 85%                         │
│ (목표: 90%)                     │
│ ████████████████░░░░           │
│                                 │
│ 할일 완료율                     │
│ 🟡 78%                         │
│ (목표: 85%)                     │
│ ███████████████░░░░░           │
│                                 │
│ 채팅 활동                       │
│ 🟢 매우 활발                   │
│ (일평균 35.4개)                 │
│                                 │
│ 파일 공유                       │
│ 🟢 활발                        │
│ (주평균 6.4개)                  │
└─────────────────────────────────┘
```

---

### 2️⃣ 주의 필요 알림

```
┌─────────────────────────────────┐
│ ⚠️ 주의 필요                    │
│                                 │
│ 출석률 하락 멤버 (3명)           │
│ • 최지은: 65% (▼ 15%)          │
│ • 정소현: 58% (▼ 22%)          │
│ • 강동욱: 55% (▼ 25%)          │
│ [독려 메시지 보내기]            │
│                                 │
│ 지연된 할일 (5건)                │
│ • 백준 1234번 (3일 지연)        │
│ • 코드 리뷰 (2일 지연)          │
│ [리마인더 보내기]               │
│                                 │
│ 미확인 공지 (2건)                │
│ • "이번 주 일정 변경" (3명)     │
│ [알림 재전송]                   │
└─────────────────────────────────┘
```

---

### 3️⃣ AI 추천 액션

```
┌─────────────────────────────────┐
│ 💡 AI 추천 액션                 │
│                                 │
│ 🎯 출석 독려                    │
│ "3명의 출석률이 떨어지고        │
│  있습니다. 격려 메시지를        │
│  보내보세요."                   │
│ [메시지 작성]                   │
│                                 │
│ 📊 활동 정규화                  │
│ "월요일과 금요일 활동이         │
│  저조합니다. 정기 이벤트를      │
│  계획해보세요."                 │
│ [일정 추가]                     │
│                                 │
│ 🎉 멤버 칭찬                    │
│ "김철수님이 3주 연속 1위        │
│  입니다. 칭찬해주세요!"         │
│ [칭찬 보내기]                   │
└─────────────────────────────────┘
```

---

### 4️⃣ 다음 목표

```
┌─────────────────────────────────┐
│ 📅 다음 목표                    │
│                                 │
│ 출석률 90% 달성                 │
│ 현재: 85%                       │
│ 목표까지: +5%                   │
│ ████████████████░░░░           │
│ 예상 달성일: 11/20              │
│                                 │
│ 할일 완료율 85%                 │
│ 현재: 78%                       │
│ 목표까지: +7%                   │
│ ██████████████░░░░░░           │
│ 예상 달성일: 11/25              │
│                                 │
│ [목표 설정하기]                 │
└─────────────────────────────────┘
```

---

### 5️⃣ 스터디 성장

```
┌─────────────────────────────────┐
│ 📈 스터디 성장                  │
│                                 │
│ 이번 달 vs 지난 달              │
│                                 │
│ 활동량    ▲ 12% 증가           │
│ 출석률    ▲ 3% 증가            │
│ 할일 완료 ▼ 5% 감소            │
│ 파일 공유 ▲ 8% 증가            │
│                                 │
│ 🎯 종합 평가                    │
│ 전반적으로 긍정적입니다!        │
│ 할일 관리에 더 집중해보세요.    │
└─────────────────────────────────┘
```

---

## 📥 리포트 다운로드

### PDF 리포트 구성

```
[표지]
- 스터디명
- 분석 기간
- 생성일

[요약]
- 핵심 지표 4개
- 전체 평가
- 주요 성과

[상세 분석]
1. 활동 추이 그래프
2. 멤버별 순위
3. 활동 유형별 분포
4. 출석률 히트맵
5. 시간대별 패턴

[멤버 리스트]
- 전체 멤버 상세 통계
- 개인별 활동 점수
- 기여도 순위

[개선 제안]
- AI 분석 기반 추천
- 목표 설정 가이드
```

### Excel 리포트

```
[시트 1: 요약]
- 핵심 지표
- 기간별 비교

[시트 2: 멤버별 통계]
- 전체 멤버 데이터
- 출석, 채팅, 할일 등

[시트 3: 일별 활동]
- 날짜별 활동 수
- 유형별 분류

[시트 4: 원본 데이터]
- 가공되지 않은 로우 데이터
```

---

## 🎨 그래프 라이브러리

### Recharts 사용 예시

```javascript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts'

const ActivityChart = ({ data }) => {
  return (
    <LineChart width={800} height={300} data={data}>
      <CartesianGrid strokeDasharray="3 3" />
      <XAxis dataKey="date" />
      <YAxis />
      <Tooltip />
      <Legend />
      <Line type="monotone" dataKey="chat" stroke="#6366F1" strokeWidth={2} />
      <Line type="monotone" dataKey="task" stroke="#10B981" strokeWidth={2} />
      <Line type="monotone" dataKey="file" stroke="#F59E0B" strokeWidth={2} />
    </LineChart>
  )
}
```

---

## 📊 데이터 수집 및 처리

### 실시간 데이터 집계

```javascript
// 활동 이벤트 수집
const trackActivity = async (type, data) => {
  await prisma.studyActivity.create({
    data: {
      studyId,
      userId,
      type,  // 'CHAT', 'TASK', 'FILE', 'NOTICE' 등
      metadata: data,
      createdAt: new Date()
    }
  })
  
  // Redis에 실시간 카운터 업데이트
  await redis.incr(`study:${studyId}:activity:${type}:count`)
}

// 일별 집계 (크론잡)
const aggregateDailyStats = async (studyId, date) => {
  const activities = await prisma.studyActivity.findMany({
    where: {
      studyId,
      createdAt: {
        gte: startOfDay(date),
        lte: endOfDay(date)
      }
    }
  })
  
  const stats = {
    date,
    chatCount: activities.filter(a => a.type === 'CHAT').length,
    taskCount: activities.filter(a => a.type === 'TASK').length,
    fileCount: activities.filter(a => a.type === 'FILE').length,
    noticeCount: activities.filter(a => a.type === 'NOTICE').length,
    activeMembers: new Set(activities.map(a => a.userId)).size
  }
  
  await prisma.studyDailyStats.create({ data: stats })
}
```

---

## 🔔 자동 알림

### 이상 패턴 감지

```javascript
const detectAnomalies = async (studyId) => {
  const recentStats = await getRecentStats(studyId, 7) // 최근 7일
  const previousStats = await getRecentStats(studyId, 14, 7) // 그 전 7일
  
  // 출석률 급감 감지
  if (recentStats.attendance < previousStats.attendance * 0.8) {
    await sendAlert({
      type: 'ATTENDANCE_DROP',
      severity: 'HIGH',
      message: '출석률이 20% 이상 감소했습니다'
    })
  }
  
  // 비활성 멤버 감지
  const inactiveMembers = await findInactiveMembers(studyId, 7)
  if (inactiveMembers.length > 0) {
    await sendAlert({
      type: 'INACTIVE_MEMBERS',
      severity: 'MEDIUM',
      members: inactiveMembers
    })
  }
}
```

---

## 📱 반응형 설계

### Desktop (1920px)
```css
.analytics-layout {
  display: grid;
  grid-template-columns: 240px 1fr 280px;
  gap: 20px;
}

.charts-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
}
```

### Tablet (1024px)
```css
.analytics-layout {
  grid-template-columns: 60px 1fr 240px;
}

.charts-grid {
  grid-template-columns: 1fr;
}
```

### Mobile (<768px)
```css
.analytics-layout {
  display: flex;
  flex-direction: column;
}

.metric-cards {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}

.insights-widgets {
  order: 3;
  margin-top: 24px;
}
```

---

## ✅ 구현 체크리스트

### Phase 1: 기본 구조
- [ ] 레이아웃 및 네비게이션
- [ ] 기간 선택 필터
- [ ] 핵심 지표 카드 (4개)

### Phase 2: 그래프 및 차트
- [ ] 일별 활동 추이 (꺾은선)
- [ ] 활동 유형별 분포 (도넛)
- [ ] 출석률 히트맵
- [ ] 시간대별 패턴 (막대)

### Phase 3: 멤버 분석
- [ ] 멤버별 활동 순위
- [ ] 활동 점수 계산 로직
- [ ] 개인별 상세 통계

### Phase 4: 인사이트
- [ ] 주의 필요 알림
- [ ] AI 추천 액션
- [ ] 다음 목표 트래킹
- [ ] 스터디 성장 비교

### Phase 5: 데이터 처리
- [ ] 실시간 활동 트래킹
- [ ] 일별 집계 크론잡
- [ ] Redis 캐싱
- [ ] 이상 패턴 감지

### Phase 6: 리포트
- [ ] PDF 리포트 생성
- [ ] Excel 리포트 생성
- [ ] 이메일 전송 기능

### Phase 7: 최적화
- [ ] 대용량 데이터 페이징
- [ ] 그래프 렌더링 최적화
- [ ] 실시간 업데이트 (WebSocket)
- [ ] 반응형 디자인

---

## 🎯 UX 최적화 포인트

1. **직관적인 시각화**
   - 복잡한 데이터를 쉽게 이해
   - 색상 코딩으로 빠른 파악
   - 인터랙티브 차트

2. **실용적인 인사이트**
   - AI 기반 분석 및 추천
   - 즉시 실행 가능한 액션
   - 목표 대비 진행 상황

3. **문제 조기 발견**
   - 자동 이상 패턴 감지
   - 주의 필요 항목 강조
   - 예방적 알림

4. **동기부여**
   - 성장 추이 시각화
   - 멤버 간 선의의 경쟁
   - 목표 달성 축하

5. **데이터 기반 의사결정**
   - 객관적인 지표
   - 기간별 비교
   - 트렌드 분석

6. **접근성**
   - 관리자만 접근
   - 민감 정보 보호
   - 리포트 공유 옵션

---

## 🔒 권한 및 보안

### 접근 권한
- OWNER: 전체 분석 접근
- ADMIN: 전체 분석 접근
- MEMBER: 접근 불가 (자신의 통계만)

### 데이터 보호
- 개인 식별 정보 익명화 옵션
- 민감한 채팅 내용 제외
- GDPR 준수

---

## 📈 성능 최적화

### 캐싱 전략
```javascript
// Redis 캐싱
const getCachedStats = async (studyId, period) => {
  const cacheKey = `study:${studyId}:stats:${period}`
  const cached = await redis.get(cacheKey)
  
  if (cached) return JSON.parse(cached)
  
  const stats = await calculateStats(studyId, period)
  await redis.setex(cacheKey, 300, JSON.stringify(stats)) // 5분 캐시
  
  return stats
}
```

### 쿼리 최적화
```javascript
// 집계 쿼리 최적화
const getActivityStats = await prisma.studyActivity.groupBy({
  by: ['type', 'date'],
  where: {
    studyId,
    createdAt: { gte: startDate, lte: endDate }
  },
  _count: true,
  orderBy: { date: 'asc' }
})
```

