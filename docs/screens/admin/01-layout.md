# 관리자 레이아웃 설계

> **작성일**: 2025-11-26

---

## 📋 개요

관리자 시스템의 전체 레이아웃, 네비게이션, 헤더를 정의합니다.

---

## 🎨 전체 레이아웃

```
┌──────────────────────────────────────────────────────────┐
│  AdminHeader (Fixed Top, 64px)                           │
├────────┬─────────────────────────────────────────────────┤
│        │                                                  │
│ Admin  │                                                  │
│  Nav   │            Main Content                          │
│ (240px)│            (Scrollable)                          │
│        │                                                  │
│        │                                                  │
│        │                                                  │
└────────┴──────────────────────────────────────────────────┘
```

---

## 🧭 AdminNav (좌측 네비게이션)

### 구조
```
┌─────────────────────┐
│  📊 CoUp Admin      │ ← 로고
├─────────────────────┤
│  🏠 대시보드        │
│  👥 사용자 관리     │
│  📚 스터디 관리     │
│  🚨 신고 관리       │
│  📝 콘텐츠 모니터링 │
│  📊 통계 분석       │
│  ⚙️  시스템 설정    │
├─────────────────────┤
│  ← 일반 페이지      │ ← 하단
└─────────────────────┘
```

### 스타일
```css
.admin-nav {
  width: 240px;
  height: calc(100vh - 64px);
  background: #1F2937;
  color: white;
  position: fixed;
  left: 0;
  top: 64px;
  overflow-y: auto;
}

.admin-nav-item {
  display: flex;
  align-items: center;
  padding: 12px 20px;
  color: #D1D5DB;
  cursor: pointer;
  transition: all 0.2s;
}

.admin-nav-item:hover {
  background: #374151;
  color: white;
}

.admin-nav-item.active {
  background: #4F46E5;
  color: white;
}
```

### 메뉴 항목
```javascript
const menuItems = [
  { icon: '🏠', label: '대시보드', href: '/admin' },
  { icon: '👥', label: '사용자 관리', href: '/admin/users' },
  { icon: '📚', label: '스터디 관리', href: '/admin/studies' },
  { icon: '🚨', label: '신고 관리', href: '/admin/reports' },
  { icon: '📝', label: '콘텐츠 모니터링', href: '/admin/content' },
  { icon: '📊', label: '통계 분석', href: '/admin/stats' },
  { icon: '⚙️', label: '시스템 설정', href: '/admin/settings' }
]
```

---

## 📌 AdminHeader (상단 헤더)

### 구조
```
┌───────────────────────────────────────────────────────┐
│ 📊 CoUp Admin    [검색창]          🔔(3)  👤 관리자 ▼ │
└───────────────────────────────────────────────────────┘
```

### 구성 요소

#### 1. 로고
- 좌측 정렬
- "📊 CoUp Admin" 텍스트

#### 2. 검색창 (중앙)
```jsx
<input 
  type="search"
  placeholder="사용자, 스터디 검색..."
  className="w-96 px-4 py-2 border rounded-lg"
/>
```

#### 3. 알림 아이콘
- 🔔 아이콘 + 배지 (미처리 신고 수)
- 클릭 시 드롭다운

#### 4. 프로필 드롭다운
```
┌──────────────────┐
│ 👤 관리자        │
│ admin@coup.com   │
├──────────────────┤
│ 👤 마이페이지   │
│ ⚙️  설정         │
│ 🚪 로그아웃      │
└──────────────────┘
```

### 스타일
```css
.admin-header {
  height: 64px;
  background: white;
  border-bottom: 1px solid #E5E7EB;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24px;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
}
```

---

## 📄 AdminLayout (전체 레이아웃 컴포넌트)

### 구현
```javascript
// components/admin/layout/AdminLayout.jsx
export default function AdminLayout({ children }) {
  return (
    <div className="admin-layout">
      <AdminHeader />
      <div className="admin-body">
        <AdminNav />
        <main className="admin-content">
          {children}
        </main>
      </div>
    </div>
  )
}
```

### 스타일
```css
.admin-content {
  margin-left: 240px;
  margin-top: 64px;
  padding: 24px;
  min-height: calc(100vh - 64px);
  background: #F9FAFB;
}
```

---

## 📱 반응형 (선택적)

### Tablet (< 1024px)
- 네비게이션을 햄버거 메뉴로 전환
- 오버레이 네비게이션

### Mobile (< 768px)
- 관리자 페이지는 Desktop 전용 권장

---

**다음 문서**: [02-dashboard.md](./02-dashboard.md)

